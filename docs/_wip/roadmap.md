# プロダクトロードマップ

**プロジェクト:** Vigilith AI（旧 Obsidian Mind）
**初版:** 2026-07-22（最終更新: 2026-08-02）
**基準:** A〜E案と2026-07-31の品質改善（表示Markdownの非同期化・蒸留の復旧チェック・補記の後始末）は実機確認済み。ReadingTrace v1 と C案の一部に実機確認が残る。旧 TimeCapsule 設計は棄却
**課題そのもの（何が壊れているか）は [current_issues.md](current_issues.md) が持つ。** 本書は順序だけを決める。
**形式:** Now / Next / Later（日付を切らず、優先度と成熟度で3段に分ける可変スケジュール）
**主眼:** プロダクトの方向性（北極星）を上位に置き、機能はそこから導く

> **この文書は使い捨て。** 今の並び順と「なぜこの順か」だけを持ち、**完了した項目は取り消し線で残さずその場で削除する**。
> 記録は残る場所が別にあるため（変更は [change_history.md](../dev/change_history.md)、判断は [design/](../dev/design/)、未対応課題は [current_issues.md](current_issues.md)）、
> ここに履歴を溜めると「次に何をやるか」が読み取れなくなる。**項目番号はこのファイル内だけの見出し**なので、振り直しても再利用しても構わない。

---

## 0. 北極星 — このアプリは何であろうとしているか

> **Vigilith AI は「過去の自分の思考と再会し、もう一段深める、オンデバイスAIの読書相手」である。**

このアプリの心臓は **Rediscover（ランダムに過去ノートを引く）**。単なるVaultビューアではなく、「たまたま引いた過去のノート」を起点に、**AIが読書相手として思考を深める手伝いをする**ことに独自性がある。

### 3つのコア価値（ぶらさない軸）

1. **再発見（Rediscover）** — 偶然を"再会"へ変える。アプリの入口であり心臓。
2. **省察（Reflect）** — ノートの中身を育てる（蒸留）／前回の自分の読み方と再会する（ReadingTrace）。
3. **オンデバイス完結** — 全AIは端末内Gemini Nano。クラウド送信ゼロ＝プライバシーが差別化の土台。この制約は弱点ではなく設計哲学。

### 判断基準（機能を足すかどうかの物差し）

- **Rediscover→Reflectのループを濃くするか？** YESなら優先。単独で便利なだけの機能は後回し。
- **オンデバイスNanoの制約（出力256トークン・Mutex直列・60秒）の中で成立するか？**
- **機能ごとに堅牢性レベルを意図的に選べているか？**（蒸留＝重厚 / ReadingTrace＝ベストエフォート）
- **本質（ノートを読む）を妨げないか？** AIは主役ではなく相手役。
- **ユーザーに意識させる必要があるか？** ReadingTrace で分かったのは、**AIの役割を「創作」から「要約」に落とすと、ユーザーの操作を増やさずに価値を出せる**ことだった（→ [reflect_reading_trace](../dev/design/reflect_reading_trace.md) §2）。新機能に導線を足す前に、既にある行動の痕跡で足りないかを先に問う。

---

## 🟢 Now — 直近で着手（今のループを完成させる）

**テーマ: 前提が揃った。次の一手を選ぶ段。**
性能の最優先課題（表示用MarkdownのMain上での二重解析）と F-2 の2件は 2026-07-31 に実装・実機確認とも完了し、
**ReadingTrace v1 も同日クローズした**（残っていた実機確認は構造上ほぼ実施不可能と判断
→ [reflect_reading_trace](../dev/design/reflect_reading_trace.md) §12）。

**instrumentation の土台（旧 N-6）は 2026-08-01 に Android 16 で 2/2 成功し、閉じた。**
**SAF境界の gateway 化（旧 N-7）も 2026-08-01 に全段階が完了した** — ドキュメント参照の
`DocumentRef` 化に加え、さがす／補記の `ContentResolver` と Vault ルートを `VaultBrowser` の裏へ束ね、
`model` / `domain` / `controller` / `ui` から `android.net.Uri` が消えた（詳細は下記 L-3 §）。

**N-1（読書痕跡の孤児削除・段階0〜4）は 2026-08-01 に完了した。** 手動削除まで実装し、
外部レビュー指摘9件（高2・中3・軽微4）も修正済み。実機確認はオプション機能で
クリティカルでないため必須としない（オーナー判断）。設計は
[reflect_reading_trace](../dev/design/reflect_reading_trace.md) §13〜§14 に残る。
**段階5（限定自動化）はスコープ外のまま** — シャドーモードでは保留がほとんど発生せず
（痕跡がまだ少ない）、遮断器が実データで働くところを一度も観測していないため
「十分な実績」が未達。着手条件は §14 のまま据え置く。

**2026-08-02 に L-4 の一部（Markdownのリスト構造）とバッジの記号のコントラスト是正を前倒しで消化した。**
（バッジの塗り側は 2026-08-09 に「ノートへのひとこと」で対象ごと消えたため、課題としても閉じている。）
どちらも Now に積んでいた項目ではなく、実物を見て気づいた順に着手している。
残りは Next / Later のまま（クリック可能リンク・画像・埋め込み・数式、バッジ塗りのナビ帯上の判別）。

**Now は空。** 次に何をやるかは Next / Later から選んで決める。


## 🟡 Next — 次に取り組む（ループを豊かにし、土台を強くする）

**テーマ:「再会」を深く、機能追加に耐える土台へ。**
Nowを閉じた後、再会体験を濃くする拡張（X-2）と、長期運用のための地ならし（X-3・X-6）を行う。
**新機能側の選択肢は X-2 ただ1つで、残りはすべて土台側である**点を意識して順序を決める。

### X-2. 「意図して問いを残す」機能（旧 TimeCapsule の核）
- **なぜここ:** ReadingTrace は行動データなので「**何を考えていたか**」を回収できない。「前回は40%で止まった」は「前回は〇〇が引っかかった」ではない。思考の連続性は依然として未達。
- **なぜ今回見送ったか:** 「無意識に作られる」と「未来へ託す意図」は原理的に両立しない。代替としてセクションチャットの質問を使おうとしたが、**チャットに自由入力がなく**（`SectionChatSheet.kt` に `TextField` がない）AI候補のタップだけだったため、「自分の言葉の問い」にならなかった。→ [reflect_reading_trace](../dev/design/reflect_reading_trace.md) §2
- **取りうる道:** (a) セクションチャットに自由入力を足して「自分の言葉の問い」を痕跡に加える／(b) 作成は無意識のまま、再会カードに「これはまだ考えたい」を置いて**意図を再会側に宿す**／(c) 明示ボタンの機能として独立させる。**(b) が「意識させない」原則と両立する唯一の道**なので、まずここから検討する。

### X-3. ReadingTrace の move/rename 追従
- **孤児掃除は N-1 で完了した**（手動削除。実機確認は必須としない判断で決着）。ここに残るのは move/rename 追従だけ。
- **v1でやらなかった理由:** 引き当てミスが**未読ノートで常時起きる**ため、ミス時にフォルダ全走査をすると
  Rediscover が毎回重くなる。痕跡を失っても `.md` は無傷なので設計原則の範囲内
  → [reflect_reading_trace](../dev/design/reflect_reading_trace.md) §8・判断7。
- **着手はしない。** 改名で残った古い痕跡は、手動削除（N-1）でユーザー自身が掃除できる側に回った
  ため、追従しないことの実害はさらに減っている。段階5（限定自動化）の実績が積み上がった時点で
  改めて要否を見る。

### X-6. AI状態UXの統一
- **なぜここ:** AI非対応・モデル未準備・一時エラーの見せ方が機能ごとにバラつく。蒸留の「インライン説明フォールバック」を良い先例として、要約・検索・クイズ・補記へ横展開。**ReadingTrace は例外として扱う**（意識させない機能なので、失敗は黙って劣化させるのが仕様）。→ [current_issues.md](current_issues.md) AI-2

---

## 🔵 Later — 将来の方向（北極星の解像度を上げる）

**テーマ:「読書相手」としての深みと、Vault規模への強さ。**
ループが確立した先の、体験の質とプロダクトの堅牢性を上げる投資。優先度は状況で見直す。

### L-1. AI入力抽出の残り（予算調整とフォーカスセクション文脈）
- **本体は 2026-07-28 に完了した。** 先頭固定長切り出しは、見出し骨格＋冒頭＋末尾を用途別予算へ配分する抜粋へ置き換わり、実機確認も済んでいる（[PR #43](https://github.com/miyakemasaki0808-MM/ObsidianMind/pull/43)・[ai_input_excerpt](../dev/design/ai_input_excerpt.md)）。**ここに残るのは意図的に切り離した2件だけ。**
- **予算値の調整は 2026-08-01 に完了し、打ち止めにした**（→ [ai_input_excerpt](../dev/design/ai_input_excerpt.md) §14）。関連ノートを 600 → 800 へ上げて実機確認まで実施したが、**体感は変わらなかった**。長く最優先としていた「注意書きが予算の38%」も**トークンでは1.7%**で、短縮注意書き案とともに撤回。**予算を減らしても増やしても品質が動かなかったことで、§9.1「効いたのは量ではなく配置」が両方向から追認された。** 関連ノートの品質に不満が出たら、**原因は予算ではなく候補の選び方・再ランク・指示文の側**を疑う。
- **フォーカスセクション文脈:** [related_notes_ai](../dev/design/related_notes_ai.md) Phase 1 の未実装案。関連ノートは「ノートを開いた瞬間」に自動起動するのに対しフォーカスはスクロールで動くため、**どの時点のセクションで推薦するかを先に決める**必要がある。現在の全体抜粋を置き換えるか補助シグナルにするかも含め、別機能として扱う。

### L-2. Reflect機能群の連携
- 蒸留（重要文の太字化）× ReadingTrace（読書痕跡）× セクションAIを横断させる。例：前回止まった箇所を蒸留の候補選定に効かせる、痕跡をセクションAIのコンテキストに含める。「読書相手」としての一貫した人格に近づける。→ [reflect_reading_trace](../dev/design/reflect_reading_trace.md)

### L-4. Vault規模・堅牢性への投資（必要になったら）
- YAML/Markdown対応拡充（画像・クリックリンク・複数行YAML）、キャッシュ戦略の見直し。ユーザー体験を壊す顕在化があった時に引き上げる。
  **Markdown対応拡充の課題は台帳から削除済み**（2026-08-02。困ってから新規IDで起票する方針）。
- **リストの入れ子・番号・タスク混在は 2026-08-02 に対応した**（→ [design/markdown_rendering.md](../dev/design/markdown_rendering.md)）。残るのはクリック可能リンク・画像・埋め込み・数式。
- **Markdown対応拡充を検討する際は、読書体験だけでなくAI品質側の効果も一緒に数える。** リストの件では、失われていたのが描画だけでなくAI入力の順序情報でもあったため、読書体験だけで測ると過小評価になっていた。**残っている項目にも同じ問いを当てる** — 画像は代替テキストが、リンクは遷移先のタイトルが、いずれもAI入力側で意味を持ち得る。

### L-5. 公開可能な成果物にする（→ current_issues REL-1）
- `applicationId` は `com.vigilith.ai` へ確定し release build type も入ったが、生成物は依然 `app-release-unsigned.apk` で `isMinifyEnabled = false`。**署名も R8 も未設定。** → [current_issues.md](current_issues.md) REL-1
- **設定を1行変える作業ではない。** ML Kit GenAI はリフレクションで解決される部分があり、縮小で消えると**release ビルドの実機でだけ全AI機能が落ちる**。JVMテストは縮小前のクラスを見るので検出できない。**keepルール＋AI 7経路の release 実機一巡がセット**になる。配布の意思が固まってから着手する。
- **効果とコスト:** 開発・リリース運用 7.5 → 8.5（**単一軸では最大の伸び**）。コストは大で、
  R8を有効にした瞬間にJVMテストの守備範囲外へ出る。

> **上の「効果」は品質再評価の軸別スコアから引いた見込みで、測定ではない**（→ [lessons.md](../dev/lessons.md) L12）。
> 総合の上がり幅だけを根拠に順序を決めない。現在の軸別評価は
> [レビュー一覧](../review/README.md) を正とする。

---

## 意図的にやらないこと（Non-Goals）

北極星に照らして、当面やらないと決めるもの。判断のブレを防ぐために明記する。

- **クラウドAIへの切り替え/併用** — オンデバイス完結がプライバシーの土台。Nanoの制約は受け入れるコストであって、逃げる先ではない。
- **汎用ノート編集機能** — 本アプリは「読む・再発見する・省察する」相手役。フルエディタ化はObsidian本体の領分。**ノート本文への書き込みは蒸留の太字化だけ**に限定し、それ以外（補記・読書痕跡）はサイドカーへ逃がす。
- **単独で便利なだけの機能** — Rediscover→Reflectループを濃くしないものは、どれだけ一般的でも優先しない。

---

## 更新運用

- 大きめのPRやマイルストーン到達時に、Now/Next/Laterの中身を1回見直す。
- **完了項目は取り消し線を残さず削除する。** 記録は [change_history.md](../dev/change_history.md) が持つ。
  取り消し線で残すと、完了分がNowの先頭に溜まって「次に何をやるか」が読み取れなくなる。
- 削除でNowが空いたらNextを繰り上げる。**番号は振り直さない**（design/・current_issues が番号で参照するため）。
- 北極星（§0）は安易に変えない。変える時は「なぜ変えたか」をこのセクションに残す。
