# 未対応課題の台帳

**プロジェクト:** Vigilith AI（旧 Obsidian Mind）
**最終更新:** 2026-08-05（TEST-3 を起票）
**この文書が答える問い:** **いま何が壊れている／足りないのか。**

> **ここには未対応のものだけを置く。** **実機検証まで終わったら、その場で削除する**
> （実装完了では消さない。検証待ちが台帳から消えると誰も確認しなくなる）。
> **完了の経緯は残さない** — 経緯を引きたくなったら、それは下記の記録側が足りていないサインと考える。
> 記録の行き先は別にある — 何をいつ変えたかは [change_history.md](../dev/change_history.md)、
> 今どうなっているかは [source_code_analysis.md](../dev/source_code_analysis.md)、
> なぜそうしたかは [design/](../dev/design/)、繰り返し現れた教訓は [lessons.md](../dev/lessons.md)。
>
> **順序と計画は [roadmap.md](roadmap.md) が持つ。** ここには「何が問題か」だけを書き、
> 「いつ・どの順でやるか」は書かない。両方に書くと必ず片方が古くなる。
>
> **番号はカテゴリ記号。** 優先度が変わっても動かないので、振り直さず末尾へ追加する。
> ただし `_wip/` はリリース時に廃棄するため、**恒久文書からは参照させない**。

---

## 一覧

| ID | 課題 | 優先度 | 影響 |
|---|---|:--:|---|
| [TRACE-1](#trace-1-削除直前の再確認が三値でなくルート読取失敗で生きた痕跡を消し得る) | 削除直前の再確認が三値でなく、ルート読取失敗で生きた痕跡を消し得る | **高** | 生きている読書痕跡を削除する |
| [IMG-1](#img-1-寸法未確定の画像がある間の進捗報告が到達率を水増しする) | 寸法未確定の画像がある間の進捗報告が到達率を水増しする | **高** | 未読位置が最深到達点として永続化される |
| [TRACE-2](#trace-2-ルート直下の遮断器が別サブツリーの遮断器を消す) | ルート直下の遮断器が別サブツリーの遮断器を消す | 中 | 保留すべき候補が孤児として出る |
| [TRACE-3](#trace-3-連続削除が同じjobを奪い合う) | 連続削除が同じJobを奪い合う | 中 | 消えたのに画面へ残る |
| [IMG-2](#img-2-索引ヒット時に画像の世代が更新されず古い画像が固定される) | 索引ヒット時に画像の世代が更新されず、古い画像が固定される | 中 | 上書きした画像が再起動まで反映されない |
| [IMG-3](#img-3-boundedinputstream-が上限ちょうどの入力を切断扱いにする) | `BoundedInputStream` が上限ちょうどの入力を切断扱いにする | 中 | 許可範囲内の画像が表示できない |
| [AI-3](#ai-3-完成プロンプトにハード上限が無い) | 完成プロンプトにハード上限が無い | 中 | 長い履歴・質問・候補名で入力が上限を超え得る |
| [TEST-3](#test-3-受付漏れ検査に2つの抜け道がある) | 受付漏れ検査に2つの抜け道がある | 中 | レビュー指摘を取りこぼしても検査が通る |
| [TEST-2](#test-2-実端末を通す統合テストの中身が無い) | 実端末を通す統合テストの中身が無い | 低 | 実端末依存の回帰を検出できない |
| [AI-2](#ai-2-ai状態の一時エラーと非対応を同一視している) | AI状態の一時エラーと非対応を同一視している | 低 | 原因の区別ができない |
| [A11Y-1](#a11y-1-バッジの塗りが下部ナビ帯の上で判別できない) | バッジの塗りが下部ナビ帯の上で判別できない | 低 | 記号は読めるが、存在に気づけない |
| [REL-1](#rel-1-r8とリリース署名が未設定) | R8とリリース署名が未設定 | 低 | 配布可能な成果物がまだ作れない |
| [MAINT-1](#maint-1-依存が12件古いml-kit-genai-の調査は完了上げない判断) | 依存が12件古い（ML Kit GenAI の調査は完了。上げない判断） | **超低** | 現時点で実害なし。beta4 は着手しない |
| [PERF-1](#perf-1-読書痕跡の索引作成がファイル数に比例する) | 読書痕跡の索引作成がファイル数に比例する | 低 | 遠いプロバイダで列挙が重くなる（実測待ち） |
| [MAINT-2](#maint-2-gradientheader-の全幅化が呼び出し側との手動契約になっている) | `GradientHeader` の全幅化が呼び出し側との手動契約になっている | 低 | 余白変更で見出しが崩れる |

**高が2件・中が6件。**

**増えた理由は2つあり、性質が違う。**

- **IMG-1〜3** は画像表示（N-3）の外部レビュー2巡目で見つかった。1巡目の指摘7件へ修正を入れたが、
  **3件は受理条件まで満たしていなかった**（1件は部分解消、2件は目的未達）。
- **TRACE-2・TRACE-3・AI-3 は、2026-08-01 の総評（No.9）で指摘されながら起票されていなかった。**
  総評を文書へ追記した時点で台帳へ落とす運用（[README](../dev/document_map.md) §4 の2）が働かず、
  **2日間どこにも追跡されていなかった。** コードを当たって未対応であることを確認済み。

**「テストが緑」は受理条件を満たしたことを意味しない。** IMG-1〜3 はいずれも
JVMテスト714件が全件成功する状態で残っている。純関数の値伝播と単独部品は覆えても、
**Composeのスクロール状態との交差・索引の時間経過・上限ちょうどの境界**を通していないため。

---


## TRACE-1. 削除直前の再確認が三値でなく、ルート読取失敗で生きた痕跡を消し得る

**実装は 2026-08-05 に完了。実機確認だけが残っている。**
対象は `_ReadingTraces` の補助JSONで、Markdownノート本体ではない。

> **実機確認の要否はオーナーが決める。** 判定は純関数のJVMテストで覆えており
> UI変更も文言追加だけなので「不要」と考える余地はあるが、
> **実装完了で台帳から消さない**のが規約なので、判断が出るまでここに残す。

- **現状:** [`runDelete()`](../../app/src/main/java/com/example/newproject/controller/ReadingTraceCleanupController.kt#L196) は
  削除直前にVaultを再走査するが、判定を `stillMissing: Boolean` へ潰している。
  結果は `DELETED` / `NOT_ORPHAN_ANYMORE` / `FAILED` の3つで、**「確認できなかった」が無い。**
- **成立する順序:** `scan.unreadableFolderPaths` がルートを表す `""` を含み、対象が `ideas/note.md` の場合、
  [`isUnderUnreadableFolder()`](../../app/src/main/java/com/example/newproject/domain/ReadingTraceOrphans.kt#L170) は
  `"ideas" == ""` でも `"ideas".startsWith("/")` でもないので **false** を返す。
  ルートが読めずノートが1件も見えない状態でも `stillMissing = true` になり、**削除する。**
- **洗い出し側だけ守られている:** 同じ状況を洗い出しでは `Blocked` にできている。
  **削除直前だけ安全規則が弱い** — 同じ判定が2箇所にあり、片方だけ強い。
- **もう1つの副作用:** 通常のサブフォルダ読取失敗は削除こそ止まるが `NOT_ORPHAN_ANYMORE` へ畳まれ、
  候補が画面から消える。**「ノートが再出現した」と「確認できなかった」が同じ状態**なので再試行できない。
- **対応候補:** 再確認を `PRESENT` / `MISSING` / `INDETERMINATE` の三値にする。
  ルート読取失敗は全パスを `INDETERMINATE` とし、対象の祖先が読めない場合も同じ扱いにする。
  `INDETERMINATE` では削除せず候補を残す。**安全判定を純関数へ一元化する**のがテスト追加より効く
  （同型の処理が2箇所にある限り、片方だけ直す形が再発する）。
- **規模感:** 小〜中。純関数の三値化とController側の分岐、回帰テスト3件（ネスト候補＋ルート読取失敗で削除しない／
  対象フォルダが読めないとき候補が残る／実際に再出現したときだけ候補から外れる）。
## IMG-1. 寸法未確定の画像がある間の進捗報告が到達率を水増しする

**画像表示（N-3）の設計が最重要とした保護が、まだ閉じていない。**
→ [design/note_image_rendering.md](../dev/design/note_image_rendering.md) §6

- **現状:** 寸法が取れるまでのプレースホルダは画面1枚ぶんの高さを確保する。
  これは「初期オフセットが0なら直後のブロックを隠す」ことしか保証せず、**元画像の高さの上限ではない。**
- **成立する順序:** ①縦長画像の途中まで通常表示でスクロールする ②全画面へ入る —
  [`FullscreenNoteScreen`](../../app/src/main/java/com/example/newproject/ui/screen/FullscreenNoteScreen.kt#L108) は
  `rememberLazyListState(tabListState.firstVisibleItemIndex, tabListState.firstVisibleItemScrollOffset)` で
  **位置を引き継ぐ** ③全画面側は新規コンポジションなので寸法は未確定で、画像は1画面ぶんしかない
  ④引き継いだオフセットが仮の高さを超えると、後続ブロックが可視項目になる
  ⑤`ReadingProgressReporter` が後続indexを報告し、最深到達点は後から下がらない。
  **寸法取得を待っている間にスクロールした場合も同じ**。
- **設計時の判断が誤っていた:** 設計書 §6 は「大きすぎる確保は到達率を低く見積もるだけ」と書いたが、
  これが成り立つのは**プレースホルダが実際の高さ以上のとき**だけで、画面高はそれを保証しない。
  **「誤るなら大きい側へ」は方向としては正しいが、画面高はその『大きい側』になっていない。**
- **影響:** 見た目の一時的なずれではなく、**未読位置が最深到達点としてサイドカーへ永続化される。**
- **対応候補:** 任意に大きい仮高を置くのをやめ、**寸法未確定の画像より後ろの進捗を報告対象から外す。**
  または通常表示と全画面で画像寸法を共有し、新しいLazyListへ未計測状態を持ち込まない。
  **最大寸法から巨大な仮高を作る案はスクロール体験を壊すので採らない。**
- **受理条件:** 寸法未確定の画像より後ろのblock indexが `onReadingProgress` へ渡らない／
  縦長画像の途中から全画面へ入っても測定完了前に最深到達点が進まない／
  測定完了後は正しい高さと位置で報告を再開する／通常表示と全画面の両方を含む回帰テストがある。
- **規模感:** 中。報告の抑止をどこへ置くか（`ReadingProgressReporter` か Coordinator か）の判断を含む。

## TRACE-2. ルート直下の遮断器が別サブツリーの遮断器を消す

- **現状:** [`assessReadingTraceOrphans()`](../../app/src/main/java/com/example/newproject/domain/ReadingTraceOrphans.kt#L110) は
  最も浅いフォルダへ報告を寄せるため、`blocked` にルート `""` と `"ideas"` が同時に入ると
  [`isDescendantFolder`](../../app/src/main/java/com/example/newproject/domain/ReadingTraceOrphans.kt#L157) が
  `""` を `"ideas"` の祖先とみなし、**`"ideas"` を落とす。**
- **一方で** ネスト候補の [`breakerGroupPaths()`](../../app/src/main/java/com/example/newproject/domain/ReadingTraceOrphans.kt#L149) は
  ルートを含めない。したがって `ideas/` 配下の候補は `shallowest`（＝`""` だけ）と一致せず、
  **保留されずに孤児候補として出る。**
- **具体例:** ルート直下で2件、`ideas/` 配下で2件が同時に欠けると、**ルート直下だけが保留され `ideas/` の2件は候補になる。**
- **食い違いの正体:** 「ルートは直接の親としてのグループであり、ネスト候補の共通祖先ではない」という
  設計コメントと実装が、**この混在時だけ**一致していない。
- **対応候補:** ルートのブロックを、非ルートのブロックを包含しない特別値として扱う。上記の混在ケースを回帰テストへ追加する。
- **規模感:** 小。純関数の1条件とテスト1件。

## TRACE-3. 連続削除が同じJobを奪い合う

- **現状:** [`ReadingTraceCleanupController`](../../app/src/main/java/com/example/newproject/controller/ReadingTraceCleanupController.kt#L54) は
  洗い出しと削除に**同じ `job`** を使い、削除のたびに前のJobをキャンセルする。
- **問題:** SAFの `deleteDocument()` は同期的な外部I/Oなので、**キャンセル時点で物理削除だけ完了し、
  その後の状態更新が落ちる**ことがある。さらに各削除は開始時の `current` を捕捉するため、
  AとBを素早く押すと **AのJSONは消えたのに画面に残り**、B完了時には古い一覧を基にBだけを外す。
- **自己修復はするが誤解を与える:** 再洗い出しで整合するものの、その前は「削除に失敗した」ように見える。
- **対応候補:** 洗い出し用のJobと削除を分け、削除は `Mutex` か単一キューで直列化する。
  実行中は同じ候補のボタンを無効化し、完了時は**捕捉した `current` ではなく最新状態から**対象を除く。
  連続2件・洗い出し中の削除・Vault切替中の削除をテストする。
- **規模感:** 小〜中。

## IMG-2. 索引ヒット時に画像の世代が更新されず、古い画像が固定される

- **現状:** 復号キャッシュの鍵へ更新日時（`contentVersion`）を足したが、**その値を取り直す契機が無い。**
  [`VaultImageIndexStore.resolve()`](../../app/src/main/java/com/example/newproject/data/VaultImageIndexStore.kt#L80) は
  `if (!first.isMiss() || now() - loadedAt < ttlMillis) return first` で、**解決に成功したら TTL を見ずに返す。**
  索引の作り直しは「解決に失敗し、かつ索引が古いとき」だけなので、**ヒットし続ける限り世代は初回の値のまま。**
- **成立する順序:** ①初回走査で画像Aの参照と `lastModified = 1` を索引へ保存しBitmapをキャッシュ
  ②Obsidian側で同じ画像Aを上書きし、参照は同じまま `lastModified = 2` になる
  ③TTLを超えて再表示しても古い索引で解決自体は成功する ④再走査されず `contentVersion = 1` のまま
  ⑤鍵が変わらず**古いBitmapを返す。**
- **削除＋同名再作成でも同じ:** 参照が変わっても、古い参照が索引上「ヒット」である限り再走査されず、
  **壊れた表示が固定され得る。**
- **影響:** アプリ再起動またはVault世代の変化まで、更新前の画像か読めない古い参照を表示し続ける。
- **対応候補:** 全Vault走査を毎回行う必要はない。`load()` の直前に解決済みドキュメントの
  `COLUMN_LAST_MODIFIED` を個別照会して鍵に使うか、TTL超過後の最初の画像解決で索引を1回だけ更新する。
  どちらでも**miss再走査の連打防止**と、`lastModified` を返さないプロバイダでの既知制約を明記する。
- **受理条件:** 同じ参照の更新日時が変われば同じ表示幅でも復号し直す／TTL超過後のヒットでも新しい世代を取れる／
  削除・同名再作成で古い参照へ固定されない／壊れたリンクで再コンポーズごとに全走査しない／
  Fake時計と可変の走査結果で「初回load → 外部更新 → 再load」を通す統合テストがある。
- **規模感:** 小〜中。

## IMG-3. `BoundedInputStream` が上限ちょうどの入力を切断扱いにする

- **現状:** [`exhausted()`](../../app/src/main/java/com/example/newproject/domain/BoundedInputStream.kt#L55) は
  `readBytes == maxBytes` になった時点で、**元ストリームに次の1バイトがあるか確認せず** `truncated = true` にする。
- **契約と食い違う:** 呼び出し側のメタデータ判定は `size > MAX_INPUT_BYTES` だけを拒否するので、
  **16MiBちょうどは許可**である。それにもかかわらず、デコーダが終端確認のreadを行うと
  正常な上限ちょうどの画像が `TooLarge` になり得る。
- **`InputStream` の契約違反も1つ:** `readBytes == maxBytes` の状態で `len == 0` の配列readを受けると
  `-1` を返す。契約では0を返すべきで、切断扱いにしてはいけない。`available()` も上限内の残量へ丸めていない。
- **影響:** SAFプロバイダや画像形式による読み方の違いで、許可範囲内の画像が表示できない。
- **対応候補:** **上限到達と真の超過を分ける。** 最大1バイトだけ先読みして超過を確定するか、
  上限＋1バイトまで内部判定用に読み、呼び出し側へは上限までしか返さない。
  単一バイトread・配列read・`skip()` で判定を統一する。
- **受理条件:** `maxBytes - 1` と `maxBytes` は `truncated == false`／`maxBytes + 1` だけ `true`／
  read種別と回数を混ぜても同じ結果／`len == 0` は0を返す／`TooLarge` と `Broken` を取り違えない回帰テストがある。
- **規模感:** 小。**境界値（-1 / ちょうど / +1）を実装と同時に固定していれば防げた型。**

## AI-3. 完成プロンプトにハード上限が無い

- **現状:** 本文の抜粋には用途別の上限があるが、**完成プロンプト全体を閉じる制約が無い。**
  - [`SectionChatController.sendMessage()`](../../app/src/main/java/com/example/newproject/controller/SectionChatController.kt#L95) は
    会話履歴を**全件**渡し、ユーザーの質問にも長さ上限が無い
  - [`renderCandidatesWithinBudget()`](../../app/src/main/java/com/example/newproject/domain/RelatedCandidateContext.kt#L63) の
    最終フォールバックはIDとタイトルを全件残し、**`fits()` を再確認しない。**
    長いタイトル群だけで候補予算を超える場合がある
- **認識はされている:** `PromptTokenBudgetTest` 自身が「定義済み計測プロファイルの回帰であって
  入力長の上限保証ではない」と明記している。**本番側の閉じた制約になっていないだけ。**
- **対応候補:** 完成プロンプト単位の入力予算を1箇所で強制する。履歴は直近N件か文字予算で切る。
  関連候補はタイトルのみでも超える場合に候補数か表示長を減らし、**最終返却前の `fits()` を必須にする。**
- **規模感:** 中。

## TEST-3. 受付漏れ検査に2つの抜け道がある

**2026-08-05 に入れた [`ReviewFindingsLedgerTest`](../../app/src/test/java/com/example/newproject/architecture/ReviewFindingsLedgerTest.kt) 自身の欠陥。**
現在のデータは5条件を満たしているが、**下の2つの経路では取りこぼしても検査が通る。**

- **① 受付IDが日付部分だけで、同じ日のレビューが潰れる。**
  ファイル名から `2026-08-03` の部分だけを取って `Set` にしているので、
  `2026-08-03-other.md` に `P1-1` を足すと、既存の `2026-08-03-image-n3.md` の `P1-1` と
  **同じIDへ潰れる**。受付簿を更新しなくてもテストが通る。
  受付IDは日付ではなく**完全なファイルstem**（`2026-08-03-image-n3/P1-1`）にする必要がある。
  あわせて、**レビュー見出し側・受付行側それぞれの重複IDも明示的に拒否する**
  （潰れたことに気づけないのが今回の根）。
- **② 「起票」の参照先は1件でも実在すれば通る。**
  判定が `referenced.none { it in known }` なので、
  `` `起票` TRACE-2 / TYPO-999 `` のように**実在IDと存在しないIDが混ざっていても成功する**。
  参照されたIDを**すべて**検証し、未知IDが1件でもあれば落とす（`all` にする）。
- **なぜ見つかったか:** 外部レビュー。**変異検証は6件やって全部落ちたのに、この2つは素通りした** —
  変異が「既存データを壊す」形だけで、**「新しいデータを足す」形を試していなかった**。
- **対応候補:** 上記2つに加え、**変異の型に「足す」を含める**
  （既存行を壊す変異だけでは、追加時に潰れる欠陥を検出できない）。
- **規模感:** 小。テストの数行と、変異検証のやり直し。**プロダクトコードには触らない。**

## TEST-2. 実端末を通す統合テストの中身が無い

- **現状:** SAF走査・補記保存/削除・端末AI・Compose Navigation・全画面遷移・画面回転/プロセス再生成・
  連続操作時の競合を絡めたテストがない。JVMテスト714件は純粋ロジックの回帰防止に有効だが、
  実端末依存の動作は保証範囲外。**画像表示（N-3）で「テスト714件が緑でも受理条件は満たしていない」
  ことが実例として出た**（→ IMG-1〜3）ので、この課題の重さは以前より上がっている。
- **前提が2つある。①は 2026-08-01 に満たした** — Android 16 で 2/2 成功し、**Runnerが起動しComposeを描画できることが実証された**（土台の故障と切り分けられる状態になった）。
  **②も 2026-08-01 に完了した。** `DocumentRef` 化（段階1〜6）に加え、`VaultBrowser` で
  `ContentResolver` と Vault ルートを束ねた（段階7）。`model` / `domain` / `controller` / `ui` から
  `android.net.Uri` が消え、`PackageDependencyTest` で固定済み。
  **JVMで書けるものが instrumentation へ流れ込む状態は解消した** — さがす・補記の世代照合と
  走査キャッシュは21件のJVMテストで覆われている。**`VaultBrowser` 移行の実機確認も完了した**
  （旧 VERIFY-4）。
- **部分的な前進:** 補記の保存経路だけは 2026-07-31 に `AnnotationDocumentGateway` として切り出し、
  失敗注入をJVMテストで書けるようにした。**同じ形が機能することは確認済み**で、残るのは他への横展開。
- **`Uri` を import するファイルは17→8になった** → [design/saf_boundary_gateway.md](../dev/design/saf_boundary_gateway.md)。
  残る8は `data` 7・`NoteViewModel` 1 で、**いずれもSAF境界／Android境界そのもの**。
- **規模感:** 大。**前提は2つとも揃ったので、残るのは実テストの中身そのものだけ。**

## AI-2. AI状態の一時エラーと非対応を同一視している

- **現状:** [`checkAvailability()` 自体の例外を `Unavailable` にまとめる](../../app/src/main/java/com/example/newproject/ai/AICoreClient.kt#L67)ため、
  非対応端末と一時的な状態取得失敗を区別できない。
- **対応候補:** AI非対応・モデル未準備・一時エラーのUXを要約・検索・クイズ・補記で統一する。
  **ReadingTrace は横展開の対象外**（意識させない機能なので、失敗は黙って劣化させるのが仕様）。
  → [lessons.md](../dev/lessons.md) L4
- **規模感:** 中。

## A11Y-1. バッジの塗りが下部ナビ帯の上で判別できない

- **現状:** AIタブのバッジは、パネルではなく**下部ナビ帯**の上に載る。ライトのナビ帯はブランド色
  `Indigo #4D3DFF` で、その上での塗りのコントラストは **Success 1.61・Error 1.04**（非文字基準は3:1）。
- **問題:** 塗りだけではバッジの存在に気づけない。中の記号（「✓」「!」）は
  2026-08-02 に `onStatusBadge`（記号なので3:1基準）へ整理して**読めはする**が、それは見つけた後の話。
  **この課題は塗り側だけを指す。**
- **直さなかった理由:** ライトのIndigoは彩度が高く相対輝度も中位で、**その上で3:1を取れる塗りがほとんど無い**。
  直すならナビ帯の色かバッジの明度、あるいは輪郭線の追加になり、いずれもブランド配色そのものへ踏み込む。
  [`AppColorContrastTest`](../../app/src/test/java/com/example/newproject/ui/theme/AppColorContrastTest.kt)
  に実測値を固定するテストだけ置いてある。
- **輪郭線案は 2026-08-02 に実装したが、見た目が悪く差し戻した。** `LogoNavy` の1dp輪郭を足すと
  数値上は 3.15 で基準を満たすが、**10dp程度のバッジに濃紺の環が乗ると重く、汚れのように見える**。
  グラデーション上のボタン（`buttonOutlineOnGradient`）で同じ手が成立するのは面積が大きいからで、
  **同じ解き方が小さい部品へはそのまま移らない。** 再提案するなら輪郭線以外の道を先に検討する。
- **見落としの構造:** 「塗りには必ず対の前景を持つ」は決めたが、**塗り自身が載る面を数えていなかった**。
  → [lessons.md](../dev/lessons.md) L9
- **規模感:** 小〜中（値を決めるだけなら小。ナビ帯の色に触るなら中）。

## REL-1. R8とリリース署名が未設定

- **現状:** `buildTypes { release { } }` と `proguard-rules.pro` は置いたが、
  **`isMinifyEnabled = false`／`signingConfig` 無し**。`assembleRelease` の生成物は
  依然として `app-release-unsigned.apk`（約10MB）で、配布可能性を意味しない。
- **後回しにした理由:** ML Kit GenAI はリフレクションで解決される部分があり、縮小で消えると
  **全AI機能が実機の release ビルドでだけ落ちる**。JVMテストは縮小前のクラスを見るので検出できない。
  Compose の Composable メタデータも同様。つまりR8の有効化は「設定を1行変える作業」ではなく
  **実機検証の計画とセット**になる。
- **対応候補:** keep ルールを書いた上で `isMinifyEnabled = true` にし、release ビルドを実機へ入れて
  7つのAI経路（要約・補記・クイズ・関連ノート・セクションチャット・蒸留・読書痕跡の要約）を一巡する。
  署名は keystore の保管方針を決めてから。
- **規模感:** 中。実機確認が必須。

## MAINT-1. 依存が12件古い（ML Kit GenAI の調査は完了。上げない判断）

**優先度は超低。** 2026-08-01 に最大の未知だった `genai-prompt` beta4 を調査し切り、
**「上げない」と判断した**（下記）。残っているのは「古い」という状態だけで、**現時点で実害は無い。**
**着手の契機は「新版に必要な機能が入ったとき」であって、新版が出たことではない。**

- **方針は 2026-08-01 に確定した** → [design/dependency_policy.md](../dev/design/dependency_policy.md)。
  更新の単位（7グループ）・棚卸しの手順と契機・各グループの確認範囲を決めてある。
  3チェックは `disable` から **`informational`（hint）へ変えた** — 素のまま有効化すると12件が
  Error になり `lintDebug` タスクが失敗するが、`informational` なら
  **「0 errors, 0 warnings, 12 hints」で成功し、12件はレポートに残る**。
  ゲートに載せずに催促だけ残せる。
- **残っている問題:** 方針ができても**依存は12件古いまま**で、1件も上げていない。距離の内訳は
  [dependency_policy.md](../dev/design/dependency_policy.md) §4 のスナップショットにある。
  優先して見るべきは2つ。
  - **`com.google.mlkit:genai-prompt` 1.0.0-beta2 → beta4 は当面着手しない（2026-08-01 決定）**
    → [design/dependency_policy.md](../dev/design/dependency_policy.md) §5。公開APIの削除はゼロでソース互換だが、
    **`maxOutputTokens` の未指定時の既定値が 256 → 4096 へ変わる。**
    実測の `getTokenLimit()` が 4,352 なので入力に許されるのは256トークンだけになり、
    **最小の読書痕跡要約（349）すら通らない。** つまり**そのまま上げると8経路すべてが止まる。**
    上げるなら `maxOutputTokens` の明示設定が**前提条件**で、値の決定・AI 8経路の実機一巡・
    基準線の再取得がセットになる。**新版が出たことは上げる理由にならない**ので、
    beta4 の新機能（構造化出力・`SystemInstruction`）を実際に使いたくなるまで動かさない。
  - **Compose BOM（2024.09.03）はLintの指摘に出てこない。** `platform()` 宣言が更新対象として
    見られていないためで、最新である意味ではない。**棚卸しの自動検出から漏れる唯一の枠**なので手で確認する。
- **規模感:** 調査は小。更新の実行はグループごとに小〜中で、いずれも実機確認を伴う。


## PERF-1. 読書痕跡の索引作成がファイル数に比例する

**実測で問題化するまで着手しない。** 先回りで入れると移行リスクだけを先払いになる。

- **現状:** `_ReadingTraces/` はフラットな1階層で、セッション中1回だけ子一覧を全列挙して
  key→参照の索引を作る。容量は問題にならない（1ファイル最大3.3KB・2,000ノートで約1.4MB）が、
  **列挙コストはファイル数に比例**する。孤児削除（手動）は入ったが**押されるとは限らない**ため、
  ファイル数は単調増加**しうる**（→ [design/reflect_reading_trace.md](../dev/design/reflect_reading_trace.md) §14）。
  遠いプロバイダ（Google Drive 等）ほど効く。
- **対応候補:** 削除ではなく**分割**で解く。ファイル名が `sha256Hex(相対パス)` なので、
  **先頭1桁で16フォルダへ分ける**と1件の照合で列挙する件数が 1/16 になる。
  引く側はキーから所属フォルダを計算できるので探索は要らない。
  **生きた痕跡を1つも消さずに性能だけ守れる**のが孤児削除に対する優位点。
  → [design/reflect_reading_trace.md](../dev/design/reflect_reading_trace.md) §13 判断14
- **着手の契機:** 実機で列挙の遅さが体感できたとき。既存ファイルの移行を伴うので、それまで動かさない。
- **規模感:** 中（移行と実機確認を伴う）。

## MAINT-2. `GradientHeader` の全幅化が呼び出し側との手動契約になっている

**単独では着手しない。** 該当ファイルを別の理由で触るときに、ついでに直す。

- [`GradientHeader`](../../app/src/main/java/com/example/newproject/ui/component/GradientHeader.kt#L50) の
  `horizontalBleed = 20.dp` は、呼び出し側の水平余白と一致している前提で成立する。現在の画面では
  合っているが、**余白を変えた瞬間に見出しだけがずれる**。レイアウトテストは無い。
  配色は「テストで強制する」ところまで持っていったのに対し、寸法の対応関係は約束のままである。
- **規模感:** 極小。
