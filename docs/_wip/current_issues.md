# 直近課題抽出

**プロジェクト:** Vigilith AI（旧 Obsidian Mind）
**抽出日:** 2026-07-22（最終更新: 2026-07-25）
**基準:** ReadingTrace v1 実装済み・JVMテスト通過・実装レビュー課題あり / `feature/New_AI_Function_TimeCapcel`
**目的:** 次の一手を決めるための、現時点で確認できる課題の棚卸し。ロードマップ（[roadmap.md](roadmap.md)）の入力とする。

> 何をいつ変えたかは [change_history.md](../change_history.md)、今どうなっているかは [source_code_analysis.md](../source_code_analysis.md)、なぜそうしたかは [design/](../design/) を参照。本ファイルは「まだ手を付けていない/追いついていない」課題のみを新しい順の観点で集約する。

---

## 0. サマリー（優先度マップ）

| 優先度 | 課題 | 種別 | 影響 |
|---|---|---|---|
| 高 | ReadingTraceの到達率がMarkdownブロック数基準で、長い1ブロックを即100%扱いする | 正確性/UX | 「最後まで読んだ」という誤表示 |
| 高 | `onStop()`後の再開で読書セッションを再開せず、復帰後の進捗を失う／背面時間を10秒判定へ含める | Lifecycle | 訪問・最深到達点が不正確 |
| 高 | Vault切替前に起動済みの痕跡保存が、切替後のVaultへ書き込まれ得る | データ分離/並行処理 | 旧Vaultの痕跡が新Vaultへ混入 |
| 高 | 検索フォールバックの文言差（40件以下で先頭3件なのに「キーワード一致」表示） | 一貫性/UX | 利用者の期待とのズレ |
| 中 | Job管理の不統一（検索・補記一覧等が未保護） | 並行処理 | 連続操作時の競合余地 |
| 中 | 読書痕跡の孤児ファイルが掃除されない／move・rename に追従しない | 保守 | 長期運用でファイル数が単調増加 |
| 中 | `_ReadingTraces`の索引が外部同期で追加されたファイルをプロセス再起動まで認識しない | 同期/キャッシュ | 再会カードの見逃し・重複作成余地 |
| 中 | 30件上限後も保持件数を「これまで開いた回数」と表示する | 正確性/UX | 31回目以降も「30回」と誤表示 |
| 中 | AI入力が先頭固定長切り出し | AI品質 | 長文ノートの後半が無視される |
| 低 | 統合テスト不足（SAF・端末AI・Navigation） | テスト | 実端末依存の回帰を検出できない |
| 低 | エラー通知の握りつぶし（フォルダ取得失敗・補記削除失敗等） | 堅牢性 | 無言の失敗 |
| 低 | AI状態の一時エラーと非対応の同一視 | UX | 原因の区別ができない |

---

## 1. 高優先度

### 1-1. ReadingTraceの到達率がMarkdownブロック数基準
- **現状:** UIは `LazyListState.layoutInfo.visibleItemsInfo.lastOrNull()?.index` を最終可視位置として送り、Controllerは `(blockIndex + 1) / totalBlocks` で到達率を計算する。分母・分子はいずれも画面上の距離ではなくMarkdownブロック数。
- **問題:** 非常に長い段落・コード・表・リストが1ブロックとして描画されると、冒頭しか見えていなくても `1 / 1 = 100%` になる。カードは100%以上を「最後まで読んでいます」と断定するため、機能の中心データが誤る。
- **対応候補:** `LazyListItemInfo` のoffset/sizeとviewportEndOffsetを使って最終ブロック内の可視位置を加味する、またはブロックごとの描画高さを重みにした到達率へ変更する。少なくとも「100%」判定は最終ブロックの末端がviewport内へ入った場合に限定する。
- **必要な検証:** 1つの長大段落、長大コードブロック、短い複数ブロック、通常/全画面の切替をCompose UIまたはinstrumentationで確認する。現行JVMテストは均一なブロック番号しか検証していない。
- **規模感:** 中。UI座標を扱うため純粋JVMテストだけでは完結しない。

### 1-2. Activity停止・再開と読書セッションの不整合
- **現状:** `MainActivity.onStop()`が `flushReadingTrace()` を呼ぶ。10秒以上ならSessionの `recorded` が永久に `true` になり、同じノートへ戻っても新しいSessionを開始しない。10秒未満ならSessionは未記録のまま残り、`openedAtMillis` も更新されない。
- **問題:** 10秒以上読んで背面へ回った場合、復帰後に読み進めた位置が保存されない。10秒未満で背面へ回った場合は、背面にいた時間が10秒判定へ混ざり、実際には短時間しか読んでいなくても次の離脱時に訪問扱いになり得る。
- **対応候補:** `onStop`で現在の訪問を確定してSessionを閉じ、`onStart`/`onResume`で表示中ノートの新しいSessionを開始する。あるいはControllerへ明示的な `pause/resume` を導入し、能動読書時間だけを積算する。
- **必要な検証:** 10秒未満/以上の双方で「背面化→復帰→追加スクロール→再離脱」をテストする。Activity lifecycleを含むためinstrumentationが望ましい。
- **規模感:** 中。ViewModelが現在ノートのパス・タイトル・documentIdを再開始できる形も必要。

### 1-3. Vault切替と起動済み保存コルーチンの競合
- **現状:** `flush()`は `viewModelScope.launch` で保存を非同期起動し、保存先Gatewayは実際のI/O時点で `vaultUri()` を読む。`saveVault()`の `discard()` は現在Sessionを捨てるだけで、すでに起動済みの保存Jobを待機・キャンセルしない。
- **問題:** 旧Vault向けのflushが待機中にVaultを切り替えると、相対パス・タイトルは旧ノートのまま、保存先だけ新Vaultになる可能性がある。現在のコメントは「切替前にSessionを捨てれば防げる」としているが、起動済みJobには効かない。
- **対応候補:** 保存要求にVault UriまたはVault単位のPersistenceをスナップショットとして持たせる。加えてVault切替時に旧Vaultの保存Job完了を待つ、または世代IDで旧要求を破棄する。
- **必要な検証:** 保存を意図的に遅延させたFake Gatewayで、flush直後にVaultを切り替えても新Vaultへ旧データが書かれないことを確認する。
- **規模感:** 中。保存先の動的lambda設計を見直す必要がある。

### 1-4. 検索フォールバックの意味差
- **現状:** 自然文検索の候補が40件以下のとき、bigramによる並べ替えを行わず SAF が返した候補リストの**先頭3件**をそのまま返す。一方、画面文言は「キーワード一致で表示しています」。
- **問題:** AI非対応・未ダウンロード時のフォールバックが、文言の示す「キーワード一致上位」になっていない。
- **対応候補:** `SearchPickerUseCase` のフォールバックを候補数に関係なく bigramスコア順に統一し、UI文言と実装を一致させる。（解析書 §6.5 / §14.2 / §15-1）
- **規模感:** 小。純粋ロジック側で完結しやすくテストも書きやすい。

---

## 2. 中優先度

### 2-1. Job管理の不統一
- **現状:** クイズ・補記・蒸留は requestId＋Job で保護済み。一方、検索・スコープ内ランダム・補記一覧/削除・要約側のモデルDL Job は要求単位で追跡されない。
- **問題:** 短時間に複数要求できる経路で、完了順による状態上書きの余地が残る。将来UI導線が増えると顕在化し得る。
- **対応候補:** 検索にも Job または requestId を導入し、最後の要求だけが状態を更新できるようにする。（解析書 §10.1 / §14.2 / §15-2）
- **規模感:** 中。

### 2-2. AI入力の先頭固定長切り出し（関連ノートは設計書との乖離も含む）
- **現状:** 要約・関連・補記いずれも `String.take()` による文字数上限で、意味境界やトークンで切っていない。長いノートの後半（結論が末尾にある等）はAI入力に含まれない。クイズと蒸留はこの型から抜けている（クイズ＝フォーカス周辺、蒸留＝全文を文分割してスコア選抜）。
- **関連ノート固有の問題:** `design/related_notes_ai.md` の Phase 1 に「フォーカスセクション文脈で推薦し、先頭切り出しを解消」と書かれていたが、**2026-07-24 の実装突合で未実装と判明**（`fetchRelatedNotes` はノート全文を渡し、プロンプト側で先頭600文字に切る。履歴上も一度も入っていない）。設計書・change_history は訂正済みで、**課題としてはここに残る**。
- **対応候補:** 見出し・冒頭・末尾・重要語を考慮した抽出へ発展させる。ただしNanoの入力予算とのトレードオフあり。関連ノートでフォーカスセクション文脈を採る場合は、**自動起動（ノートを開いた瞬間に実行）とスクロール追従するフォーカスの噛み合わせ**をどう決めるかが論点（クイズは「吹き出しから明示起動」で回避したが、同じ手は使えない）。
- **規模感:** 中〜大。品質改善だが検証が要る。

### 2-3. 読書痕跡の孤児ファイルと move/rename 追従
- **現状:** `_ReadingTraces/<相対パスのSHA-256>.json` は相対パスでしか引き当てない。ノートを削除・改名すると対応する痕跡は**誰も掃除せず**、新しいパスで別ファイルが作られる。
- **問題:** 容量は問題ない（1ファイル最大3.3KB・2,000ノートで約1.4MB）。効いてくるのは**ファイル数に比例する処理**で、年単位の運用で孤児が単調増加する。
- **v1で見送った理由:** 自動削除は、同期途中や一時的な読み取り失敗でノートが走査結果に現れないときに**生きている痕跡を消す**危険がある。move/rename 追従も、引き当てミスが未読ノートで常時起きるためフォルダ全走査のコストが恒常化する。
- **対応候補:** オプション画面に手動の一括削除を置く（`_AI補記` の `AnnotationManagerScreen` と同じ発想）／痕跡に「最後に確認できた日時」を持たせ、長期間見つからないものだけ落とす。（design/reflect_reading_trace.md §8・roadmap X-3）
- **規模感:** 小〜中。

### 2-4. `_ReadingTraces`索引が外部同期の追加を認識しない
- **現状:** `SafReadingTraceDocumentGateway` はVaultごとにフォルダの子一覧を1回だけ読み、key→Uriの `FolderIndex` をプロセス内に保持する。自分で作ったファイルは追記するが、Google Drive等の外部同期で後から増えたファイルを再走査する契機がない。
- **問題:** 別端末で作られた痕跡が同期されても、現在プロセスではカードが出ない。さらに未存在と判断して保存すると、プロバイダによっては同じkeyの重複ファイルを作り、次回索引時にどちらを採るかが列挙順依存になる。
- **対応候補:** 短いTTL、Rediscover照合でミスした場合だけの再走査、またはContentObserver相当の無効化を検討する。毎回全走査は避けつつ、外部追加を最終的に認識できる設計にする。
- **規模感:** 中。同期プロバイダ上での実機確認が必要。

### 2-5. 訪問保持上限と累計回数の混同
- **現状:** `withVisit()`は訪問を最大30件へ切り詰める。一方、カードとAIプロンプトは `visits.size` を「これまで開いた回数」として使う。
- **問題:** 31回目以降も常に「これまで30回」と表示・要約される。保存履歴数と累計訪問数は異なる概念。
- **対応候補:** `totalVisitCount`を別フィールドとして累積し、`visits`は傾向分析用の直近30件として維持する。既存schemaからの移行方針も合わせて決める。簡易対応なら文言を「記録に残っている直近30回」に限定する。
- **規模感:** 小〜中。schemaVersion更新を伴う場合は移行テストが必要。

---

## 3. 低優先度（顕在化していないが記録）

### 3-1. 統合テスト不足
- SAF走査・補記保存/削除・Gemini Nano・Compose Navigation・全画面遷移・連続操作時の競合を絡めたテストがない。ローカルユニットテストは純粋ロジックの回帰防止には有効だが、実端末依存の動作は保証範囲外。
- 対応候補: Fake `ContentResolver` または instrumentation テストで Vault走査・保存・削除を検証。（解析書 §13.3 / §15-5）

### 3-2. エラー通知の握りつぶし
- フォルダ一覧取得失敗はユーザーに通知されない。補記削除は `deleteDocument()` の戻り値を確認せず一覧を再読込する。閲覧履歴のJSONパース失敗は空履歴扱い。
- 対応候補: 少なくとも削除失敗・フォルダ取得失敗に明示エラーを出す。（解析書 §11.2）

### 3-3. AI状態の一時エラーと非対応の同一視
- `checkAvailability()` 自体の例外を `Unavailable` にまとめるため、非対応端末と一時的な状態取得失敗を区別できない。
- 対応候補: AI非対応・モデル未準備・一時エラーのUXを要約・検索・クイズ・補記で統一する。（解析書 §11.2 / §15-8）

### 3-4. その他の既知の限定実装
- YAML解析が簡易（複雑なYAML・ネスト・複数行値は未対応 → tags/aliasesの取りこぼし）
- Markdownが限定実装（ordered list番号・クリック可能リンク・画像・埋め込み・数式は未対応）
- キャッシュTTL 60秒により外部同期・編集結果の反映が最大60秒遅れる

---

## 4. 横断テーマ（課題の背景にある構造）

1. **Nano制約が全設計を規定している** — 出力256トークン上限・Mutex直列化・60秒タイムアウトが、要約字数・クイズ形式・補記項目数・ReadingTraceの「生の痕跡を先に出して要約は裏で」まで決めている。新機能はこの制約と必ず衝突するため、判断軸として明文化する価値がある。
2. **機能ごとに堅牢性レベルを変える方針への転換** — 蒸留（重厚な原子性・復旧）→ ReadingTrace（シンプル最優先・ベストエフォート）。この使い分けは健全だが、どの機能をどのレベルで守るかの基準を残すと再現性が上がる。
3. **文言と実装の乖離** — 検索フォールバック（1-4）が代表例。機能追加が続く中で、小さいうちに潰すのが得策。
4. **AIの役割を「創作」から「要約」へ落とすと導線が要らなくなる** — ReadingTrace で得た知見。ユーザーの操作を増やさずに価値を出せるかは、**既にある行動の痕跡で足りないか**を先に問うと判断しやすい。→ [reflect_reading_trace](../design/reflect_reading_trace.md) §2
5. **「意識させない機能」は他の機能と作法が違う** — 通知しない・失敗を見せない・自動DLしない、が仕様になる。AI状態UXの統一（3-3）を進める際、ReadingTrace は横展開の対象外として扱う必要がある。

---

## 4.5. 解決済み（記録のみ・再掲不要）

- **解析書ヘッダ／規模表の未追従**（旧 2-2, 優先度中）— 2026-07-24 に解消。ヘッダを `be1c0e9` 基準へ更新し、§2.1 規模表を実測値（本番50ファイル9,377行・テスト29ファイル192ケース）へ、§3 ファイル構成に `Distill*`・`NoteSnapshot`・`QuizInputProfile` を追加、§13 テスト表を全面差し替え。あわせて `testDebugUnitTest` 192ケースのグリーンを確認済み。
- 同日、design/ の記述ズレも一部修正（`note_fullscreen` の全画面FAB 44dp→48dp、`architecture` のViewModel行数・Controller数）。関連ノートの「フォーカスセクション文脈」が**未実装**だった件は `related_notes_ai.md` / `change_history.md` を訂正し、課題そのものは 2-2 へ移設。
- **Controller共通化のトリガー到達**（旧 2-1, 優先度中）— 2026-07-24 に判断実施、**共通化しない**で決着。共通なのは requestId ガードの数行のみで、モデルDLポリシー（自動再開 vs 明示タップ）・通知の有無・状態数（4 vs 11）が異なる。判断根拠と再検討条件は `design/architecture.md` の 2026-07-24 追記に記録済み。4件目の `ReadingTraceController` でも 2026-07-25 に再判定し**同じ結論**（同追記に記録）。
- **vault相対パス生成**（旧 1-2, 優先度高・実装ブロッカー）— 2026-07-25 に解消。BFS走査と相対パス連結を純関数 `traverseMarkdownPaths`（`VaultPathTraversal.kt`）へ切り出し、`NoteFile` に `vaultRelativePath` を追加。`DocumentsContract` がAndroid staticでBFSごとテストできない問題を、Uri構築だけをリポジトリ層に残すことで回避し、10ケースのJVMテストを付けた。副産物として `SafDocuments.kt` へSAF操作を集約し `_AI補記` と共有した。
- **旧TimeCapsule設計の棄却**（旧 5-A）— 2026-07-25。「ボタンではなく無意識の補助機能に」という要求で前提が崩れ、**ReadingTrace** として設計を置換して実装完了。残った「意図して問いを残す」課題は [roadmap.md](roadmap.md) X-2 へ移設。→ [reflect_reading_trace](../design/reflect_reading_trace.md)

---

## 5. 次アクションの選択肢

- **A. ReadingTraceの正確性・Vault分離を修正**（1-1〜1-3）
- **B. 検索フォールバックの一貫性を解消**（1-4）
- **C. 読書痕跡の保守性に手を入れる**（2-3〜2-5）
- **D. 「意図して問いを残す」の検討**（ReadingTraceで回収できなかった思考の連続性 → roadmap X-2）

→ 中長期の進め方は [roadmap.md](roadmap.md) に整理する。
