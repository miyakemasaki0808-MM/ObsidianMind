# 直近課題抽出

**プロジェクト:** Vigilith AI（旧 Obsidian Mind）
**抽出日:** 2026-07-22（最終更新: 2026-07-29）
**基準:** ReadingTrace（実機確認待ち・サイドカーは schema v2）・Vigilith Phase 3（最終実機確認済み）・
ダークモード・B案（実機確認済み）・A案／C案／D案／E案（コード上は完了、実機確認待ち）・
AI入力の抜粋化（実機確認済み・PR #43） / `feature/Improvement_Function_No.5`
**直近の入力:** 2026-07-28 のソースコード品質再評価（[source_code_quality_review.md](../source_code_quality_review.md)）。
前回の主要指摘はA〜C案で解消し、総合評価は7.2→8.0へ改善した。再評価で別系統の3件
（UI側Markdown解析、蒸留復旧チェックの競合、補記保存失敗時の残骸）を追加起票した。
2026-07-29 にD案（ライト配色のAA是正）とE案（リリース構成の整備）を実施し、
アクセシビリティと開発・リリース運用の2軸に初めて手を入れた。**ライトの見た目は意図的に変わっている。**
**目的:** 次の一手を決めるための、現時点で確認できる課題の棚卸し。ロードマップ（[roadmap.md](roadmap.md)）の入力とする。

> 何をいつ変えたかは [change_history.md](../change_history.md)、今どうなっているかは [source_code_analysis.md](../source_code_analysis.md)、なぜそうしたかは [design/](../design/) を参照。本ファイルは「まだ手を付けていない/追いついていない」課題のみを新しい順の観点で集約する。**解消した課題はこのファイルから削除する**（記録は上記3文書に残るので、ここに残すと未対応課題が埋もれる）。
>
> **番号はこのファイル内だけの見出し。** 恒久文書からは参照させない（`_wip/` はリリース時に廃棄するため、恒久側が番号に依存すると全部リンク切れになる）。したがって振り直しても再利用しても構わない。欠番の一覧も持たない。いつ何を解消したかは [change_history.md](../change_history.md) を見る。

---

## 0. サマリー（優先度マップ）

> **A案（非同期の境界）・C案（入出力の境界）は 2026-07-26、B案（依存と状態の境界）は 2026-07-27 に完了**したため、対象だった課題は本ファイルから削除した。実施前の効果見込みは [§5](#5-改善活動abc案は完了)、完了後の再評価は [source_code_quality_review.md](../source_code_quality_review.md) にまとめる。

| 優先度 | 課題 | 種別 | 影響 | 計画 |
|---|---|---|---|:--:|
| 高 | **2-6 最大1MBのMarkdownをCompose Main上で解析し、全画面で再解析する** | 性能 | 長文を開く時と全画面進入時にUIが停止し得る | — |
| 中 | **2-5 関連ノートは抜粋予算600のうち38%を注意書きに払っている** | AI品質 | 現ノート文脈が実質228文字しか届かない | — |
| 中 | **2-7 蒸留の復旧チェックだけJob・requestId管理の外にある** | 並行処理 | 復旧表示を進行中分析が上書きし得る | — |
| 中 | **2-8 補記ファイルの書込失敗で空・部分ファイルが残り得る** | 保存 | 失敗表示後もVaultに残骸が残る | — |
| 中 | 2-3 読書痕跡の孤児ファイルが掃除されない／move・rename に追従しない | 保守 | 長期運用でファイル数が単調増加 | — |
| 中 | 2-4 `_ReadingTraces`の索引が外部同期で追加されたファイルをプロセス再起動まで認識しない | 同期/キャッシュ | 再会カードの見逃し・重複作成余地 | — |
| 低 | 3-1 統合テスト不足（SAF・端末AI・Navigation） | テスト | 実端末依存の回帰を検出できない | — |
| 低 | 3-3 AI状態の一時エラーと非対応の同一視 | UX | 原因の区別ができない | — |
| 低 | 3-4 YAML/Markdownの限定実装・キャッシュTTL 60秒 | 機能限定 | 取りこぼし・反映遅延 | — |
| 低 | **3-15 バッジの塗りが下部ナビ帯の上で判別できない** | a11y | Success 1.61・Error 1.04。記号は読める | — |
| 低 | **3-16 R8とリリース署名が未設定** | リリース | 配布可能な成果物がまだ作れない | — |
| 低 | **3-17 依存更新の方針が無く、Lintの更新系3チェックを無効化している** | 保守 | 更新の判断が先送りされ続ける | — |

> **D案・E案は 2026-07-29 に完了**したため、対象だった 3-8・3-9・3-12・3-13・3-14 は本ファイルから
> 削除した。判断の記録は [theme_and_ui_refactor](../design/theme_and_ui_refactor.md) の追記にある。
> やり残しは上の 3-15〜3-17 として起票し直した（全部が片付いたわけではないが、残りは別の問題である）。
> **D案・E案とも実機確認待ち**（→ §6 ①）。ライトの見た目は意図的に変わっている。

---

## 1. 高優先度

### 2-6. 表示用Markdown解析がMain上に残り、通常／全画面で二重に走る（2026-07-28 追加）
- **現状:** [`NoteReaderTab`](../../app/src/main/java/com/example/newproject/ui/screen/NoteReaderTab.kt#L92) と
  [`FullscreenNoteScreen`](../../app/src/main/java/com/example/newproject/ui/screen/FullscreenNoteScreen.kt#L125) が、
  それぞれの `remember` 内で `buildNoteSectionModel(content)` を同期実行する。表示フォールバックは最大1MB。
- **問題:** 同じMarkdownパーサを使うAI入力抜粋は、既存実測で1MB約460ms・200KB約20msだったため
  `Dispatchers.Default` へ移したが、表示経路はComposeのMain上に残っている。全画面は別Composableの
  `remember`なので、同じ本文を進入時にもう一度解析する。長文を開く時と全画面進入時にUIが停止し得る。
- **文書との差:** `source_code_analysis.md` §9.4は通常／全画面が「同じパース済みブロック」を使うと説明するが、
  実装上は共有されない。
- **対応候補:** ノート読込後にDefault上で `NoteSectionModel` を1回だけ作り、通常／全画面へ共有する。
  Composable内の同期解析を検出するソース走査テストと、200KB／1MBの時間・キャンセル計測を追加する。
- **規模感:** 中。見た目は変えず、状態または専用キャッシュへパース結果を通す必要がある。

> ReadingTrace v1 で 2026-07-25 に解消した4件（到達率・Lifecycle・Vault分離・検索フォールバック）は
> SAF実装のVault照合と Activity lifecycle を通した pause/resume が JVMテストの範囲外のため、
> **実端末確認は未実施**。完了判定は [roadmap.md](roadmap.md) N-1 の「完了の定義」で追跡する。

---

## 2. 中優先度

### 2-7. 蒸留の復旧チェックだけJob・requestId管理の外にある（2026-07-28 追加）
- **現状:** [`DistillController.checkRecovery()`](../../app/src/main/java/com/example/newproject/controller/DistillController.kt#L334) は
  裸の `scope.launch` で、Jobを保持せず、通常の分析が使う `activeRequestId` も進めない。
- **問題:** Vault切替直後などに復旧確認が遅延し、その間に新しい分析が始まると、復旧結果が
  `RecoveryRequired` を出した後で進行中分析の候補が同じrequestIdのまま後着し、復旧表示を上書きできる。
  保存層は未解決レコードを再確認するので危険な書込は止まるが、表示の混乱と不要なAI実行が起きる。
- **対応候補:** 復旧確認を追跡Jobへ載せ、確認中は新規分析を開始させないか、復旧結果を反映する時点で
  分析世代を無効化する。遅い復旧確認と分析を交差させるJVMテストを追加する。
- **規模感:** 小〜中。

### 2-8. AI補記は書込失敗時に作成済みファイルを後始末しない（2026-07-28 追加）
- **現状:** [`NoteRepository.createAnnotationFile()`](../../app/src/main/java/com/example/newproject/data/NoteRepository.kt#L197) は
  `createDocument()` の後に対象URIへ直接書き込み、ストリームopen失敗・途中書込例外時の削除や再読込検証を行わない。
- **問題:** UIは生成失敗を表示しても、`_AI補記` に空または部分ファイルが残り得る。ファイル名も分単位なので、
  同一分の再生成でプロバイダが改名した場合、Controllerの予測名と実名がずれる。
- **対応候補:** 書込失敗時に作成済みURIをベストエフォート削除し、書込後の最低限の内容検証を行う。
  表示名は保存後のメタデータから取得し、Fake gatewayによる失敗注入テストを追加する。
- **規模感:** 小〜中。蒸留ほどの復旧機構は不要。

### 2-5. 関連ノートは抜粋予算の38%を注意書きに払っている（2026-07-28 追加）
- **現状:** AI入力の抜粋化で、抜粋であることを伝える注意書き（226文字）を用途別上限の**内側**から支払う設計にした。上限を超えない不変条件が保てる代わりに、予算の小さい経路ほど占有率が跳ね上がる。

| 用途 | 上限 | 注意書きの占有率 | AIへ届く実本文 |
|---|---:|---:|---:|
| 要約 | 1,200 | 19% | 678文字 |
| 補記 | 1,500 | 15% | 903文字 |
| **関連ノート** | **600** | **38%** | **228文字** |

- **問題:** 関連ノートの現ノート文脈は実質228文字しかなく、骨格・冒頭・末尾に配分すると各部が数十文字になる。**抜粋化の恩恵がこの経路だけ薄い**可能性がある。
- **単独で数字をいじってはいけない:** 関連ノートのプロンプトは候補ブロック3,500文字＋指示文＋タイトルを含む。600だけを見て増やすと完成プロンプトが Prompt API の入力トークン上限に近づく。**実トークン計測が前提**（`countTokens()` / `getTokenLimit()` が利用できる）。
- **対応候補:** 関連ノート専用の短縮注意書きを持たせる（候補側は既にID提示で構造化されているため、注意書きを削っても誤読しにくい）／上限を800→1,000の順で単独調整する。どちらも計測が先。
- **前提:** モデルへ実際に渡ったプロンプトを見る手段が無いため、現状は判断材料が無い（→ feature_ideas D-1 プロンプトログ）。
- **規模感:** 小（短縮注意書きのみ）〜中（予算調整まで含める場合）。

### 2-3. 読書痕跡の孤児ファイルと move/rename 追従
- **現状:** `_ReadingTraces/<相対パスのSHA-256>.json` は相対パスでしか引き当てない。ノートを削除・改名すると対応する痕跡は**誰も掃除せず**、新しいパスで別ファイルが作られる。掃除APIもオプション画面の導線も存在しない。
- **問題:** 容量は問題ない（1ファイル最大3.3KB・2,000ノートで約1.4MB）。効いてくるのは**ファイル数に比例する処理**で、年単位の運用で孤児が単調増加する。
- **v1で見送った理由:** 自動削除は、同期途中や一時的な読み取り失敗でノートが走査結果に現れないときに**生きている痕跡を消す**危険がある。move/rename 追従も、引き当てミスが未読ノートで常時起きるためフォルダ全走査のコストが恒常化する。
- **対応候補:** オプション画面に手動の一括削除を置く（`_AI補記` の `AnnotationManagerScreen` と同じ発想）／痕跡に「最後に確認できた日時」を持たせ、長期間見つからないものだけ落とす。（design/reflect_reading_trace.md §8・roadmap X-3）
- **規模感:** 小〜中。

### 2-4. `_ReadingTraces`索引が外部同期の追加を認識しない
- **現状:** [`ReadingTraceStore`](../../app/src/main/java/com/example/newproject/data/ReadingTraceStore.kt#L210) はVaultごとにフォルダの子一覧を1回だけ読み、key→Uriの `FolderIndex` をプロセス内に保持する。自分で作ったファイルは追記するが、TTLも無効化契機も無く、Google Drive等の外部同期で後から増えたファイルを再走査する契機がない。
- **問題:** 別端末で作られた痕跡が同期されても、現在プロセスではカードが出ない。さらに未存在と判断して保存すると、プロバイダによっては同じkeyの重複ファイルを作り、次回索引時にどちらを採るかが列挙順依存になる。
- **対応候補:** 短いTTL、Rediscover照合でミスした場合だけの再走査、またはContentObserver相当の無効化を検討する。毎回全走査は避けつつ、外部追加を最終的に認識できる設計にする。
- **規模感:** 中。同期プロバイダ上での実機確認が必要。

---

## 3. 低優先度（顕在化していないが記録）

### 3-1. 統合テスト不足
- SAF走査・補記保存/削除・Gemini Nano・Compose Navigation・全画面遷移・画面回転/プロセス再生成・連続操作時の競合を絡めたテストがない。`app/src/androidTest` ソースセット自体が存在しない。ローカルユニットテスト450件は純粋ロジックの回帰防止には有効だが、実端末依存の動作は保証範囲外。
- **B案で前提は解消:** 7 Controllerの生成・状態所有・切替調停をAndroid APIを呼ばない `NoteSessionCoordinator` へ出し、Vault/ノート切替の一斉停止・初期化を素のJVMで固定した。`NoteViewModel` を直接生成しないのは、Robolectricやモック依存を足さない判断による。残るのは実際の `ContentResolver`・端末AI・Activity/Composeを通す境界テストである。
- **土台は 2026-07-29 に解消（E案）:** `androidTestImplementation` 一式・`testInstrumentationRunner`・
  `compose-ui-test-junit4`・`espresso` を追加し、`app/src/androidTest` に土台スモークテストを1本置いた
  （Runnerの起動・対象Contextの取得・Composeの描画の3点だけを見て、土台の故障とテスト対象の故障を
  切り分けられるようにしたもの）。**着手時に環境構築から始める必要はもう無い。**
- **残っているのは中身:** SAF走査・補記保存/削除・端末AI・Compose Navigation・全画面遷移・
  画面回転/プロセス再生成・連続操作時の競合。いずれも実端末が要る。
- 対応候補: Fake `ContentResolver` または instrumentation テストで Vault走査・保存・削除を検証。（解析書 §13.4 / §15-5）

### 3-3. AI状態の一時エラーと非対応の同一視
- [`checkAvailability()` 自体の例外を `Unavailable` にまとめる](../../app/src/main/java/com/example/newproject/ai/AICoreClient.kt#L67)ため、非対応端末と一時的な状態取得失敗を区別できない。
- 対応候補: AI非対応・モデル未準備・一時エラーのUXを要約・検索・クイズ・補記で統一する。（解析書 §11.2 / §15-8）

### 3-4. その他の既知の限定実装
- YAML解析が簡易（[`parseMeta`](../../app/src/main/java/com/example/newproject/data/NoteRepository.kt#L217) は `---` 囲みの先頭ブロックから `tags`/`aliases` を単純抽出するだけ。ネスト・複数行値は未対応 → 取りこぼし）
- Markdownが限定実装（ordered list番号・クリック可能リンク・画像・埋め込み・数式は未対応）。
  **ordered list の番号は描画だけでなくAI入力でも失われる** — 予算超過ノートの抜粋は同じパーサを通すため、
  番号自体に意味があるノート（手順書など）では順序の情報がモデルへ届かない
- キャッシュTTL 60秒（`NOTES_CACHE_TTL_MS`）により外部同期・編集結果の反映が最大60秒遅れる

### 3-15. バッジの塗りが下部ナビ帯の上で判別できない（2026-07-29 追加）
- **現状:** AIタブのバッジは、パネルではなく**下部ナビ帯**の上に載る。ライトのナビ帯は
  ブランド色 `Indigo #4D3DFF` で、その上での塗りのコントラストは
  **Success 1.61・Error 1.04**（非文字基準は3:1）。
- **問題:** 塗りだけではバッジの存在に気づけない。中の記号（「✓」「!」）は
  D案で対の前景に直したので**読めはする**が、それは見つけた後の話。
- **D案で直さなかった理由:** ライトのIndigoは彩度が高く相対輝度も中位で、
  **その上で3:1を取れる塗りがほとんど無い**。直すならナビ帯の色かバッジの明度、
  あるいは輪郭線の追加になり、いずれもブランド配色そのものへ踏み込む。
  [`AppColorContrastTest`](../../app/src/test/java/com/example/newproject/ui/theme/AppColorContrastTest.kt)
  に実測値を固定するテストだけ置いた。
- **見落としの構造:** ダークモード実装時に「塗りには必ず対の前景を持つ」は決めたが、
  **塗り自身が載る面を数えていなかった**。パネル上の3:1は測っていて、同じトークンが
  ナビ帯にも出ることを見ていない。→ 横断テーマ13
- **規模感:** 小〜中（値を決めるだけなら小。ナビ帯の色に触るなら中）。

### 3-16. R8とリリース署名が未設定（2026-07-29 に範囲を絞って再起票）
- **現状:** E案で `buildTypes { release { } }` と `proguard-rules.pro` は置いたが、
  **`isMinifyEnabled = false`／`signingConfig` 無し**。`assembleRelease` の生成物は
  依然として `app-release-unsigned.apk`（約11MB）で、配布可能性を意味しない。
- **R8を後回しにした理由:** ML Kit GenAI はリフレクションで解決される部分があり、
  縮小で消えると**全AI機能が実機の release ビルドでだけ落ちる**。JVMテストは縮小前の
  クラスを見るので検出できない。Compose の Composable メタデータも同様。
  つまりR8の有効化は「設定を1行変える作業」ではなく**実機検証の計画とセット**になる。
- **対応候補:** keep ルールを書いた上で `isMinifyEnabled = true` にし、release ビルドを
  実機へ入れて7つのAI経路（要約・補記・クイズ・関連ノート・セクションチャット・蒸留・
  読書痕跡の要約）を一巡する。署名は keystore の保管方針を決めてから。
- **規模感:** 中。実機確認が必須。

### 3-17. 依存更新の方針が無く、Lintの更新系3チェックを無効化している（2026-07-29 追加）
- **現状:** E案で警告0を達成する際、`GradleDependency`・`NewerVersionAvailable`・
  `AndroidGradlePluginVersion` を [app/build.gradle.kts](../../app/build.gradle.kts) の
  `lint { disable }` へ入れた。**指摘そのものは消えていない**（9件相当）。
- **無効化した理由:** 「いつ・どこまで上げるか」の方針が無いまま毎ビルドで出しても行動に
  つながらない。CLAUDE.md は「依存ライブラリを一括更新しない。機能単位で上げ、実機確認を伴う」
  と定めているので、**出しっぱなしにすると規約と矛盾した状態が常態化する**。
- **問題:** 黙らせた以上、更新の判断が誰からも催促されなくなった。特に ML Kit GenAI は
  beta2 であり、SDKの制約がバイナリを見ないと分からない前例（`maxOutputTokens` の1〜256）がある。
- **対応候補:** 更新の単位と頻度を決める（例: 四半期に1回、ML Kit GenAI は単独で、
  Compose BOM は BOM ごと、実機確認をセットにする）。決めたら `disable` から外す。
- **規模感:** 小（方針を決めるだけ）。実行は別。

---

## 4. 横断テーマ（課題の背景にある構造）

1. **Nano制約が全設計を規定している** — 出力256トークン上限・Mutex直列化・60秒タイムアウトが、要約字数・クイズ形式・補記項目数・ReadingTraceの「生の痕跡を先に出して要約は裏で」まで決めている。新機能はこの制約と必ず衝突するため、判断軸として明文化する価値がある。
2. **機能ごとに堅牢性レベルを変える方針への転換** — 蒸留（重厚な原子性・復旧）→ ReadingTrace（シンプル最優先・ベストエフォート）。この使い分けは健全だが、どの機能をどのレベルで守るかの基準を残すと再現性が上がる。
3. **文言と実装の乖離** — 検索フォールバックが代表例（画面は「キーワード一致」と言うのに実装は列挙順の先頭3件だった。2026-07-25 に解消）。機能追加が続く中で、小さいうちに潰すのが得策。
4. **AIの役割を「創作」から「要約」へ落とすと導線が要らなくなる** — ReadingTrace で得た知見。ユーザーの操作を増やさずに価値を出せるかは、**既にある行動の痕跡で足りないか**を先に問うと判断しやすい。→ [reflect_reading_trace](../design/reflect_reading_trace.md) §2
5. **「意識させない機能」は他の機能と作法が違う** — 通知しない・失敗を見せない・自動DLしない、が仕様になる。AI状態UXの統一（3-3）を進める際、ReadingTrace は横展開の対象外として扱う必要がある。
6. **「対策を入れた」と「対策が効いている」は別** — Vault分離（2026-07-25）は、方針が正しかったぶん実装が効いていなくても文書・コミットメッセージが通ってしまった。修正の主張は、修正コードを別の目で読むまで確定させない。→ [bugfix_reports](../bugfix_reports.md) #4
7. **「整理した」と「境界が効いている」も別**（2026-07-26 追加、2026-07-27 B案で解消） — パッケージのレイヤー別整理（PR #37）は正しい方向だったが、依存の**向き**は制約されないままで循環が4組残っていた。B案では共有型を葉の `model` へ移し、許可方向をimport走査テストで固定した。状態所有も同様に、KDocの約束ではなく機能別Writerで制限した。同じ型がコントラストテストにもあった。既知未達を「失敗させず実測値で記録する」形にしていたため、**テストが全緑でもAA準拠を意味しない**状態が続いていた（2026-07-29 D案で、記録から強制へ移して解消）。**「何を保証していないか」を測定と同じ場所に書く**と、この型の誤読を防げる。
8. **横展開は「最後の1本」を取り残す**（2026-07-26 追加・A案で解消） — モデルDLの世代管理は `QuizController`・`AnnotationController`・`DistillController` の3つに入っているのに、**Controller化されなかった `NoteViewModel` 直書きの1本だけが取り残されていた**。パターンを横展開するときは、**Controller一覧ではなく「同じAPIを呼ぶ全箇所」を検索対象にする**（`grep downloadModel` で4箇所目が出る）。同じ型の見落としは、痕跡の保存が `viewModelScope` に載ったままだった件（C案で解消）にもあった。

9. **テストが「効いている」かは変異させて確かめる**（2026-07-26 追加） — A案の `SummaryControllerTest` は初版が `cancelAndClear()` 経由で書かれていたため、`downloadJob` が止まって requestId ガードに到達せず、**ガードを外しても落ちなかった**。テーマ6の「対策を入れた≠効いている」はテスト自身にも当てはまる。**新しいガードを入れたら、そのガードを1行消してテストが落ちることを確認する**。落ちなければ、テストが検証しているのは別の機構か、そのガードが等価な冗長コードのどちらか（今回は両方あった）。

10. **「純粋」と「軽い」は別**（2026-07-28 追加） — AI入力の切り出しをAndroid依存の無い純関数へ出したのは判断として正しかったが、**その関数は最大1MBの本文をパースして約460msかかり、それを `Dispatchers.Main` のスコープから呼んでいた**。従来の `take()` が実質ゼロコストだったため、「賢くした」結果として新しい負荷が持ち込まれていた。テーマ7（整理した≠境界が効いている）の同族で、**純関数化はテスト容易性を保証するが実行コストについては何も保証しない**。Mainのスコープから呼ぶ純関数は、**入力サイズに比例するかどうか**を必ず見る。

11. **AI出力を別のAI入力へ流す箇所では、改善も劣化も乗算される**（2026-07-28 追加） — 補記プロンプトは本文だけでなく **AI要約と関連ノートも入力として受け取っている**。抜粋化で3つとも同時に良くなったため、補記の改善だけが突出して体感された。裏返すと**要約の品質は補記の品質でもある**ので、上流を触るときは下流の消費者を数えてから優先度を決める。現在の消費関係は「要約 → 補記」「関連ノート → 補記」の2本。

12. **効果の説明と効果の測定は別**（2026-07-28 追加） — 抜粋化がなぜ効くかはコードから筋の通った説明ができるが、**A/B比較はしていない**。さらに予算内のノートはプロンプトが1文字も変わらないため、短いノートでの体感の変化は Nano の出力揺らぎでしかあり得ない。**「モデルに実際に何を渡したか」を見る手段が無い**ことがこの限界の根で、計測器としてのプロンプトログを feature_ideas に D-1 として起票した。テーマ6を「主張」から「効果」へ広げた形になる。

13. **トークンを検証するときは「その色が実際に載る面」を全部数える**（2026-07-29 追加） — ダークモード実装で「塗りには必ず対の前景を持つ」という規約を作り、`errorSurface`/`onErrorSurface` の対まで用意した。それでもバッジは直り切らなかった。**塗り vs 前景は測ったのに、塗り vs 背面はパネルの上でしか測っていなかった**からで、同じトークンが下部ナビ帯（ライトは彩度の高いIndigo）にも出ることを見ていない。テーマ8「横展開は最後の1本を取り残す」の配色版だが、取り残されたのは呼び出し箇所ではなく**組み合わせの片側**である。色は単体では検証できず、常に「どの面の上か」とセットでしか意味を持たない。→ 3-15

14. **警告を黙らせると、判断の催促も一緒に消える**（2026-07-29 追加） — Lint警告0を達成する際、依存更新系の3チェックを無効化した。「方針が未定のうちは出しても行動につながらない」という理由は正しいが、**黙らせた瞬間から誰も更新を促さなくなる**。特に ML Kit GenAI は beta2 で、SDKの制約がバイナリを見ないと分からない前例がある。**抑制は「やらない」ではなく「いつやるか決める」とセットにしないと、静かな放置に変わる**。抑制した項目は必ず課題として起票し直す（→ 3-17）。同じことは `@Suppress("UsableSpace")` にも当てはまるが、こちらは理由と代替案をコード内に書いたので起票しない。

---

## 5. 改善活動（A・B・C案は完了）

2026-07-26 の品質総評（7.2 / 10）を受けた改善活動は、2026-07-27 にコード上の実装を完了した。

> **A案（非同期の境界）と C案（入出力の境界）は 2026-07-26、B案（依存と状態の境界）は 2026-07-27 に完了した。**
> A案＝Vault世代の採番・全4経路の世代照合・削除失敗件数の表示。
> B案＝依存生成と調停の分離・機能別StateWriter・循環4組の解消と依存方向のCI固定。
> C案＝用途別の読込予算・累計回数の分離（schema v2）・保存スコープと保存結果の扱い。
> 対象課題はいずれも本ファイルから削除済み。判断の記録は
> [architecture.md](../design/architecture.md)（世代IDの二層化・依存と状態の境界）と
> [reflect_reading_trace.md](../design/reflect_reading_trace.md)（判断9・判断10）にある。
> **B案は実機確認済み。A案・C案は実機確認待ち**（→ §6 ①）。

### 合計効果

下表はA・B・C案の実施前に置いた見込み値。2026-07-28の再評価では総合 **8.0 / 10** を確認したが、新規課題も3件見つかったため、現在の軸別評価は [source_code_quality_review.md](../source_code_quality_review.md) を正とする。

| 評価軸 | 当初 | A・B・C案後（見込み） | 効いた案 |
|---|---:|---:|---|
| 可読性 | 7.5 | 8.0 | B |
| 保守性 | 6.5 | 7.5 | B |
| 拡張性 | 6.5 | 7.5 | B |
| 信頼性 | 7.0 | 8.5 | A, C |
| テスト容易性 | 7.5 | 8.5 | A, B |
| 並行処理 | 7.0 | 8.5 | A |
| 性能 | 6.5 | 8.0 | C |
| セキュリティ・プライバシー | 8.5 | 8.5 | — |
| アクセシビリティ | 6.5 | 6.5 | **なし** |
| 開発・リリース運用 | 7.0 | 7.0 | **なし** |
| ドキュメント | 8.5 | 8.5 | — |
| **総合** | **7.2** | **約8.0** | |

> **2026-07-28 のAI入力の抜粋化はこの表の外側にある**（A・B・C案の一部ではない）。再評価では抜粋化の効果を確認した一方、表示用Markdown解析がMainに残るため、性能は7.0と評価した。
> 性能軸には注意が要る — 抜粋生成は1MBノートで約460msかかり、**従来の `take()` に無かった負荷**である。
> `Dispatchers.Default` へ逃がしてMainは止めないが、処理そのものが消えたわけではない。

### 枠外（D案・E案は 2026-07-29 に完了）

A〜C案では**アクセシビリティ 6.5 と開発・リリース運用 7.0 が動かなかった**ため、別立てで進めた。

- **D. ライト配色のAA是正**（旧 3-8・3-9）— 完了。グレーを3段階へ統合し、彩度を持つ3色は色相を保ったまま明度を下げた。テストを「既知未達の記録」から「基準の強制」へ移し、旧値へ戻す変異で落ちることを確認した。**残ったのはナビ帯の上のバッジ塗り（→ 3-15）**
- **E. リリース構成の整備**（旧 3-14・3-12・3-13）— 完了。`buildTypes`・`lint {}`・androidTest基盤・Java 11 を入れ、`applicationId` を `com.vigilith.ai` に確定し、Lint warning を0にして `warningsAsErrors` で固定した。**残ったのは R8/署名（→ 3-16）と、黙らせた依存更新系（→ 3-17）**

> **どちらも「軸を満点にした」わけではない。** D案はライトの文字と塗りをAA準拠にしたが、
> バッジという1組が面の数え漏れで残った。E案は構成の枠を作ったが、R8と署名という
> 「実機検証を伴う部分」には踏み込んでいない。**残りは同じ課題の続きではなく、
> 実機確認を必要とする別の性質の作業**なので、番号を振り直して起票してある。

---

## 6. 次アクションの選択肢（改善活動に含まれないもの）

> **A〜E案のコード実装は完了した。** 品質軸で残るまとまりは無い。
> ここに残すのは、品質軸の点数では測れない「実機確認」と「機能側の判断」だけ。
> 記号が §5 と衝突しないよう ①〜④ で振る。

- **① 実機確認（A案・C案・D案・E案／ReadingTrace）** — JVMテストで覆えない境界を同じ端末セッションでまとめて確認する。B案の回帰確認は2026-07-27、AI入力の抜粋化は2026-07-28に完了済み。
  - **A案（2026-07-26 完了分）**: ⓐさがすタブで検索実行中に別フォルダのchipをタップ → 前の結果が出ないこと
    ⓑVault切替直後にさがすタブへ入り、旧Vaultのフォルダchipsが出ないこと
    ⓒ補記管理画面を開いた直後にVault切替 → 旧Vaultの補記が並ばないこと
    ⓓ（モデル未DL状態を作れれば）DL中にノート切替 → 旧ノートの要約・Downloading表示が出ないこと。
    **ⓑとⓒは非nullの `Uri` が要るためJVMテストで固定できていない**ので、ここだけが担保手段になる。
    ⓓは `SummaryControllerTest` でフル検証済みなので、未DL状態を作るのが難しければ省略してよい。
  - **C案（2026-07-26 完了分）**: サイドカーが schema v2 へ上がったので、**移行の確認が最優先かつ不可逆**。
    ⓔ**v1 の痕跡がある状態で更新し、再会カードが従来どおり出ること**（回数・最終位置・要約）。
    ここが壊れると既存の痕跡が全部「破損＝孤立」になる
    ⓕ同じノートを読み返して「これまで◯回開いています」が増えること
    ⓖノート表示中にホーム→復帰→離脱で、回数が1回分しか増えないこと
    ⓗ**10秒以上読んだ状態でタスクスワイプ終了 → 再起動して Rediscover し、その閲覧が記録されていること**
    ⓘ1MB超のノートを開いて先頭が表示され、切り詰めた旨の文言が出ること・固まらないこと
    ⓙ巨大ノートを含むVaultで、関連ノートの生成が以前より遅くなっていないこと
  - **D案（2026-07-29 完了分）**: **ライトの見た目が意図的に変わる**ので、確認の性質が他と違う。
    数値はJVMテストで固定済みなので、実機で見るのは「読みやすくなったか」ではなく
    **「暗くしすぎて印象が変わっていないか」**。ライトモードで一巡する。
    ⓚ関連ノートタブの「関連ノート」「AI推薦」の2見出しが、**互いに別の色として見分けられること**
    （彩度と明度を落としたので、紫と緑が近づいて見えないか）
    ⓛノート一覧の更新日時が、本文より弱いまま読めること（弱くなくなると本文と競合する）
    ⓜ緑ボタン（「Vaultを選択」「🎰 ランダムに引く」）が白背景から浮いて見えること
    ⓝ空状態の案内・注記・完了済みタスクの打ち消し線が、**濃くなりすぎて主張していないか**
    （旧 `#888888`・`#999999` が `#757575` へ寄ったので、ここが一番印象が変わる）
    ⓞダークモードへ切り替えて、**何も変わっていないこと**（ダーク値は `buttonSecondary` 含め据え置き）
  - **E案（2026-07-29 完了分）**: `applicationId` を変えたので、**端末上は別アプリとして入る**。
    ⓟ**初回起動でVaultの選び直しを求められること**（SAFの永続URI権限はパッケージ単位なので、
    旧アプリの権限は引き継がれない）。ここで旧アプリが残っていることに気付けるかも見る
    ⓠVaultを選び直した後、`_ReadingTraces` の再会カードと `_AI補記` が従来どおり見えること
    （どちらもVault側にあるので消えていないはず）
    ⓡ補記を1件生成し、**ファイル名の日時と本文の日時が一致すること**
    （`Date()` を2回作っていたのを1回にまとめた。分をまたぐ瞬間の食い違いが消えている）
    ⓢランチャーアイコンが従来どおり出ること（`mipmap-anydpi-v26` → `mipmap-anydpi` へ改名した）
  - **ReadingTrace（2026-07-25 完了分）**: 到達率・Lifecycle・Vault分離・検索フォールバックの4件。
    これが済むまで roadmap N-1 は完了扱いにしない。
  - **AI入力の抜粋化（2026-07-28 完了・2026-07-28 実機確認済み・[PR #43](https://github.com/miyakemasaki0808-MM/ObsidianMind/pull/43)）**:
    7経路すべての入力の作り方が変わった。設計は [ai_input_excerpt](../design/ai_input_excerpt.md)。
    回帰・引っ掛かり・抽出効果・Nano誤読・特殊構造ノート・状態遷移の確認観点はPR本文に記載済みで、
    問題は報告されていない。**「効果の確認は1回の生成で判断しない」原則（→ §4 テーマ12）のとおり、
    体感の裏付けであり厳密なA/B測定ではない**点は変わらない。
- **② 読書痕跡の保守性**（2-3 孤児ファイル・2-4 索引の無効化契機）— C案で解消した累計回数・保存スコープとは別系統で、
  どちらも同期プロバイダ上での実機確認が要る。①と同じ端末セッションで扱える。
- **③ AI入力の予算調整**（2-5 関連ノートの注意書き比率）— **計測が先**。用途ごとの完成プロンプトの実トークン数を
  見ないと、600だけを動かして候補ブロック3,500文字と衝突する。計測手段そのものが無いので、
  実質的には feature_ideas D-1（プロンプトログ）が前提になる。①の実機確認で
  「関連ノートだけ抜粋化の恩恵が薄い」感触が得られたら優先度を上げる。
- **④ 「意図して問いを残す」の検討** — ReadingTraceで回収できなかった思考の連続性（→ roadmap X-2）。唯一の新機能側の選択肢。

→ 中長期の進め方は [roadmap.md](roadmap.md) に整理する。
