# 未対応課題の台帳

**プロジェクト:** Vigilith AI（旧 Obsidian Mind）
**最終更新:** 2026-08-09（**ANNOT-1〜3 と A11Y-1 を実機確認済みでクローズ**。
「AI補記メモ」は「ノートへのひとこと」へ作り直され、5巡の実機確認を終えた
→ [features/reflect_remark.md](../dev/features/reflect_remark.md)）
**この文書が答える問い:** **いま何が壊れている／足りないのか。**

> **ここには未対応のものだけを置く。** **実機検証まで終わったら、その場で削除する**
> （実装完了では消さない。検証待ちが台帳から消えると誰も確認しなくなる）。
> **完了の経緯は残さない** — 経緯を引きたくなったら、それは下記の記録側が足りていないサインと考える。
> 記録の行き先は別にある — 何をいつ変えたかは [change_history.md](../dev/change_history.md)、
> 今どうなっているかは [source_code_analysis.md](../owner/source_code_analysis.md)、
> なぜそうしたかは [features/](../dev/features/)・[system/](../dev/system/)、繰り返し現れた教訓は [lessons.md](../dev/lessons.md)。
>
> **順序と計画は [roadmap.md](roadmap.md) が持つ。** ここには「何が問題か」だけを書き、
> 「いつ・どの順でやるか」は書かない。両方に書くと必ず片方が古くなる。
>
> **番号はカテゴリ記号。** 優先度が変わっても動かないので、振り直さず末尾へ追加する。
> ただし `_wip/` はリリース時に廃棄するため、**恒久文書からは参照させない**。

---

## 一覧

| ID | 課題 | 優先度 | 影響 |
|---|---|:--:|---|
| [TRACE-3](#trace-3-連続削除が同じjobを奪い合う) | 連続削除が同じJobを奪い合う | 中 | 消えたのに画面へ残る |
| [IMG-2](#img-2-索引ヒット時に画像の世代が更新されず古い画像が固定される) | 索引ヒット時に画像の世代が更新されず、古い画像が固定される | 中 | 上書きした画像が再起動まで反映されない |
| [AI-3](#ai-3-完成プロンプトにハード上限が無い) | 完成プロンプトにハード上限が無い | 中 | 長い履歴・質問・候補名で入力が上限を超え得る |
| [AI-6](#ai-6-プロンプトの字下げが全行に残っている) | プロンプトの字下げが全行に残っている | 中 | 入力トークンを無駄に払い、計測の基準線も汚れている |
| [TRACE-4](#trace-4-ユーザーが書いた返事に退避手段が無い) | ユーザーが書いた返事に退避手段が無い | 中 | Vaultのフォルダが消えると書いた言葉ごと失われる |
| [DISTILL-1](#distill-1-蒸留候補にwikilinkだけの行が上がる) | 蒸留候補に wikilink だけの行が上がる | 中 | 太字にする価値の無い行が候補枠を占める |
| [AI-4](#ai-4-aiピッカーだけid契約から外れている) | AIピッカーだけID契約から外れている | 中 | 候補が黙って消え、検索結果が減る |
| [AI-5](#ai-5-関連ノートが算出した根拠を捨てている) | 関連ノートが算出した根拠を捨てている | 低 | なぜ関連なのかが画面から分からない |
| [AI-2](#ai-2-ai状態の一時エラーと非対応を同一視している) | AI状態の一時エラーと非対応を同一視している | 低 | 原因の区別ができない |
| [REL-1](#rel-1-r8とリリース署名が未設定) | R8とリリース署名が未設定 | 低 | 配布可能な成果物がまだ作れない |
| [MAINT-1](#maint-1-依存が12件古いml-kit-genai-の調査は完了上げない判断) | 依存が12件古い（ML Kit GenAI の調査は完了。上げない判断） | **超低** | 現時点で実害なし。beta4 は着手しない |
| [PERF-1](#perf-1-読書痕跡の索引作成がファイル数に比例する) | 読書痕跡の索引作成がファイル数に比例する | 低 | 遠いプロバイダで列挙が重くなる（実測待ち） |
| [MAINT-2](#maint-2-gradientheader-の全幅化が呼び出し側との手動契約になっている) | `GradientHeader` の全幅化が呼び出し側との手動契約になっている | 低 | 余白変更で見出しが崩れる |

**高が0件・中が7件・低が5件。**

**増えた理由は3つあり、性質が違う。**

- **AI-4・AI-5・DISTILL-1 は 2026-08-05 に、既存機能の強化点を洗う目的でコードを読んで見つけた。**
  外部レビューでも総評でもなく、**「補記の評価内容がわかりにくい」「蒸留のレコメンドがイマイチ」という
  オーナーの実感が出発点**である。体感から入って実装を当てたら、原因が6件に分かれた。
  **うち4件は出力の品質ではなく契約の緩さ**
  （空でも保存が通る・固定選択肢を検証しない・ID契約が1経路だけ抜けている・算出した根拠を捨てる）で、
  **プロンプトを直しても解けない**。「AIの出力がイマイチ」に見えるものが、
  実際にはAIの外側の設計だったという型。
  **同じ実感から出た補記側の3件（ANNOT-1〜3）は 2026-08-09 にクローズした** —
  1本の因果（出力枠のゼロサム）だと分かり、機能ごと作り直して5巡の実機確認を終えている。
  **DISTILL-1 はさらにその先で、AIへ渡す前の候補選定（純関数）に原因があった** —
  AIは与えられた候補から選んだだけで、**渡す候補にリンク行が混ざる仕組みのほうが問題だった。**

- **IMG-2** は画像表示（N-3）の外部レビュー2巡目で見つかった。1巡目の指摘7件へ修正を入れたが、
  **3件は受理条件まで満たしていなかった**（1件は部分解消、2件は目的未達）。
  同時に見つかった IMG-1（寸法未確定の画像がある間の進捗水増し）は 2026-08-08 にクローズ済み
  → [features/note_image_rendering.md](../dev/features/note_image_rendering.md) §6.1。
- **TRACE-3・AI-3 は、2026-08-01 の総評（No.9）で指摘されながら起票されていなかった。**
  総評を文書へ追記した時点で台帳へ落とす運用（[README](../dev/document_map.md) §4 の2）が働かず、
  **2日間どこにも追跡されていなかった。** コードを当たって未対応であることを確認済み。

**「テストが緑」は受理条件を満たしたことを意味しない。** IMG-2 はいずれも
JVMテストが全件成功する状態で残っている。純関数の値伝播と単独部品は覆えても、
**Composeのスクロール状態との交差・索引の時間経過**を通していないため。

---


## TRACE-3. 連続削除が同じJobを奪い合う

- **現状:** [`ReadingTraceCleanupController`](../../app/src/main/java/com/example/newproject/controller/ReadingTraceCleanupController.kt#L54) は
  洗い出しと削除に**同じ `job`** を使い、削除のたびに前のJobをキャンセルする。
- **問題:** SAFの `deleteDocument()` は同期的な外部I/Oなので、**キャンセル時点で物理削除だけ完了し、
  その後の状態更新が落ちる**ことがある。さらに各削除は開始時の `current` を捕捉するため、
  AとBを素早く押すと **AのJSONは消えたのに画面に残り**、B完了時には古い一覧を基にBだけを外す。
- **自己修復はするが誤解を与える:** 再洗い出しで整合するものの、その前は「削除に失敗した」ように見える。
- **対応候補:** 洗い出し用のJobと削除を分け、削除は `Mutex` か単一キューで直列化する。
  実行中は同じ候補のボタンを無効化し、完了時は**捕捉した `current` ではなく最新状態から**対象を除く。
  連続2件・洗い出し中の削除・Vault切替中の削除をテストする。
- **規模感:** 小〜中。

## IMG-2. 索引ヒット時に画像の世代が更新されず、古い画像が固定される

- **現状:** 復号キャッシュの鍵へ更新日時（`contentVersion`）を足したが、**その値を取り直す契機が無い。**
  [`VaultImageIndexStore.resolve()`](../../app/src/main/java/com/example/newproject/data/VaultImageIndexStore.kt#L80) は
  `if (!first.isMiss() || now() - loadedAt < ttlMillis) return first` で、**解決に成功したら TTL を見ずに返す。**
  索引の作り直しは「解決に失敗し、かつ索引が古いとき」だけなので、**ヒットし続ける限り世代は初回の値のまま。**
- **成立する順序:** ①初回走査で画像Aの参照と `lastModified = 1` を索引へ保存しBitmapをキャッシュ
  ②Obsidian側で同じ画像Aを上書きし、参照は同じまま `lastModified = 2` になる
  ③TTLを超えて再表示しても古い索引で解決自体は成功する ④再走査されず `contentVersion = 1` のまま
  ⑤鍵が変わらず**古いBitmapを返す。**
- **削除＋同名再作成でも同じ:** 参照が変わっても、古い参照が索引上「ヒット」である限り再走査されず、
  **壊れた表示が固定され得る。**
- **影響:** アプリ再起動またはVault世代の変化まで、更新前の画像か読めない古い参照を表示し続ける。
- **対応候補:** 全Vault走査を毎回行う必要はない。`load()` の直前に解決済みドキュメントの
  `COLUMN_LAST_MODIFIED` を個別照会して鍵に使うか、TTL超過後の最初の画像解決で索引を1回だけ更新する。
  どちらでも**miss再走査の連打防止**と、`lastModified` を返さないプロバイダでの既知制約を明記する。
- **受理条件:** 同じ参照の更新日時が変われば同じ表示幅でも復号し直す／TTL超過後のヒットでも新しい世代を取れる／
  削除・同名再作成で古い参照へ固定されない／壊れたリンクで再コンポーズごとに全走査しない／
  Fake時計と可変の走査結果で「初回load → 外部更新 → 再load」を通す統合テストがある。
- **規模感:** 小〜中。

## AI-3. 完成プロンプトにハード上限が無い

- **現状:** 本文の抜粋には用途別の上限があるが、**完成プロンプト全体を閉じる制約が無い。**
  - [`SectionChatController.sendMessage()`](../../app/src/main/java/com/example/newproject/controller/SectionChatController.kt#L95) は
    会話履歴を**全件**渡し、ユーザーの質問にも長さ上限が無い
  - [`renderCandidatesWithinBudget()`](../../app/src/main/java/com/example/newproject/domain/RelatedCandidateContext.kt#L63) の
    最終フォールバックはIDとタイトルを全件残し、**`fits()` を再確認しない。**
    長いタイトル群だけで候補予算を超える場合がある
- **認識はされている:** `PromptTokenBudgetTest` 自身が「定義済み計測プロファイルの回帰であって
  入力長の上限保証ではない」と明記している。**本番側の閉じた制約になっていないだけ。**
- **対応候補:** 完成プロンプト単位の入力予算を1箇所で強制する。履歴は直近N件か文字予算で切る。
  関連候補はタイトルのみでも超える場合に候補数か表示長を減らし、**最終返却前の `fits()` を必須にする。**
- **規模感:** 中。

## AI-6. プロンプトの字下げが全行に残っている

**2026-08-09、ひとことの実装中に実測で見つかった。** テストが緑のまま本番で起きている。

- **現状:** `trimIndent()` は**補間後の文字列**に効く。複数行の値を埋めると
  その2行目以降がインデント0の行として混ざり、**共通インデントが0と判定されて
  テンプレート側の字下げ（12スペース）が全行に残る。**
- **実測**（`buildRelatedNotesPrompt`・候補2件）:

  ```
  [            You are a note-taking assistant. Find the notes most related ...]
  [            C01 | 候補A]
  [C02 | 候補B]
  ```

- **本文抜粋も複数行になるので、実運用ではほぼ常に発生している。**
  関連ノートと蒸留のプロンプトが現在この状態。
- **影響:** ①1行あたり12文字ぶんの入力トークンを無駄に払う
  ②整形が崩れ、モデルから見た構造が読み取りにくい
  ③`PromptTokenBudgetTest` の計測値にこの余白が含まれており、**予算判断の基準線が汚れている**。
- **なぜテストで気づけなかったか:** 既存の回帰テストは抜粋も候補も**単一行**の値で比較していた。
  複数行のときだけ壊れるので、緑のまま通り抜けた。
  **「テストが緑」は受理条件を満たしたことを意味しない**の実例がもう1件増えた形。
- **対応候補:** 静的な部分だけを `trimIndent()` して連結する
  （ひとこと（`buildRemarkPrompt`）は既にこの形にしてある）。
  直すと計測の基準線が動くので、**基準線の取り直しとセット**にする。
- **受理条件:** 複数行の抜粋・複数件の候補を渡したとき、どの行にも余分な先頭空白が無い／
  複数行の値で回帰テストを固定する（単一行では検出できない）。
- **規模感:** 小。ただし instrumentation の基準線を取り直す。

## TRACE-4. ユーザーが書いた返事に退避手段が無い

**2026-08-09、ひとことに「返事」を入れたことで性質が変わった。**

- **現状:** 痕跡サイドカー（`_ReadingTraces/*.json`）に**ユーザー自身が書いた文章**が入るようになった。
  それまでの中身は訪問履歴とAI生成の要約だけで、失っても読み直せば再生成できた。
  **返事は再生成できない。**
- **退避手段が1つも無い。** Vaultのフォルダが消える・別端末へ移る・再インストールで、
  訪問履歴・AI俯瞰要約・ひとこと・**返事**がまとめて失われる。
- **設計上、予見されていた。** `feature_ideas` N-11 は
  「余白メモが入ると痕跡は『アプリが作った記録』から『ユーザーが書いたもの』に変わり、
  失われたときの損失が跳ね上がる。**N-6 を入れるなら本項は同時か直後に要る**」と書いている。
  返事は N-6（余白メモ）そのものではないが、**ユーザーの言葉を受け取る口**という点で同じ性質。
- **足場はある:** 整合性検査と孤児掃除は `ReadingTraceCleanupController`、
  読み書きは `ReadingTraceJson`（スキーマ版・バイト上限つき）として既存。
  SAF でユーザーに保存先を選ばせる口も `data` 配下にある。**新規は書き出し／読み込みの経路だけ。**
- **対応候補:** サイドカーを1ファイルへ書き出し、別端末や再インストール後に読み戻す。
  **インポートは既存の痕跡を壊し得る**ので、同じノートの痕跡が両方にあるときの
  上書き／統合の規則を先に決める。書き出したファイルには読書履歴が平文で入るため、
  どこへ出すかはユーザーが都度選ぶ形にする。
- **退避形式は版を持たせる。** 現行は schema v5 で、今後も拡張される。
- **2026-08-09 に対象が増えた。** 返事に加えて「映し返し」（返事を受けたAIの1文）も入り、
  1組の対話がまるごとサイドカーにある。失われたときの損失はさらに上がった。
- **保存失敗時の退避はプロセスメモリにしか無い。** アプリが落ちれば書けなかった返事は消える。
  「必ず残る」と言うには**アプリ内部の回復ファイル**が要る（蒸留が復旧レコードで同じことをしている）。
  現状は「プロセスが生きている間は必ず書き直す」まで。**本項とは別の穴なので、
  退避・復元を作るときに一緒に見る。**
- **規模感:** 小〜中。

## DISTILL-1. 蒸留候補に wikilink だけの行が上がる

**出発点は「蒸留のレコメンドがイマイチ。`[[リンク情報]]` が候補に上がってきたが重要ではない」という実感。**
コードを当てたところ、**単一の原因ではなく3層が重なって、リンク行が構造的に有利になっていた。**

- **① リンクを候補から外す規則が、そもそも存在しない。**
  リンク範囲（`linkSpans`）は計算済みだが、使われているのは
  [「文の境界がリンクの内側に落ちたら、その文を捨てる」](../../app/src/main/java/com/example/newproject/domain/DistillSourceModel.kt#L140)
  という**分割位置の保護だけ**である。
  しかも [`DistillTextRange.contains`](../../app/src/main/java/com/example/newproject/model/DistillModels.kt#L18) は
  `offset > start && offset < endExclusive` と**両端を含まない**ため、
  **`[[リンク情報]]` のように文とリンクが完全一致する場合は、両端が一致して判定が false になり素通りする。**
  行がまるごとリンクでも、ふつうの文として候補に入る。
- **② 構造ボーナスが、リンク集の置き場所と一致している。**
  [`structuralWeight`](../../app/src/main/java/com/example/newproject/domain/DistillCandidateScoring.kt#L68) は
  `isParagraphFirst`（+0.3）・`isChunkLast || isNoteLast`（+0.4）・`isHeadingAdjacent`（+0.2）を加点する。
  **Obsidian のノートは「関連」「リンク」を末尾の見出し直下に箇条書きで置く慣習がある**ので、
  リンク行はこの3つを同時に取りやすく、**最大 +0.9** になる。
  短文ペナルティは **-0.25 だけ**（15文字未満）なので、まったく釣り合わない。
- **③ チャンクごとに必ず1件が選ばれる（これが決定打）。**
  `leaders = scored.groupBy { chunkIndex }.values.map { それぞれの最高点 }` の形なので、
  **各チャンクから最低1件が候補に入ることが保証されている。**
  「リンク」見出しのセクションは独立したチャンクになり、**中にリンク行しか無ければ、
  それがどれだけ低スコアでも、そのチャンクの代表として必ず候補になる。**
  KDoc の「全文の各所を残しつつ」という意図どおりの動作であり、**バグではなく設計の副作用。**
- **④ 補助的に、タイトル類似でも有利になりやすい。** スコアの主項は
  `TITLE_WEIGHT * dice(文, ノートタイトル)` で、**wikilink の宛先名は関連ノート名なので
  タイトルと文字が重なりやすい。** `[[` `]]` ごと bigram にしている点も雑音になる。

- **影響:** `MAX_AI_CANDIDATES = 24` / `FINAL_SELECTION_LIMIT = 6` と枠が小さいので、
  **リンク行が入るぶんだけ本来の文が押し出される。** AIが選べば太字になるが、
  **リンクは既にリンクであって、太字にしても読み返しの手がかりにならない。**

- **対応候補（①と③を直すのが本筋。②は触らない）:**
  - **①: 文に占めるリンクの割合で候補から外す。** `linkSpans` は既にあるので、
    文の文字数のうちリンクが占める比率が高いもの（例: 過半）を候補から落とす。
    **完全一致だけを弾く形にすると `- [[A]] と [[B]]` のような行が残る**ので、比率で見る。
  - **③: チャンク代表に下限を設ける。** ただし「全文の各所を残す」意図と正面から衝突するので、
    **一律の閾値ではなく「①で全滅したチャンクは代表を出さない」**とするのが安全。
    リンクだけのセクションは代表を持たない、という形になる。
  - **②の構造ボーナスは変えない。** 末尾・見出し直下・段落先頭が重要なのは一般には正しく、
    リンク集がそこに居るのが問題なので、**リンク側で解くほうが副作用が小さい。**
- **受理条件:** 行がまるごと `[[...]]` の文が候補に出ない／リンクだけのセクションが
  チャンク代表として候補に出ない／**通常の文の中に含まれるリンクは候補から落ちない**
  （リンクを含む普通の文まで消さない）／上記3つを固定する回帰テストがある。
- **規模感:** 小。純関数（`selectDistillCandidates` と文生成）とテストのみ。
  **プロンプトにもUIにも触らない。**

## AI-4. AIピッカーだけID契約から外れている

**同じファイルの中に、なぜIDが良いかまで書いてある。**
[`PromptBuilder.kt:82`](../../app/src/main/java/com/example/newproject/ai/PromptBuilder.kt#L82) のコメント —
「候補は『ID | タイトル』で提示し、モデルにはIDだけ返させる。ID→ノートの解決は UseCase側で確実に行うため、
**言い換え・翻訳・装飾・同名衝突に強い**」。

- **3経路のうち2経路はこの契約で守られている。** 蒸留は `S001` 形式を `validIds` と照合、
  関連ノートは `C01` 形式を [`parseCandidateIds`](../../app/src/main/java/com/example/newproject/domain/RelatedCandidateId.kt#L22) で照合し、
  **候補外のIDを破棄する。**
- **ピッカーだけ生のタイトル文字列を返させている。**
  [`buildPickerPrompt`](../../app/src/main/java/com/example/newproject/ai/PromptBuilder.kt#L151) は "Return only note titles"、
  受け側は [`notesByTitle[title.toNormalizedObsidianTitle()]`](../../app/src/main/java/com/example/newproject/domain/SearchPickerUseCase.kt#L49) で照合する。
- **影響:** AIがタイトルを言い換える・記号を足す・翻訳すると、`mapNotNull` が**黙って落とす**。
  エラーにならないので、**「3件選ばせたのに1件しか出ない」が原因不明のまま起きる。**
  正規化が吸収する範囲を超えた瞬間に結果が減る。
- **これは横展開の取り残しの型。** 方式もその理由も既に書かれていて、適用が1経路だけ抜けている。
  → [lessons.md](../dev/lessons.md) L14
- **対応候補:** ピッカーも「ID | タイトル」で提示してIDだけ返させ、`validIds` と照合する。
  `parseCandidateIds` がほぼそのまま使える。
- **受理条件:** AIが装飾・言い換えたタイトルを返しても結果件数が減らない／候補外のIDは破棄される／
  タイトルに記号や同名が含まれる場合の回帰テストがある。
- **規模感:** 小。プロンプト整形とパーサの差し替えで、既存部品を流用できる。

## AI-5. 関連ノートが算出した根拠を捨てている

- **現状:** [`relatedContextScore`](../../app/src/main/java/com/example/newproject/domain/RelatedContextScoring.kt#L44) は
  **タグ一致・本文スニペット類似・タイトル類似を個別に計算**しているが、
  `CandidateScore(score, tieBreak)` という **Double 2つへ畳んで**返す。
  畳んだ後は並べ替えにしか使われず、[`RelatedTab`](../../app/src/main/java/com/example/newproject/ui/screen/RelatedTab.kt#L240) が
  表示するのは `isWikilinked` だけ。
- **影響:** 「なぜこのノートが関連なのか」が画面から分からない。
  **根拠は計算済みなのに捨てているので、表示するのに新しい計算もAI生成も要らない。**
- **不具合ではなく強化。** ただし A-3（ピッカーに1行要約を添える）が Nano 生成を要するのに対し、
  **こちらは追加コストがゼロ**なので、納得感を上げる手段として先に検討する価値がある。
- **対応候補:** `CandidateScore` に内訳を持たせるか、上位候補についてだけ主シグナル
  （タグ一致なら一致したタグ名）を返す。**畳む前の値を1段だけ上へ通す。**
- **懸念:** 内訳を出すと「スコアが見える」ことになり、順位への納得と不満の両方を生む。
  **数値ではなく「タグが一致」のような質的な表示に留める**のが安全。
- **規模感:** 小。

## AI-2. AI状態の一時エラーと非対応を同一視している

- **現状:** [`checkAvailability()` 自体の例外を `Unavailable` にまとめる](../../app/src/main/java/com/example/newproject/ai/AICoreClient.kt#L67)ため、
  非対応端末と一時的な状態取得失敗を区別できない。
- **対応候補:** AI非対応・モデル未準備・一時エラーのUXを要約・検索・クイズ・補記で統一する。
  **ReadingTrace は横展開の対象外**（意識させない機能なので、失敗は黙って劣化させるのが仕様）。
  → [lessons.md](../dev/lessons.md) L4
- **規模感:** 中。

## REL-1. R8とリリース署名が未設定

- **現状:** `buildTypes { release { } }` と `proguard-rules.pro` は置いたが、
  **`isMinifyEnabled = false`／`signingConfig` 無し**。`assembleRelease` の生成物は
  依然として `app-release-unsigned.apk`（約10MB）で、配布可能性を意味しない。
- **後回しにした理由:** ML Kit GenAI はリフレクションで解決される部分があり、縮小で消えると
  **全AI機能が実機の release ビルドでだけ落ちる**。JVMテストは縮小前のクラスを見るので検出できない。
  Compose の Composable メタデータも同様。つまりR8の有効化は「設定を1行変える作業」ではなく
  **実機検証の計画とセット**になる。
- **対応候補:** keep ルールを書いた上で `isMinifyEnabled = true` にし、release ビルドを実機へ入れて
  7つのAI経路（要約・補記・クイズ・関連ノート・セクションチャット・蒸留・読書痕跡の要約）を一巡する。
  署名は keystore の保管方針を決めてから。
- **規模感:** 中。実機確認が必須。

## MAINT-1. 依存が12件古い（ML Kit GenAI の調査は完了。上げない判断）

**優先度は超低。** 2026-08-01 に最大の未知だった `genai-prompt` beta4 を調査し切り、
**「上げない」と判断した**（下記）。残っているのは「古い」という状態だけで、**現時点で実害は無い。**
**着手の契機は「新版に必要な機能が入ったとき」であって、新版が出たことではない。**

- **方針は 2026-08-01 に確定した** → [system/dependency_policy.md](../dev/system/dependency_policy.md)。
  更新の単位（7グループ）・棚卸しの手順と契機・各グループの確認範囲を決めてある。
  3チェックは `disable` から **`informational`（hint）へ変えた** — 素のまま有効化すると12件が
  Error になり `lintDebug` タスクが失敗するが、`informational` なら
  **「0 errors, 0 warnings, 12 hints」で成功し、12件はレポートに残る**。
  ゲートに載せずに催促だけ残せる。
- **残っている問題:** 方針ができても**依存は12件古いまま**で、1件も上げていない。距離の内訳は
  [dependency_policy.md](../dev/system/dependency_policy.md) §4 のスナップショットにある。
  優先して見るべきは2つ。
  - **`com.google.mlkit:genai-prompt` 1.0.0-beta2 → beta4 は当面着手しない（2026-08-01 決定）**
    → [system/dependency_policy.md](../dev/system/dependency_policy.md) §5。公開APIの削除はゼロでソース互換だが、
    **`maxOutputTokens` の未指定時の既定値が 256 → 4096 へ変わる。**
    実測の `getTokenLimit()` が 4,352 なので入力に許されるのは256トークンだけになり、
    **最小の読書痕跡要約（349）すら通らない。** つまり**そのまま上げると8経路すべてが止まる。**
    上げるなら `maxOutputTokens` の明示設定が**前提条件**で、値の決定・AI 8経路の実機一巡・
    基準線の再取得がセットになる。**新版が出たことは上げる理由にならない**ので、
    beta4 の新機能（構造化出力・`SystemInstruction`）を実際に使いたくなるまで動かさない。
  - **Compose BOM（2024.09.03）はLintの指摘に出てこない。** `platform()` 宣言が更新対象として
    見られていないためで、最新である意味ではない。**棚卸しの自動検出から漏れる唯一の枠**なので手で確認する。
- **規模感:** 調査は小。更新の実行はグループごとに小〜中で、いずれも実機確認を伴う。


## PERF-1. 読書痕跡の索引作成がファイル数に比例する

**実測で問題化するまで着手しない。** 先回りで入れると移行リスクだけを先払いになる。

- **現状:** `_ReadingTraces/` はフラットな1階層で、セッション中1回だけ子一覧を全列挙して
  key→参照の索引を作る。容量は問題にならない（1ファイル最大3.3KB・2,000ノートで約1.4MB）が、
  **列挙コストはファイル数に比例**する。孤児削除（手動）は入ったが**押されるとは限らない**ため、
  ファイル数は単調増加**しうる**（→ [reflect_reading_trace](../dev/features/reflect_reading_trace.md) の「性能はファイル数ではなく索引設計で解く」）。
  遠いプロバイダ（Google Drive 等）ほど効く。
- **対応候補:** 削除ではなく**分割**で解く。ファイル名が `sha256Hex(相対パス)` なので、
  **先頭1桁で16フォルダへ分ける**と1件の照合で列挙する件数が 1/16 になる。
  引く側はキーから所属フォルダを計算できるので探索は要らない。
  **生きた痕跡を1つも消さずに性能だけ守れる**のが孤児削除に対する優位点。
  → [reflect_reading_trace](../dev/features/reflect_reading_trace.md) の「性能はファイル数ではなく索引設計で解く」
- **着手の契機:** 実機で列挙の遅さが体感できたとき。既存ファイルの移行を伴うので、それまで動かさない。
- **規模感:** 中（移行と実機確認を伴う）。

## MAINT-2. `GradientHeader` の全幅化が呼び出し側との手動契約になっている

**単独では着手しない。** 該当ファイルを別の理由で触るときに、ついでに直す。

- [`GradientHeader`](../../app/src/main/java/com/example/newproject/ui/component/GradientHeader.kt#L50) の
  `horizontalBleed = 20.dp` は、呼び出し側の水平余白と一致している前提で成立する。現在の画面では
  合っているが、**余白を変えた瞬間に見出しだけがずれる**。レイアウトテストは無い。
  配色は「テストで強制する」ところまで持っていったのに対し、寸法の対応関係は約束のままである。
- **規模感:** 極小。
