# 変更履歴表

**プロジェクト:** Vigilith AI（旧 Obsidian Mind）

このファイルはPR単位の変更履歴を新しい順に記録する累積文書である。

- **何を**いつ変えたかはこの表、**今どうなって**いるかは [source_code_analysis.md](source_code_analysis.md)、**なぜそう**したかは [design/](design/) を参照する。
- 運用ルール: PRごとに1行追記する。設計判断・試行錯誤があった変更のみ design/ に対応ファイルを作成または追記し、この表からリンクする。

---

## 履歴（新しい順）

| 日付 | PR | 変更内容 | 設計メモ |
|------|----|----------|----------|
| 2026-08-01 | — | **プロンプトの実トークン余裕を測る計測器を入れ、`genai-prompt` beta4 をAARで調査した（AI-1 の前提を解消・MAINT-1 の未確認を解消）**。AI-1（関連ノートは予算600のうち226文字を注意書きに払う）は「実トークン計測が前提」で止まっており、**その計測手段の存在すら未確認**だった。AARを展開したところ **`countTokens()` / `getTokenLimit()` は現行 beta2 に既に存在**し、SDK更新は前提ではなかった。**設計上いちばん危なかったのは判定式**で、`countTokens()` が数えるのは入力だけ、`getTokenLimit()` は**入出力の合計上限**なので、`inputTokens < tokenLimit` で見ると出力予約を二重に使えると誤読する（症状は「入りきらない」ではなく**応答が途中で切れる**側に出るため原因に辿り着きにくい）。`headroom = 上限 − 入力 − 出力予約` で判定する形にした。`maxOutputTokens` は定数を書かず**SDKが埋めた実値をリクエストから読む**ので、SDK版を上げれば自動追随する。**注意書きのコストは単体計測ではなく「あり − なし」の差分で測る**（結合でトークン境界が変わるため単体値は完成プロンプトへの寄与と一致しない）。計測ケースは**12**（機能経路8＋セクション3種・クイズ3形式への分解）で、コード上に3通り混在していた経路の数え方を固定し、**`dependency_policy` の「AI 7経路」が実際にAIを呼ぶ検索ピッカーを数え落としていた**ので8経路へ訂正。**skip 判定は生の `FeatureStatus` のみで行う** — `checkAvailability()` は例外まで `Unavailable` へ畳むため、これで判定すると**SDKの回帰が「非対応端末」に化けて見逃される**（計測呼び出しの例外は skip せず失敗させる）。**これは定義済み計測プロファイルの回帰テストであって入力長の上限保証ではない**点を設計書とKDocの両方に明記した（チャット履歴・候補タイトル・クエリには本番側の上限が無く、関連候補はタイトルだけで予算超過すると収まらないまま返る）。生成と計測が同一リクエストを使うよう `buildRequest()` を切り出し、あわせて `maxOutputTokens` の既定値に関する**誤ったコメントを訂正**（beta2 の既定値はちょうど256で、未指定と256明示は同義だった）。**beta4 調査の結論は「ソース互換だが動作互換ではない」** — 公開APIの削除はゼロで現行の呼び出しは全て健在だが、**未指定時の既定値が 256 → 4096 へ変わる**ため、依存の1行を上げるだけで応答長・推論時間・60秒タイムアウトの余裕・`MAX_TOKENS` の発生頻度・生成Mutexの占有時間がまとめて動く。許容範囲の文字列だけを見て既定値の変化を取り落とさないよう、確認手順も残した。beta4 の追加（`SystemInstruction`・構造化出力・thinking mode・マルチターン `Content`）はいずれも独立した作業が要り依存更新だけでは回収できないため、判断4に沿って**今回は上げず理由を記録**した。予算値は1つも変えていない。JVMテスト508件（変化なし・計測器はandroidTest側）、Lint 0 error / 0 warning / 12 hint、`assembleDebugAndroidTest` 成功。**実機での計測は未実施**（数値が出たら AI-1 の判断へ進む） | **[ai_input_excerpt](design/ai_input_excerpt.md) §13**・**[dependency_policy](design/dependency_policy.md) §5** |
| 2026-08-01 | — | **VERIFY-2（C案の実機確認2件）を検証不要と判断し、課題台帳から削除した**。運用実態として1MB超の巨大ノートを含むVaultが存在しないため、切り詰め文言の表示確認・関連ノート生成速度の確認のどちらも対象ケースが発生しない。実機での動作未確認という点は変わらないが、発生し得ない条件の検証を台帳に残す意味がないと判断した。**コード変更なし** | — |
| 2026-08-01 | — | **`VaultBrowser` 移行（N-7 段階7）の実機確認が完了した（VERIFY-4クローズ）**。さがすタブでのフォルダ列挙・キーワード検索・ランダム、Vault切替時のchipsと検索結果の入れ替わり、補記の生成・一覧・個別削除・一括削除、Vault未選択時の「Vault が選択されていません。」表示を一巡し、問題なし。`ContentResolver` と Vault ルートを `VaultBrowser` の裏へ束ねた変更（世代照合・ハンドル取得契約を含む）が実機のSAF経路でも移行前と変わらず動くことを確認した。これで N-7 は設計・実装・JVMテスト・実機確認のすべてが揃い、TEST-2（実端末を通す統合テストの中身が無い）の着手前提2つがどちらも満たされた状態になった。課題台帳から VERIFY-4 を削除した。**コード変更なし** | **[saf_boundary_gateway](design/saf_boundary_gateway.md)** |
| 2026-08-01 | — | **さがす／補記の Vault と `ContentResolver` を `VaultBrowser` へ束ね、世代照合をJVMテストで固定した（N-7 段階7）**。設計書の処方は「Vault の解決を repository 側へ束ねて controller の引数から消す」で、効果は依存の見た目が整うことだと考えていた。**着手前に測ったら、それだけでは新しく書けるテストが0件だと分かった。** Vault スコープの経路をJVMで検証できない壁は3つある — ①`vaultUri()` が非nullの `Uri` を要る（全テストが `{ null }` を渡していた）②`NoteRepository` が具象クラスで差し替え口が無い ③全公開メソッドが `ContentResolver` を取る（**どのテストも渡していない**）。**1つ外しても残り2つで詰まる。** 証拠はテスト側のコメントに残っていた — `SearchControllerTest` は「検索の実行と `loadFolders` の世代照合は検証できない…実機確認で担保する」、`NoteSessionCoordinatorTest` は `ContentResolver` を作れず **private フィールドへリフレクションで番兵を積んでいた**。3つを同時に外す形として `VaultBrowser`（未選択なら null）と `VaultHandle`（選択中Vaultに束縛された操作）を `data` へ置いた。**新しい発明ではない** — 蒸留と読書痕跡は既に `Saf*Gateway` が `ContentResolver` を構築時に束ねる形で、**7機能のうち2つだけ解けていて残りが取り残されていた**（テーマ8）。**「Vault未選択のとき何を出すか」は controller に残した** — さがすは黙って返り補記はエラーを出すので、下へ押し込むと差が消える。**ハンドルは開始時に1回だけ取る**（`ReadingTraceStore` と同じ規約。モデルDL完了後の再開だけは数分後になり得るので引き直す）。**JVMテスト +21件**（`SearchControllerTest` +11・`AnnotationControllerTest` +9・`NoteSessionCoordinatorTest` +1）（世代照合3種・検索とランダムの実行・走査キャッシュのヒットと破棄・削除失敗の件数・ハンドルを取り直さない契約）で、**5つのガードを1つずつ削る変異で対応テストが落ちることを確認済み**。走査キャッシュの破棄は番兵をやめて実際に走査させる形に置き換えた（Jobの停止だけは外から観測できないので番兵が残る）。副次的に、使われなくなった `vaultUri` パラメータと `ContentResolver` の import が Coordinator から落ち、**`controller` パッケージから `android.*` が完全に消えた**ので `PackageDependencyTest` の Android 非依存リストへ `controller` を加えた（`model`・`domain`・`controller` の3層。import 1行の注入で落ちることを確認済み）。`android.net.Uri` を import するファイルは 17→8 になり、**残る8は `data` 7・`NoteViewModel` 1 でいずれも境界そのもの**。教訓は lessons L19（設計書に処方だけ書くと効果の見積もりが検証されないまま残る。**効果の検算を1つ書く**）。テスト487→508件、Lint 0 error / 0 warning。**実機確認は未実施** | **[saf_boundary_gateway](design/saf_boundary_gateway.md)**・[architecture](design/architecture.md)・[lessons](lessons.md) L19 |
| 2026-08-01 | — | **ドキュメント参照を `DocumentRef` へ移し、`model` / `domain` / `ui` から Android を追い出した（N-7 段階1〜6）**。`model` の4型と `domain` の1ファイルが `android.net.Uri` を持っていたため、**`Uri` を含む型を素のJVMテストで組み立てられず**（`Uri` はユニットテストではスタブで例外を投げる）、検索実行や補記保存の世代照合は実機確認だけが担保だった。`NoteFile`・`RelatedNote`・`HistoryEntry`・`AnnotationState.savedUri` を不透明な `@JvmInline value class DocumentRef` へ置き換え、`Uri` との変換は `data/SafDocuments.kt` の2関数に閉じた。`import android.net.Uri` を持つファイルは **17→10**（`model` 4→0・`domain` 1→0・`ui` 1→0）。**段取りは設計書のとおりにならなかった** — 「最小の `HistoryEntry` から1型ずつ」という当初案は実行不可能で、3型を同時に移すしかなかった。`ui/SearchScreen` が `HistoryEntry` から `RelatedNote` を、`domain/RelatedNotesUseCase` が `NoteFile` から `RelatedNote` を組み立てるため、片方だけ移すと組み立て側に新旧の変換が要る。ところが `ui` と `domain` は依存規約上 `data` を import できず変換関数に手が届かない。**「変換を `data` に閉じる」という判断そのものが部分移行を禁じていた**（教訓は lessons L18 — 移行の単位はサイズではなく「同じ変換関数に手が届く範囲」で切る）。**副次的に往復変換が2つ消えた** — `SavedAnnotation` は gateway が返す不透明文字列を一度 `Uri` へ戻していた、`reloadNoteBody` は `Uri.toString()` 由来の `targetUri` を `Uri` へ戻していた。どちらもそのまま参照へ包む形にした。**完了条件として `PackageDependencyTest` に「`model` と `domain` は Android に依存しない」を追加**。従来の依存テストは `com.example.newproject.*` の import しか見ておらず **`android.*` を数えていなかったのが穴の正体**で、`ui` を対象にしないのは Compose 自体が `androidx.*` だから。`model`・`domain` それぞれへ import を1行注入して落ちることを確認済み。**payoff の実例**として、`NoteSessionCoordinatorTest` にあった `listOf("旧Vaultの履歴") as List<HistoryEntry>`（「`HistoryEntry` は `Uri` を要るので作れない」というコメント付きの強制キャスト）を実物の組み立てに直した — **この refactor が狙っていた痛みが、そのままコメントとして残っていた**。**永続化のスキーマは変えていない**（`NoteHistoryStore` のJSONキーは `"uri"` のまま。保存済みの文字列表現に型を付け直しただけ）。テスト486→487件、Lint 0 error / 0 warning。**2026-08-01 に実機確認済み**（Rediscover・さがす検索/ランダム・今日読んだノート・補記の生成/削除・関連ノートの一巡、問題報告なし。移行前に保存された履歴JSONも問題なく読めた＝互換の証明） | **[saf_boundary_gateway](design/saf_boundary_gateway.md)**・[lessons](lessons.md) L18 |
| 2026-08-01 | — | **N-7 の段階7（Vaultルートを controller の引数から外す）には意図的に進まない**。段階1〜6でドキュメント参照を `DocumentRef` へ移し実機確認まで終えたところで、残る段階7を評価し直した — `vaultUri: () -> Uri?` という素通しの引数を1段削るだけの整理で、型を変える話ではなく、L-3（実端末を通す統合テストの中身）着手の前提でもない。**作業スコープを広げずに TEST-2 本体へ進める状態が既に整っている**ため、ここで一区切りとした。設計書・ロードマップ・課題台帳の記述を「未着手」から「意図的に進まない」へ揃えた。必要になれば独立した改善として再検討する。コード変更なし | **[saf_boundary_gateway](design/saf_boundary_gateway.md)** |
| 2026-08-01 | — | **instrumentation の土台が動くことを Android 16 で実証した（TEST-1 クローズ）**。2026-07-31 の初回実行は 0 success / 2 failure（`NoSuchMethodException: `android.hardware.input.InputManager.getInstance []`）で、失敗はテスト本体へ入る前のアイドル待機初期化で起き、Contextを見るだけのテストにもクラス共通の `createComposeRule()` が効いていたため2件とも巻き添えになっていた。`6f21419` の対策（Context確認とCompose描画のクラス分離＋`espresso-core` 3.6.1→3.7.0・`ext:junit` 1.2.1→1.3.0 の限定更新）を入れた状態で **2026-08-01 に `connectedDebugAndroidTest` を再実行し 2/2 成功**（Pixel_10_Pro_Fold AVD・Android 16 / API 36。`ComposeRenderingSetupTest` 3.9秒・`InstrumentationSetupTest` 0.001秒）。**クラス分離と依存更新を1コミットで同時に入れたため、どちらが効いたかは切り分けていない** — 実害が消えたので追わない判断とした（必要になるのは再発時）。これで「Runnerが起動しComposeを描画できる」が初めて実証され、以後の instrumentation の失敗は**土台の故障とテスト対象の故障を切り分けられる**。**CIは `assembleDebugAndroidTest`（組み立てまで）なので、この種の失敗は今もCIを素通りする**点は変わらない — 常時保証したいならエミュレータを回すジョブが要るが、現時点では入れていない。課題台帳から TEST-1 を削除し、ロードマップの N-6 も閉じた（L-3 の前提として残るのは `Uri` の gateway 化のみ）。**コード変更なし** | — |
| 2026-08-01 | — | **SAF境界を gateway の裏へ入れる設計を確定した（実装は未着手）**。2026-07-27 の `PackageDependencyTest` は `com.example.newproject.*` の import しか見ておらず **`android.*` を数えていなかった**。そのため `model` は「プロジェクト内の何も import しない」を満たしながら `android.net.Uri` だけは import する状態で残り（4型）、`domain` にも1ファイル残っている。`Uri` は素のJVMでスタブが例外を投げるため、これらの型を組み立てられず**検索実行・補記保存の世代照合は実機確認だけが担保**になっていた。**葉であることを「向き」だけで定義したのが穴**で、テスト容易性の観点ではプロジェクト内依存も外部フレームワーク依存も等しくその層をJVMで扱えなくする。設計の要点は**`Uri` が2種類あり直し方が違う**こと — Vaultルート（controllerが解釈せず repository へ素通しするだけ）は**不透明化しても何も良くならない**ので controller の引数から消す側、ドキュメント参照（`distinctBy`・`!=`・`in` でキーとしてのみ使う）は `model` の `@JvmInline value class DocumentRef` にする側。先行2 gateway は生の `String` だが、今回は `model` のフィールドになるため `title` との取り違えを防ぐ型を付ける。**永続化のスキーマ変更は不要**（`NoteHistoryStore` も蒸留も既に `uri.toString()` で保存済み＝既にある文字列表現へ型を付け直すだけ）。**完了条件は `PackageDependencyTest` へ `model` / `domain` の `android.*` 禁止を足すこと** — 「追い出した」は主張だが「戻ってこない」はテストにしかできない。段取りは**葉から7段階**で、2段目に `HistoryEntry`（2フィールド・消費2箇所）を置き、**最小の型で効くことを確かめてから `NoteFile` 本体へ入る** | **[saf_boundary_gateway](design/saf_boundary_gateway.md)**・[architecture](design/architecture.md) |
| 2026-08-01 | — | **依存更新の方針を確定し、Lint更新系3チェックを `disable` から `informational`（hint）へ変えた**。`NewerVersionAvailable`・`GradleDependency`・`AndroidGradlePluginVersion` は方針が決まったら `disable` から外すつもりだったが、**素のまま有効化すると12件すべてが Error になり `lintDebug` タスクが失敗する**（`warningsAsErrors` ＋ `abortOnError` のため。落ちるのは Lint タスクとCIジョブで、APKのコンパイルではない）。更新系は他のLint警告と種類が違い、**自分のコードを1行も触っていないのに上流が新版を出すだけで指摘が生える** — ゲートに載せると赤の意味が薄まり、「依存を一括更新しない。機能単位で上げ、実機確認を伴う」とも衝突する。**当初はここで「`disable` を維持するしかない」と結論したが、これは誤りだった** — Lint の severity は `error`/`warning`/`informational`/`ignore` の4段階で、`informational` は `warningsAsErrors` の昇格対象にならない。実測すると**「0 errors, 0 warnings, 12 hints」で BUILD SUCCESSFUL**、12件はレポートに残る（`--offline` でも同じ）。**「警告0」を保ったまま催促だけ残せる**ので `disable` より厳密に良く、一時有効化して戻す手順（戻し忘れでCIが赤になる）も不要になった。更新の単位を**7グループ**に分け、それぞれの上げ方と確認範囲を定義（ML Kit GenAI は必ず単独＋AI 7経路の実機一巡＋**上げる前にAARで制約確認**、Compose BOM は BOM ごと、AndroidX Test は本番非影響の唯一の枠、など）。棚卸しの**契機はカレンダーではなくマイルストーン**（この課題は既に一度「いつか決める」で放置されており、日付基準は守られなかったときに気づく仕組みが無い）。**初回棚卸しの結果を §4 に記録** — Gradle本体1件＋ライブラリ11件が古く、注目は `genai-prompt` beta2→beta4（アプリの心臓部・変更点未確認）と、`platform()` 宣言のため**Lintの自動検出から漏れる Compose BOM**。`NewerVersionAvailable` は毎回ネットワークへ問い合わせるためLintが概ね2倍遅くなるが、hint を残す価値を優先した。あわせて課題台帳の実態ずれを解消した（instrumentation の対策2件は `6f21419` で適用済みだったのに「対応候補」のまま／`build.gradle.kts` のコメントずれは既に解消済み）。**コード変更はLint設定1箇所のみ** | **[dependency_policy](design/dependency_policy.md)**・[lessons](lessons.md) L17 |
| 2026-07-31 | — | **ReadingTrace v1 を完了とし、残っていた実機確認4件をクローズした**。到達率・背面化復帰・Vault分離・検索フォールバックの4項目は、**いずれも対応するJVMテストが既にある**（設計書 §10）。実機でしか確かめられないのはロジックではなく境界だけ — 実 `ContentResolver` と実 Activity の `pause`/`resume` が Fake と同じ順序・粒度で振る舞うか、の一点に絞られる。加えて**再会カードは Rediscover 限定なので、痕跡を記録したあと結果を目視するには同じノートを 1/N の純粋ランダムで引き当てる必要があり**、数百ノートのVaultでは実質不可能だった。引きを操作できるようにするのは守るべき性質のほうを壊す。壊れた場合の検知は `_ReadingTraces` のファイル数とカード文面の違和感で事後に足りると判断し、**課題台帳から削除して通常運用へ戻した**。違和感が出たら症状つきで起票し直す（症状があれば再現条件を作れる）。判断の3条件と適用範囲は設計書 §12、教訓は lessons L16 | **[reflect_reading_trace](design/reflect_reading_trace.md)**・[lessons](lessons.md) |
| 2026-07-31 | — | **AI補記の書込失敗時に後始末し、消せなければ残骸を伝える（F-2の2件目）**。`createAnnotationFile()` は `createDocument()` の直後に対象URIへ書き込むだけで、ストリームを開けない・途中で例外になった場合の削除も保存後の検証も無かった。画面には生成失敗と出るのに `_AI補記` に残骸が残る。手順（作成→書込→検証→失敗時の後始末）を `AnnotationFileWriter` へ切り出し、SAFの4操作だけを `AnnotationDocumentGateway` の裏へ寄せて**Android非依存にしたので失敗注入テストが書ける**（読書痕跡の gateway と同じ形。参照は `Uri` ではなく不透明な文字列で扱う）。どの段階で失敗しても作成済みファイルの削除を試みる。**削除はSAFプロバイダ側の都合で失敗し得るので「必ず消える」とは言えない** — 消せなかった場合は失敗メッセージへ「不完全なファイルが残った可能性」を足して伝える（削除失敗で例外にはしない。元の失敗理由が後始末の失敗にすり替わるため）。**握りつぶさず、元の理由に足す**のが要点で、レビュー指摘（削除結果を捨てると利用者が残骸に気づけない）を受けて `delete()` は `Boolean` を返す形にした。**中断（`CancellationException`）でも作りかけを消してから再throwする。** 書込後は読み直して全文一致を確かめ、部分書込を失敗として扱う。**表示名は予測をやめ保存後のメタデータから取る** — 同じノートを同じ分に再生成するとプロバイダが改名し、`AnnotationState.Success.fileName` と実名がずれていた。書込失敗時の削除・読み直し検証・実名取得・中断時の削除・削除結果の無視・過剰通知の6変異で対応テストが落ちることを確認済み。テスト474→486、Lint 0 errors / 0 warnings。**2026-07-31 に実機確認済み**（問題報告なし） | — |
| 2026-07-31 | — | **蒸留の復旧チェックを追跡Jobへ載せる（F-2の1件目）**。`DistillController.checkRecovery()` だけが裸の `scope.launch` で、Jobも世代も持たなかった。復旧確認はSAF I/Oを伴うので遅れることがあり、その間にユーザーが蒸留を始めると「復旧警告を出す → 走行中の分析が候補を返して警告を上書きする」順序が成立していた（保存層が未解決レコードを再確認するので危険な書き込みは止まるが、データ安全性の警告が消えて不要なAI実行も走る）。専用の `recoveryJob` を持たせ、次の `checkRecovery()` が始まったら取り下げる。**分析の `activeRequestId` へ相乗りさせないのは寿命が違うため** — 復旧レコードはアプリ内部の未解決1件でノートにもVaultにも紐づかないので、ノート切替で打ち切ると警告が黙って消える（`cancelForNoteChange()` は触らない）。復旧結果の反映は `applyRecoveryResult()` へ集約し、**表示の直前に分析世代を無効化する**（先に表示すると直後の候補が同じ世代のまま上書きする）。3種の `RecoveryRequired` 構築を純関数へ寄せ、起動時確認と復元操作後の表示で共有した。世代照合は変異確認により冗長と判明したので削除。テスト471→474、Lint 0 errors / 0 warnings。**2026-07-31 に実機確認済み**（問題報告なし） | [architecture](design/architecture.md) |
| 2026-07-31 | — | **表示用Markdownの解析をMain外へ出し、通常表示と全画面で1回だけにする（F-1）**。`NoteReaderTab` と `FullscreenNoteScreen` がそれぞれの `remember` で `buildNoteSectionModel()` を同期実行しており、表示フォールバックは最大1MB・同じパーサのAI入力側実測は1MB約460ms／200KB約20msだったため、長文を開く瞬間と全画面へ入る瞬間にUIが止まり得た（Composableが別なので結果も共有されず二重解析）。`NoteSectionController` を新設して `Dispatchers.Default` で1回だけ解析し、`StateFlow<NoteSectionModel?>` を `MainActivity` から両画面へ配る構成にした。**状態を `NoteUiState` へ入れないのは、`model` パッケージが葉で `domain` を import できないため**（テーマと同じく独立フロー）。本文が変わる経路は `setNoteState` と `applyReloadedBody` の2つしかないので解析開始を Coordinator の同2箇所へ集約し、停止は `cancelNoteScopedJobs()` へ登録した。**解析中に本文を描かないガードが要点** — 描くと `MarkdownNoteContent` のフォールバックが働き最大1MBをMainで解析し直すため、タイトルと枠だけ先に出して本文の到着を待つ（プレースホルダ文言は数十文字なのでフォールバックのまま）。**requestIdガードは書いた上で変異確認により冗長と判明したので削除した**（`parse`／`cancelAndClear` がどちらも先にJobをキャンセルするため到達経路が無い）。解析の非同期化・切替時の破棄・蒸留差し替え時の再解析・描画ガードの4つは、1行ずつ消して対応テストが落ちることを確認済み。テスト459→471、Lint 0 errors / 0 warnings。**2026-07-31 に実機確認済み**（問題報告なし） | **[architecture](design/architecture.md)** |
| 2026-07-29〜30 | — | **ライト配色のAA是正とリリース構成の整備（D案・E案）。レビューで2度差し戻し、3度目で通した**。**①検証形式**: 当初は全色を最も明るい `panel #FDFEFF` の上で測っており、実際に文字が載る `panelBlue` では 4.19〜4.41、ボタンが載るグラデーション直上では 1.21〜1.84 と、いずれも基準を割ったまま「準拠」と判定していた。1度目の指摘で「トークン×実際に載る面」の対応表による総当たりへ作り直し、**2度目の指摘で「対応表に載せ忘れた経路」が残っていることが分かった**（グラデーション直上の白文字を最も有利な `Indigo` 停止色でだけ測っていた／`copy(alpha)` と `onVibrant` を覆えていない／停止色がテストと実装で二重管理）。停止色を `AppColorScheme` の単一ソースにし、代表を選ばず総当たりする形へ。**②文字色**: グレーを5→3段階へ統合し、基準面を弱い文字が載る最も暗い `panelChip #EEF0FF` に統一して `onSurfaceFaint #6D6D6D`・`onSurfaceMetaBlue #656C85`・`aiHeading #0C796C`・`relatedHeading #6054DE` へ再決定（見出し2色は11spで大文字例外に入らず、見分けの根拠が色相差しか無いため**3色とも色相を1度も動かしていない**）。**③グラデーション上の文字**: 白文字は Aqua 停止色で 2.07、濃い文字にすると Indigo 停止色で 2.62 と**文字色では解けない**。共通部品 `GradientHeader` が背景ごと持つ形にし、最初は `LogoNavy` α=0.42 の暗幕を敷いたが**実機で帯として重くカードにしか見えず差し戻し**、`Panel` α=0.35 の白ヘイズ＋`LogoNavy` タイトル＋`#202124` 副題へ反転（最悪 Indigo 上でタイトル6.15／副題5.12）。角丸をやめメイン領域の全幅へ抜き、フェード10dp。ダークは何も敷かず白文字のまま据え置くため**面と文字が明暗で反転する**ので、反転していること自体もテストで固定。**④帯の外**: 検索欄・フォルダチップ・空状態・進捗インジケータ・戻るボタン・linkedバッジ・ナビ未選択・字幕など12箇所を不透明面＋対の前景へ寄せ、`accentGlass` もライトを不透明化。**⑤禁止の強制**: `VibrantTextUsageTest` を新設し、画面からの `onVibrant` 直接使用と文字色への任意の `copy(alpha)` をソース走査で禁じる（許可は背景を所有する部品だけで、許可リストへ足したら対応する比を `AppColorContrastTest` へ足す決まり）。**⑥ボタン**: グラデーション直上では3役すべてが未達（Ai は Indigo 停止色と同色で 1.00）。全停止色に3:1を満たす塗りは L≤0.007 か L≥1.47 しか無く色では解けないため、WCAG 1.4.11 の隣接輪郭（`LogoNavy` 1dp）で解いた。パネル上のボタンには付けない。**⑦導線**: Vault未選択時に目立つピンクが機能しない「別のノートをひらく」に付いていたので役割を入れ替え、未選択時は無効化。空状態の本文パネルが `weight(1f)` で伸びて白が画面の大半を占めていたのを内容なりの高さへ。**⑧リリース構成**: `buildTypes`（R8は未有効・keepルール検証が要る）・`lint {}`・androidTest基盤とスモークテスト・Java 11・`applicationId` を `com.vigilith.ai` へ。**⑨警告ゲート**: `lint { warningsAsErrors }` は Android Lint にしか効かないという指摘を受け Kotlin の `allWarningsAsErrors` を追加。CIへ `assembleDebugAndroidTest` も追加。**⑩その他**: 補記のファイル名日時を `Locale.ROOT` へ固定（仏暦ロケールで `yyyy` が2569になり名前予測とずれる潜在バグ）、`Date()` の二重生成を1回に、旧OPの未参照ロゴ686KBを削除。テスト450→459。色・輪郭線・警告ゲートはいずれも旧値へ戻す変異で落ちることを確認済み。**2026-07-31 にエミュレータで5画面＋ダークモードを一巡し、その過程で暗幕→白ヘイズの反転と操作ボタンの配置是正を行った**（ダークは据え置きのとおり変化なし）。**E案も同日確認済み**で、`applicationId` の変更どおり別アプリとしてインストールされることを確認した（旧 `com.example.newproject` は端末に残るため、アンインストールが要る） | **[theme_and_ui_refactor](design/theme_and_ui_refactor.md)**・[dark_mode](design/dark_mode.md) |
| 2026-07-28 | — | **現行コードの品質を再評価**。`ac53749`を対象にJVMテスト450件全成功、Lint 0 errors / 21 warnings、release APK組み立て成功（unsigned・約11MB）を確認し、総合評価を7.2→8.0へ更新。前回の主要指摘だった非同期世代管理、依存・状態境界、用途別読込上限は解消済み。新たに、表示用Markdown解析のMain実行、蒸留復旧チェックの要求管理外、AI補記の書込失敗時に空・部分ファイルが残る可能性を課題登録した | — |
| 2026-07-27 | — | **AI入力の先頭固定長切り出しを、見出し骨格＋冒頭＋末尾の抜粋へ置換**。`PromptBuilder` 7経路から本文の `take()` を除去し、呼び出し側が用途別上限（要約/クイズ1200・補記/セクション1500・関連600文字）で `NoteExcerpt` を作る構成へ変更。予算内はMarkdownを解析せず従来プロンプトと1文字単位で同一、超過時だけlevel 3までの見出しを全体から均等選抜し、冒頭60%・末尾40%を明示ラベル付きで渡す。単一巨大ブロックも捨てずに両端を切り出し、コード断片は閉じフェンスを復元する。抜粋注意書きまで既存予算内へ予約し、フォーカスセクション推薦と予算値変更は別判断として見送った。**レビュー対応**: 最大1MBのMarkdown解析がMainを占有する問題を、5クラス・7経路の抜粋生成だけ `Dispatchers.Default` へ移して解消し、全呼び出しをソース走査テストで固定。正規化だけで全ブロックが収まる場合にも合う注意書きへ改め、本文との区切り改行1文字も共通prefixとして予算へ含めた。末尾追加・均等選抜・注意書き予約の3変異で対応テストが落ちることを確認。テスト425→450、Lint warning増加なし。抜粋の中身（捨てる場所・骨格・予算配分・正規化の副作用・モデルへの伝え方）は設計書1本へ独立させた。**2026-07-28 に実機確認完了**（[PR #43](https://github.com/miyakemasaki0808-MM/ObsidianMind/pull/43)）、問題報告なし | **[ai_input_excerpt](design/ai_input_excerpt.md)**（新規）・[architecture](design/architecture.md)・[related_notes_ai](design/related_notes_ai.md) |
| 2026-07-27 | — | **依存と状態の境界を型とCIで守る（B案・3段階）**。**①依存注入と調停分離**: `NoteViewModelDependencies` へ Repository・AiClient・UseCase・永続化・設定/履歴・`CoroutineScope` の組み立てを出し、Android境界だけを `NoteViewModel` に残した。7 Controllerの生成・状態所有・ノート/Vault切替はAndroid APIを呼ばない `NoteSessionCoordinator` へ移し、実物Controllerを束ねたJVMテストで一斉停止・一斉初期化・世代更新を固定した。当初の「ViewModel越し」は、素のJVMではAndroidスタブを生成できずRobolectric/モック依存が要るため「調停クラス越し」へ読み替えた。**②状態所有**: 状態型を `model/state/` へ分割し、`NoteUiStateStore` だけが全体Flowを所有。Controllerには機能別 `*StateWriter` だけを渡して担当外フィールドを書けなくした。テーマは独立Flowへ出し、ノート切替のリセットと`Loading`遷移は1回の更新へ原子化した。**③依存方向**: 共有データ型を葉の `model` へ移して4組の循環を解消し、`model`は依存なし／`ai→model`／`domain→model,ai`／`data→model,domain`／`controller→model,data,domain,ai`／`ui→model,domain` をimport走査テストでCI固定した。`model→data` の変異でテストが落ちることも確認。画面APIと見た目・挙動は変更なし。Vault/ノート切替・テーマ・蒸留保存・補記/クイズ通知・再会カードの実機回帰確認済み。テスト410→425、Lint warning 28件で変化なし | [architecture](design/architecture.md) |
| 2026-07-26 | — | **入出力に用途別の予算と結果の扱いを持たせる（読込予算・累計回数の分離・保存スコープの3件）**。3件とも原因は同じで、**入口で量を決めず、出口で結果を見ていない**こと。**①読込予算**: `readNoteContent()` が `readText()` でEOFまで読んでいた。この無制限経路は「大きすぎて蒸留できないノートの表示フォールバック」と「関連ノート候補の本文読込」で使われる。皮肉なことに、蒸留を256KBで止めたノートほどこちらへ落ちてくる。関連ノートは候補40件を最大8並列で読むため、巨大ファイル1つでAI入力の文字数上限が効くより前にメモリとI/Oを消費していた。用途ごとに入口を分けた（`readNoteForDisplay` 1MB／`readNoteSnippet` 8KB／`readNoteSnapshot` 256KB）。`maxBytes` を引数で受けないのは、呼び出し側が「とりあえず大きめ」を渡せると上限を置く意味が薄れるため。上限で切ると多バイト文字が割れるので `dropIncompleteUtf8Tail()` で末尾の不完全なシーケンスを落とす（日本語は3バイト文字が並ぶため上限がどこに来ても割れうる）。`readAtMostBytes()` は上限に達した時点で読むのをやめ、続きの有無は1バイト覗いて判定する（上限ちょうどのノートを「切り詰めた」と誤表示しないため）。**②累計回数**: `visits` は直近30件しか残さないのに、その件数を「これまで開いた回数」として使っていた。表示が「30回」で止まるだけでなく、`needsAiSummary` の判定も止まるため**31回目以降どれだけ読んでもAI俯瞰要約が二度と更新されず、古い要約が「最新」として出続けていた**。`totalVisitCount` を足して schema v2 へ。既定値を `visits.size` にしたので既存の構築箇所は無改修。ただし **`copy()` は既定値を再評価しない**ため、背面化→復帰→離脱で訪問を差し替える経路は累計だけ二重に増える。この不変条件を `withoutLastVisit()` に閉じ込めた（保持件数は30で頭打ちになってこの誤りを隠すので、累計そのものを見る回帰テストを置いた）。v1→v2 の移行は decode 側で、**v1 の checksum を v1 の正規形で照合してから**現行版へ寄せる（順序を逆にすると既存の痕跡が全部「破損＝孤立」になる）。v1 に `totalVisitCount` が書き足されていても読まない（v1 の checksum はこの値を含まず、読むと任意の回数を名乗れる入口になる）。**③保存の寿命**: 保存が `viewModelScope` に載っており、タスクスワイプでは `onStop()` → `pause()` の直後に `onCleared()` が走ってIOへ渡る前にキャンセルされていた（背面化のみなら成立するので気付きにくい）。アプリ寿命のスコープへ移し、`ReadingTraceSaveResult` を受けて失敗時は `dirty` を戻す。`ProcessLifecycleOwner` を使わないのは `lifecycle-process` の依存追加に対し必要なのが「`onCleared()` で死なないスコープ」1つだけだから。`persistSummary` は対象外（書けなくても次回の再会で作り直される自己修復）。**テストは全12ケースで変異検証を行い**、対応する修正を1行外すと落ちることを確認した。うち初版の再試行テストは `resume()` が無条件に `dirty` を立てるせいで巻き戻しを経由せず通っていたため書き直している（A案と同じ型の落とし穴が再発した）。テストは391→410、Lint warning 28件で変化なし | [reflect_reading_trace](design/reflect_reading_trace.md) |
| 2026-07-26 | — | **非同期の境界を世代IDで揃える（後着する4経路とエラーの握りつぶし）**。裸の `scope.launch` のまま要求単位に追跡されていなかった4経路を塞いだ。**着手前の再確認で優先度付けを訂正した** — 「要約のモデルDLだけが実害まで到達する」と起票していたが、この経路が走るのは `SummarizeUseCase` が `AiNeedsDownload` を返す**モデル未DL時のみ**で、DL済みなら `Available` 経路（`CancellationException` を再throw済み）に入り `cancel()` だけで止まる。日常的に踏むのは `selectFolder` だった。**①スコープ切替**: 検索実行中に別フォルダのchipをタップすると前のスコープの結果がそのまま出ていた（`selectFolder` は `selectedFolder` を差し替えるだけだった）。`startRequest()` で採番を進めて `searchState` を Idle へ戻す。同じchipの再タップは早期returnして取り違えで結果を失わないようにした。**②Vault世代の新設**: 補記一覧・フォルダ一覧は**ノート切替では無効化してはいけない**（補記管理画面はノートと無関係）ため、既存の `cancelAndClear()` に相乗りできない。`NoteViewModel` に `vaultGeneration` を持たせ `saveVault()` で採番し、Vault単位の要求だけがこれを `update` 直前に照合する二層構成にした。`vaultUri` の比較で代用しないのは A→B→A の選び直しで同じ値になるため（キャッシュは破棄されるので無効化したい）。**③補記一覧・削除**: `listJob` 1本に載せ、`deleteAll` は削除対象を起動時の一覧で固定したうえで1件ごとに世代を見る（永続URI権限が残っている端末では切替後も旧Vaultを消せてしまうため）。**④要約DL**: ViewModel直書きのこの1本だけがController化から取り残されていたので、最小修正ではなく Quiz/Annotation/Distill と同型の `SummaryController` へ切り出した。関連ノートは走査キャッシュ（Uriを持つ `NoteFile`）に依存するためViewModelに残し、DL完了後の再実行は `onModelReady` で呼び戻す。結果としてこのクラスはAndroid依存を持たず、**唯一 Uri 非依存でフル検証できる経路**になった。**⑤3-2**: `deleteDocument()` の Boolean を集計して失敗件数を一覧の上に出し（Error に倒すと一覧が消えて再削除できない）、`loadFolders` の空 `catch` をやめて chips の下に注記を出す。**テストの落とし穴を1つ潰した** — `SummaryControllerTest` の初版は `cancelAndClear()` 経由で書いたため `downloadJob` が止まってガードに到達せず、**ガードを外しても落ちなかった**。DLを生かしたまま次の要求だけを進める形に直し、`setStateIfCurrent` と DownloadCompleted のガードそれぞれを外すと対応するテストが落ちることを確認した。**保証していない範囲**: `loadFolders` と補記一覧・削除の世代照合は非nullの `Uri` を要するためJVMテストで固定できず、実機確認で担保する。テストは379→391、Lint warning 28件で変化なし | [architecture](design/architecture.md) |
| 2026-07-26 | — | **検索の世代管理**。`SearchController` だけが Job も requestId も持たず無条件に `scope.launch` していた（他5 Controller は実装済み）。SAF走査はスコープキャッシュを跨ぐと所要時間が大きく変わり完了順が保証されないため、連続検索でも**検索⇄ランダム切替でも**古い結果が新しい結果を上書きし得た。他5本と同じ `activeRequestId` ＋ `Job?.cancel()` 方式へ統一し、検索とランダムは同じ `searchState` を奪い合うのでJobは1本を共有する。あわせて `catch` から `CancellationException` を除外した（①だけ直すと「キャンセルがエラー表示に化ける」が顕在化するため分割不可）。**その過程で `SearchPickerUseCase.pick` も `CancellationException` を `PickerResult.Error` へ畳んでいたことが判明**。中断せず正常に戻るため呼び出し側の `catch` では捕まえられず、`cancel()` だけでは防げない型だった（`SummarizeUseCase` / `RelatedNotesUseCase` は既に再throw済みで、本UseCaseだけが外れていた）。UseCase側の再throwと Controller の requestId ガードの二重で塞いだ。Vault切替（`onVaultChanged`）でも走行中の要求を止めるようにした（旧Vaultのノートが、切替後に Idle へ戻したはずの `searchState` を上書きして現れるため）。`scopeNotesCache` が素の `mutableMapOf` で成立している前提（Main単一スレッド）もコメントで明文化。**自動テストは付いていない** — `SearchController` は `ContentResolver` を、`NoteFile` は `Uri` を要求し、`Uri.EMPTY` がスタブでnullになるためJVMテストで生成できない（既存テストが全て「Uri非依存」なのも同じ理由）。モック導入か `NoteViewModel` の依存注入によるテスト用シーム整備が前提になる。テスト379件・Lint warning 28件で変化なし | [ai_picker](design/ai_picker.md) |
| 2026-07-26 | — | **軽量課題5件の一括消化**。**未使用import**: コメント内で型名に触れるためだけに残っていた未使用import 6件を削除し、コメント側はバッククォートで型名を残した。うち `NoteSections`→`ui.markdown`、`AnnotationComposer`→`controller` の2件が残存依存だったため、**これで「ビジネスロジックからUIへの依存が消えた」（PR #37の主張）が実コード上も成立**（パッケージ間循環3組は型の移動が要る別課題として残る）。**可視性**: `Float.fractionBetween` を `internal`→`private` へ。呼び出しは `VigilithOpeningMotion.kt` 内5箇所のみで、同パッケージへ公開したままだと `VigilithMascotMotion` 側に同名の別定義が生まれる余地があった。**テスト網羅漏れ**: `Summarizing` の `lensGlowAlpha` が `Idle` を上回る（ベース 0.18 vs 0.08）という意図的な輝度差は、既存テストが各モード**内**の変化しか見ておらずモードを跨いだ大小関係を固定していなかったため、同一 `loopFraction` 同士の比較を追加。**CI導入**: `.github/workflows/ci.yml` を新設し、PR と main への push で `testDebugUnitTest` / `lintDebug` を実行。JDKはローカルの Android Studio 同梱 JBR に合わせて 21、失敗時の追跡用にテスト・Lintレポートを artifact 化、同一ブランチの連続pushは古い実行を打ち切る。**バックアップ除外**: `allowBackup="true"` かつ除外ルール未指定で `random_note_prefs`（Vault の SAF URI・テーマ・当日履歴のタイトル）がクラウドバックアップに含まれ得た。`dataExtractionRules` は **API 31以上でしか効かず minSdk 26 を覆えない**ため、API 30以下向けの `fullBackupContent` と両方を指定。API 31以上では cloud-backup に加え device-transfer からも除外した（SAFの永続URI権限は移行先端末で無効になり、復元しても壊れた参照が残るだけのため）。`applicationId` の変更は正式ID決定が要るため見送り。テストは378→379、Lint warning は28件のまま | — |
| 2026-07-26 | — | **ダークモード（オプション画面での明示切替）**。2026-07-22に「独自配色との整合コストに対し不要」として死亡宣告した項目を、整合コストの正体が色設計の負債と判明したため再オープンし実装。**切替はオプション画面のトグルのみ**（`isSystemInDarkTheme` によるOS追従は不採用。ブランド由来の独自配色がOS設定で勝手に変わるのを避ける）。`SharedPreferences` に真偽値1つを保存し、プロセス再起動なしで即時反映。**暗色パレットの土台は `QuizScreen` が単独で持っていた既存のダーク配色**（背景 `#1A1C2E` ／面 `#2A2D45`）。机上で新色を起こすより、実装済みで目視を通った値から広げた。**`Indigo` はボタン以外にも約30箇所で文字色として使われており**（AIタブ見出し・チャット・関連ノート・補記結果・再会カード）、暗面では 2.83 で読めないため役割トークン `AccentText` を新設。文字は4.5:1が要るので塗り用 `#8A80FF`（4.24）とは別に `#A79EFF`（5.76）を使う。下部ナビ／レールも `Indigo` 固定ではダークで明るい帯が残るため `NavBar` をトークン化。`themes.xml` の statusBar/navBar の `@color/indigo` 直指定を透明化し `enableEdgeToEdge()` に委譲。Vigilithは暗背景で胴体が溶けるため、WebPは再生成せず背後に `LogoPurple` 22%のhaloを敷いて輪郭を確保。既存の役割トークンを `@Composable` の窓口へ変えることで**呼び出し側163箇所を1行も書き換えずに**テーマ追従へ移行した。クイズ画面のエラー2色（`#CC0000`＝2.86／`#555555`＝2.25で読めなかった）も修正。テストは372→378（明暗両方のコントラストを固定）。**レビュー指摘3件を修正**: ①引数なしの `enableEdgeToEdge()` はシステムバーのアイコン明暗を**OSの uiMode** から決めるため、「OSライト＋アプリダーク」で暗い背景に暗いナビアイコンが描かれていた（`isAppearanceLightNavigationBars` を明示固定）。②`errorText` / `dangerAction` を Badge・チップの `containerColor` に流用したうえ白文字を固定していたため、暗所で「明るい赤に白文字」（2.52〜2.78）になっていた（塗り用の `errorSurface` / `onErrorSurface` / `onDangerAction` を新設し塗りと前景を対で持たせた）。③Vigilithの Ready バッジが `Indigo` 固定で暗背景に対し 2.20（`accentSurface` へ）。**2026-07-26に実機で一巡し問題なしを確認**（色・余白定数の追加調整なし）| [dark_mode](design/dark_mode.md)・[theme_and_ui_refactor](design/theme_and_ui_refactor.md) |
| 2026-07-26 | — | **テーマ基盤とUI構造のリファクタ（R-1〜R-4／ダークモードの土台）**。**R-2**: 同一概念だった `SectionFabStatus`（NoteReaderTab内）と `VigilithActionStatus` を後者へ統一し、`NoteReaderTab.kt` の2箇所に逐語コピーされていた導出ロジックを `VigilithMode.kt` の純関数 `sectionChatStatus` / `fullscreenAiStatus` へ集約。テストゼロだった「要約×クイズ」合成に11ケースを新設。**R-1**: `NoteReaderTab.kt`（626行）を タブ本体247行／`FullscreenNoteScreen.kt` 248行／共用部品 `NoteComponents.kt` 175行へ分割。あわせて `AnnotationManagerScreen` の `IconPillButton`（`contentDescription` をSemanticsへ設定しておらずTalkBackで読み上げ不能）を共用 `IconPill` へ統合し、見た目を変えずにa11yを修正。**R-3**: `MainActivity` の `setContent{}` に直書きされていたVigilithの導出・保持・タップ配線を `rememberVigilithState()`（`VigilithState.kt`）へ抽出。タップ分岐は stale closure を避けるため `rememberUpdatedState` 経由。**R-4**: `Color(0x…)` 直書き61箇所／9ファイルを全廃し、`AppColors.kt` を「ブランドパレット／役割トークン／クイズ暗色面／グラデーション」の4区画へ再編。`LinkBlue`→`LinkText`、`ErrorRed`→`ErrorText` へ改名（名前に色を含めない）。塗りボタンのラベル色を明示（Material3の既定 `onPrimary`＝純白に暗黙依存しており、ピンク3.41／緑2.49とAA基準割れだった → 黒ラベルで6.16／8.44へ）。`AppTheme`＋`LocalAppColors` を新設し `setContent{}` 直下へ挿入。コントラストを固定するテスト9件を追加。**唯一の意図的な見た目変更はボタンのラベル色（白→黒）**、他は現状維持。テストは352→372 | [theme_and_ui_refactor](design/theme_and_ui_refactor.md)・[dark_mode](design/dark_mode.md) |
| 2026-07-25 | #35 | **ReadingTrace レビュー指摘4件の修正**。**①到達率の誤100%**: ブロック数だけで測っていたため、長大な段落・コード・表が1ブロックとして描画されると冒頭しか見ていなくても `1/1=100%` になり、カードが「最後まで読んでいます」と誤断定していた。`(最深ブロックindex + そのブロックの可視割合) / 総ブロック数` の**切り捨て**へ変更し、100%を「最終ブロックの末端が画面へ入った」場合だけに限定。可視割合は LazyList座標(Int)だけを受ける純関数 `visibleFractionOfBlock`（`ui/ReadingProgressGeometry.kt`）へ切り出し、報告は5%刻みへ量子化してスクロール中の連続emitを抑えた。**②背面化・復帰**: `onStop` の flush でセッションが永久に消費済みになり、復帰後に読み進めた位置を失う／10秒未満なら背面時間が閾値へ混ざる問題を、`pause()`/`resume()` による**能動読書時間の積算**へ変更。背面化時の書き出しは維持しつつセッションを残し、復帰後の読み進めで**同じ訪問を差し替える**（ホームボタンのたび「これまで◯回」が膨らまない）。差し替えは保存済み末尾が自分の訪問と一致する場合に限定し、別端末の追記は上書きしない。**③Vault分離**: 保存が非同期起動で保存先を書込時点の `vaultUri` から解決していたため、切替後の新Vaultへ旧ノートの痕跡が書かれ得た。ノートを開いた時点のVault識別子(String)を要求へ添えて運び、Gatewayが書込直前に照合して不一致なら捨てる方式へ変更。当初の実装は確認後に `vaultUri()` を読み直しており対策が効いていなかったため、**読み取りを1回に集約**して修正した（[bugfix_reports](bugfix_reports.md) #4 に収録）。`NoteViewModel.vaultUri` は `@Volatile` へ。**④検索フォールバック**: 候補40件以下では並べ替えを通らずSAF列挙順の先頭3件を返しながら「キーワード一致で表示しています」と表示していた。候補数に依らずbigramスコア順へ統一し、**一致0件は返さない**（0件時は既存の「見つかりませんでした。」）。採点・選抜を `domain/SearchKeywordMatching.kt` の純関数へ切り出し、フォールバック（0件は落とす）と再現率カット（Nanoへ渡すので0件も残す）を分離。1文字クエリがbigramを作れず全件0点になる穴も部分一致で解消。テストは282→317ケース | [reflect_reading_trace](design/reflect_reading_trace.md) |
| 2026-07-22 | #32 | **蒸留（Distill）v1**: Reflectで、AIが重要文を選び（生成しない）ユーザー確認分だけを元ノートで太字化するプログレッシブ要約支援を新設。UTF-16オフセット保持＋Markdown構造認識の専用モデル（`DistillSourceModel`）、AI不使用のサリエンス＋構造的重みスコア、境界regexのID応答パース、原バイトSHA-256の二重競合確認＋`noBackupFilesDir` 復旧レコード＋起動時4分岐＋v1最小復旧UI、`openNote()` を使わない本文専用リロード（要約/関連/補記は維持・チャット/クイズは破棄）。累積太字上限は編集対象本文比率30%＋短文1文例外。保存後の状態遷移バグ（旧セクション文脈の維持で誤フォールバック・旧エラー再表示）も修正。**クイズ生成エラー削減**: 常に4択2問+解説を強要して出力上限超過→`MAX_TOKENS`全破棄を、`QuizInputProfile` の入力量分類で○×2問/3択2問/4択1問へ適応化し解説を削減して回避。多択の正解解釈を単語境界regexへ変更し崩れた応答を救済（BugFix #3） | [reflect_distill](design/reflect_distill.md)・[reflect_reading_trace](design/reflect_reading_trace.md)・[section_ai_chat](design/section_ai_chat.md)・[bugfix_reports](bugfix_reports.md) |
| 2026-07-21 | #31 | 全画面ノート（#30）の潜在不具合修正（レビュー対応）。**①スクロール書き戻し競合**: 離脱時 `scrollToItem` を起動しっぱなしにするとpop→scopeキャンセルで書き戻しが完了せず消えていた（フリング中に顕著）。`action()`（閉じる処理）を `scrollToItem` 完了後へ移動。**②進入前Snackbar残存**: `SnackbarHost` は全画面ルートでも描画されるため、進入時に `currentSnackbarData?.dismiss()` で既存通知を消す。**⑤アクセシビリティ**: `IconPill`（✕/⛶）のデッドだった `contentDescription` をSemanticsへ設定し絵文字を読み上げ除外（二重読み上げ回避）、`FullscreenAiFab` を48dp（Material推奨タッチ領域）へ拡大し `role`/`onClick`/`contentDescription` を付与しスクリーンリーダー操作可能に。**⑥多重遷移防止**: `note_fullscreen` 遷移へ `launchSingleTop` を付与。※Aquaコントラスト指摘はラベルがIndigo地上・アイコンが絵文字のため非該当として見送り。**⑦折りたたみ解除後に閉じられない不具合**: ①で `action()`（閉じる処理）を suspend の `scrollToItem` 完了後へ移したことの副作用で、Fold開閉によるActivity再生成後は `scrollToItem` が完了せず✕・システムバックとも無反応で全画面を解除できなくなっていた。非suspendの `requestScrollToItem` で保留位置を積むだけにして `action()` を即実行し、閉じる処理を書き戻しから切り離した（①の「pop→書き戻し消失」の意図は原理的に保持）。あわせて状態遷移でボタン無反応になる潜在バグの知見を `bugfix_reports.md` に新設（⑦＋Claude Fable 5混入のSectionFab古クロージャ固定を収録） | [note_fullscreen](design/note_fullscreen.md)・[bugfix_reports](bugfix_reports.md) |
| 2026-07-21 | #30 | UIUX改善。**②スケルトンUI**: 要約生成中の表示をスピナー＋文言から自前shimmerの3行スケルトンへ（accompanist非推奨のため`Brush.linearGradient`＋`infiniteRepeatable`で自作）。**④スクリム**: `ModalBottomSheet` のスクリムを既定より濃く（`alpha=0.5f`）し背景グラデーションの透けを抑制。**タイトル体系の統一**: 各タブH1を機能説明から英語コンセプト語へ（ランダムAIノート→Rediscover／さがす→Explore／関連ノート→Connect／AIアシスト→Reflect）。「英語H1＝概念／日本語サブ＝意味補助／日本語ボタン＝操作」の3層役割分担でブランド(Obsidian Mind)-概念-機能タブ(日本語)の階層を明確化。主ボタン文言も「動詞＋を＋名詞」へ統一。**ナビ選択ピル**: 下バー/レールの選択インジケータを `Panel@22%`（Indigo地に埋もれ視認性不足）から `Aqua` へ変更し選択状態を明確化（タブは絵文字アイコンのため色指定が効くのはピルのみ・ラベルはIndigo地上のため白を維持）。**全画面ノートの独立ルート化**: `content` 内オーバーレイ（バー/レールを覆えず・システムバー残存・額縁・スクロール先頭復帰）を非タブルート `note_fullscreen` へ移行。進入時に `systemBars()` 没入・離脱時ナビバーのみ復元、ノート色で全ブリード＋本文720dp中央寄せ、通常表示とスクロール位置を継承（遷移中の同時コンポーズを避けるため専用state＋開始継承/離脱書き戻し）。読書中も要約＋クイズの合成状態を最小FABで表示、全画面中はSnackbar抑制 | [note_fullscreen](design/note_fullscreen.md) |
| 2026-07-21 | #29 | 関連ノートAI推薦の Phase 3。**3a**: 採番順の並べ替えを全Vaultのタイトル話題スコアへ置換（文字bigram Dice係数＋採番近接の加点）。**3b**: 上位40件を現ノートの tags/snippet/title 類似で再ランクする二段ランキング（件数維持・並べ替えのみ／Nanoの位置バイアス対策）。採点戦略を注入する汎用ランキング基盤 `rankByScore` を分離し3a/3bで共有。実機で「jetpackcompose」ノートの関連が全てKotlin系で揃うことを確認 | [related_notes_ai](design/related_notes_ai.md) |
| 2026-07-20 | #28 | 関連ノートAI推薦の Phase 2。候補をタイトルに加え本文スニペット・タグ・aliasesで肉付けし入力予算内へ動的短縮（2a）／候補本文を `URI+lastModified` でキャッシュ（成功時のみ格納）し `Semaphore(8)` 上限付き並列で読込（2b） | [related_notes_ai](design/related_notes_ai.md) |
| 2026-07-20 | #27 | 関連ノートAI推薦の Phase 1。候補に一時ID(C01..)を採番しNanoにはID応答させるID応答方式（言い換え・同名衝突での解決失敗を解消）。※当初この行には「フォーカスセクション文脈での推薦（長ノートの先頭切り出し解消）」も記載していたが、2026-07-24の実装突合で**未実装**と判明したため削除（現ノートの文脈は全文の先頭600文字のまま） | [related_notes_ai](design/related_notes_ai.md) |
| 2026-07-20 | #26 | アプリ起動時のブランドOPアニメーションを追加。システムスプラッシュ（`core-splashscreen`）＋Compose OP（`OpeningScreen.kt`）の2層構成。背景を着地画面と同色（`ReadingGradient`）で終端し継ぎ目を解消／`savedInstanceState==null` で新規起動時のみ再生／アダプティブランチャーアイコン新設／レビュー対応（TalkBack二重読み上げ回避・発光色を`AppColors`へ集約） | [opening_animation](design/opening_animation.md) |
| 2026-07-20 | #25 | AI補記の途切れ対策（finishReason検知・出力要求の絞り込み）／クイズを「フォーカスセクション周辺から2問」へ再設計し入口を吹き出しシートへ移動（AIタブのQ&Aボタン廃止・バッジ補記のみ）／クイズのセッション従属化／吹き出しタップ無反応の修正（pointerInputのstale closure） | [section_ai_chat](design/section_ai_chat.md)・[background_ai_ux](design/background_ai_ux.md)・[architecture](design/architecture.md) |
| 2026-07-20 | #24 | PR #23のレビュー対応3件（デッドコード削除・Snackbarイベントキーのテスト可能化・画面回転での通知再表示抑止）／Vault選択をオプションへ移動／全画面✕ボタンの視認性修正／当日分の閲覧履歴「今日読んだノート」追加 | [background_ai_ux](design/background_ai_ux.md)・[tab_navigation](design/tab_navigation.md)・[ai_picker](design/ai_picker.md) |
| 2026-07-20 | #23 | Q&A・AI補記のバックグラウンド生成UX。待機画面への遷移を廃止し、Snackbar通知＋AIタブバッジ＋未確認（isViewed）管理に変更。`QuizController` 新設、requestIdによる古い結果の混入防止 | [background_ai_ux](design/background_ai_ux.md) |
| 2026-07-19 | #22 | AI要約の生成中もノート閲覧を継続可能に（要約待ちのブロッキング解消） | [background_ai_ux](design/background_ai_ux.md) |
| 2026-07-19 | #21 | 壁打ちUI改善: 読む画面の低彩度グラデーション（ReadingGradient）・ボタン配色3役ルールの明文化・ノート出現アニメーション・検索結果への更新日表示 | — |
| 2026-07-19 | #16〜#20 | コード品質改善活動。静的分析で27項目（重要度高4・中8・効率5・リファクタ10）を指摘し全件解消。`NoteViewModel` の機能Controller分割（906行→348行）、SAF走査キャッシュ、AIジョブのキャンセル/タイムアウト制御、ユニットテスト整備 | [architecture](design/architecture.md) |
| 2026-07-18 | #15 | AIピッカー「さがす🔎」タブ。フォルダスコープを共通土台に、自然文キーワード（Nano選定3件）とランダム（AI不使用3件）の2モード | [ai_picker](design/ai_picker.md) |
| 2026-07-17 | #14 | セクション単位AIチャット。浮遊吹き出しがスクロールに追従し、今見ているセクションの要約・質問候補・Q&Aを生成 | [section_ai_chat](design/section_ai_chat.md) |
| 2026-07-16 | #13 | AI補記メモの削除機能（オプション画面を新設） | — |
| 2026-07-16 | #12 | UIをタブ・ナビゲーション構成に再設計（Plan C）。Foldのボタン切れとダブルタップ全画面不発を構造的に解消 | [tab_navigation](design/tab_navigation.md) |
| 2026-06-19 | #11 | AI補記プロンプト改善・UI名称変更・表示整理 | — |
| 2026-06-19 | #10 | Android 17対応・ステータスバー非表示・UI調整 | — |
| 2026-05-31 | #9 | AI補記メモ機能の追加・AI推薦の改善 | — |
| 2026-05-31 | #8 | Q&Aフラッシュカード（オンデバイス4択生成）・グラフビュー・関連ノート改善 | — |
| 2026-05-30 | #7 | リファクタリング: Activity分割・ノート走査のスタック安全化 | — |
| 2026-05-30 | #6 | Fold展開時に関連ノートTop-5を左ペイン表示・タップで本文切替 | — |
| 2026-05-30 | #5 | Gemini Nano 4によるオンデバイスノート要約（ML Kit GenAI Prompt API） | — |
| 2026-05-30 | #4 | Markdownレンダリング強化（見出し・リスト・コード・引用・テーブル等） | — |
| 2026-05-11 | #2, #3 | アプリ名を「Obsidian Mind」へ変更、旧View系リソース整理、ソースコード解析書の整備 | — |
| 2026-05-10 | #1 | Jetpack Compose移行 | — |
| 2026-04-30〜05-10 | — | プロジェクト開始。Java実装→Kotlin化、MVVM＋SAF＋Coroutinesの基礎構築 | [project_origin.md](project_origin.md) |
