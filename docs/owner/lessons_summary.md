# 教訓（lessons）の棚卸しシート

**プロジェクト:** Vigilith AI（旧 Obsidian Mind）
**作成:** 2026-09-08 / 全62件を通読して作成
**更新:** 2026-09-08 — `L12`・`L15` の畳み込みと `L7`・`L9`・`L10` の統合判定を反映し、検査の件数を数え直した

**位置づけ:** [dev/lessons.md](../dev/lessons.md) と [dev/lessons/](../dev/lessons/) の**中身を精査するための作業台**。
正本は `dev/` 側にあり、**ここは目録と観察であって判断ではない**。
`owner/` の他文書と同じく検査に載せない（→ [README](README.md)）。

---

## 1. 数字

| 数え方 | 件数 |
|---|---:|
| **教訓の総数** | **62**（`L1`〜`L63`。**欠番は `L62` の1つだけ**＝2026-09-07 に取り下げ） |
| 独立カードを持つもの | 36（`lessons/L*.md`） |
| **索引の中に本文があるもの** | **26**（`L1`〜`L18` と `L21` `L27` `L33` `L37` `L43` `L54` `L57` `L63`） |
| **検査を名指ししている＝規約** | **26** |
| **検査を持たない＝参考** | **36** |
| （うち索引の「検査」列が埋まっている行） | 23 |
| 索引の行数 | 49（`L1`〜`L16` が1行に畳まれているため、件数とは一致しない） |
| **役目を終えた（転送だけを持つ）** | **2**（`L12` `L15`） |

> **索引の行数と教訓の件数は別物である。** 前者は読むための目次で、`L1`〜`L16` を1行にまとめている。
> 数えるときに取り違えやすい（実際に一度取り違えた）。
>
> **検査の件数も同じ理由でずれる。** 畳まれた行は検査列を持てないので、
> **`L7`（`PackageDependencyTest`）・`L9`（`AppColorContrastTest`）・`L13`（`NoteExcerptThreadingTest` ほか）は
> 本文に検査があるのに索引では空欄に見える。** 数えるなら索引の列ではなく本文を見る。

## 2. テーマ別の全62件

**凡例:** 📄=独立カード ／ 📋=索引内に本文 ／ ✅=検査あり（規約） ／ 空欄=参考 ／ ~~取り消し線~~=役目を終えた（転送）

### A. 「できた」と「効いている」は別（14件）

**このリポジトリで最も厚い系列。** テストや検査が「ある」ことと「効いている」ことを繰り返し取り違えている。

| ID | 一言 | | |
|---|---|---|---|
| `L7` | 「整理した」と「境界が効いている」は別。レイヤー分割後も循環が4組残っていた | 📋 | ✅ |
| `L9` | 検証条件が実物と一致しているかは、テストが通ることでは分からない（配色を存在しない面で測っていた） | 📋 | ✅ |
| `L10` | 「土台を組み立てられる」と「土台が動く」は別。CIは緑で 0 success / 2 failure | 📋 | |
| `L11` | **テストが効いているかは変異させて確かめる。** 落ちるテストを書けないガードは削除候補 | 📋 | |
| `L13` | 「純粋」と「軽い」は別。1MBの解析460msをMainで呼んでいた | 📋 | ✅ |
| `L25` | 「網羅テストがある」と「そのフィールドが網羅されている」は別 | 📄 | ✅ |
| `L30` | 失敗が「赤」ではなく**件数の減少**として出ると気づけない | 📄 | ✅ |
| `L44` | 出力を丸ごと固定する回帰テストは、**誤った出力も同じ強さで固定する** | 📄 | ✅ |
| `L46` | リフレクションで全欄を見る検査でも、入力を手で書いていれば新しい欄は素通りする | 📄 | ✅ |
| `L52` | 上限テストは長さだけでなく**要素数**も最大化する | 📄 | ✅ |
| `L53` | 実機でしか走らないテストは、効いていることを確かめられない | 📄 | ✅ |
| `L55` | **走査テストは呼び出しの存在しか見ない。** 受理条件は結果で書く。3度出た | 📄 | ✅ |
| `L59` | 実装から書いた受け入れ条件は、実装が狙いを外していることを検出できない | 📄 | |
| `L61` | **前提を共有した検査は前提ごと間違える。** 符号の意味は値に現れない | 📄 | ✅ |

### B. 直した場所の隣が残る（6件）

| ID | 一言 | | |
|---|---|---|---|
| `L14` | **横展開は最後の1本を取り残す。** 関数名ではなく性質で grep する | 📋 | ✅ |
| `L27` | 仕様を変えるときは、旧仕様を固定しているテストと文書を**先に数える** | 📋 | |
| `L37` | 誤りを直した先で新しい誤りを作る。事実1行ではなく**支えている判断ごと**当て直す | 📋 | |
| `L40` | 改名した型は、正本の機能仕様まで**同じPRで**当て直す | 📄 | ✅ |
| `L41` | 単位を細かく割ると、その値を読む既存箇所が古い単位で取り残される。3度出た | 📄 | ✅ |
| `L63` | **新しい入口は「いつ起きるか」の前提を壊す。** 1回のレビューで3件同時に出た | 📋 | |

### C. 宣言と実行の乖離（9件）

**「そう書いた」ことと「そうした」ことの距離。** 文書運用そのものへの教訓が集まっている。

| ID | 一言 | | |
|---|---|---|---|
| `L8` | 文言と実装の乖離は小さいうちに潰す。画面の説明はそのまま仕様として読まれる | 📋 | |
| ~~`L15`~~ | **役目を終えた（転送）** → `L17`。二択の立て方そのものが誤りだった | 📋 | |
| `L19` | 設計書には処方だけでなく**効果の検算を1つ**書く | 📄 | |
| `L23` | 台帳に長く残った課題ほど、着手時には前提が古い | 📄 | |
| `L29` | **規則は検査へ変えるまで守られない。** 検査は守りたい性質より狭い対象を数える | 📄 | ✅ |
| `L31` | **件数はトリガーにならない。** 閾値は観測か予測かを先に問う。3度 | 📄 | |
| `L32` | 「自分を評価する」と宣言した仕組みは、**評価する規則自体が守られない** | 📄 | |
| `L33` | **教訓を書く行為は、適用を保証しない。** 書いたら、いま触っているものへ当て直す | 📋 | |
| `L38` | **検証の証跡を、検証せずに書く。** 存在しないコミットIDが12本に書かれていた | 📄 | ✅ |

### D. 非同期・中断・状態の寿命（5件）

| ID | 一言 | | |
|---|---|---|---|
| `L26` | 非同期の要求は「今」を読み直さない。**要求時点の対象を要求自身に持たせる** | 📄 | |
| `L35` | 必ず実行できるべき操作を、完了保証のない suspend の後ろに置かない | 📄 | |
| `L49` | **中止は要求であって完了ではない。** 待っている間の再入も同じ穴。2度出た | 📄 | ✅ |
| `L56` | 1つの受け入れ条件を複数の状態で満たすなら、**寿命を揃える** | 📄 | ✅ |
| `L58` | **中間状態は届くとは限らない。** 契機を「通ったか」に置かない | 📄 | ✅ |

### E. 不可逆操作と破壊性（6件）

| ID | 一言 | | |
|---|---|---|---|
| `L2` | 機能ごとに堅牢性レベルを意図的に選ぶ。軸は「壊れたときノートが失われるか」 | 📋 | |
| `L24` | 判定の**根拠が壊れている**なら、ユーザー確認を挟んでも訂正されない | 📄 | |
| `L43` | 破壊性はタスク名ではなく**実際に踏む境界**で判断する（対象アプリごと消した） | 📋 | |
| `L47` | 不在を破壊的処理の根拠にするなら、**不在を返しうる全境界**で「不能」と分ける | 📄 | ✅ |
| `L48` | 不可逆操作の承認は、**見せた瞬間と書く直前**を突き合わせて初めて意味を持つ | 📄 | ✅ |
| `L54` | 原状回復より先に、**実データへ再進入しない終了地点**を探す | 📋 | |

### F. 外部（SDK・AI）の意味を実測で確かめる（10件）

| ID | 一言 | | |
|---|---|---|---|
| `L20` | 端末AIのテストは**フォアグラウンドActivityが前提条件そのもの** | 📄 | |
| `L21` | 切り分けが止まったら、**「動くはずのもの」を対照に置く** | 📋 | |
| `L22` | 文字数の比率とトークンの比率は別物。**分母と単位を明示する** | 📄 | |
| `L34` | AI混入バグは**通常フローで表面化しない**。3類型ごとに勘所が違う | 📄 | |
| `L36` | **防御的パースは全分岐で対称に。** 緩くしすぎると別種の誤りを生む | 📄 | |
| `L39` | テストダブルは、**確かめていないSDKの契約を固定してしまう** | 📄 | ✅ |
| `L45` | 「必ず収まる」の最後の砦は、**何を残すべきかを知らない** | 📄 | ✅ |
| `L50` | **外から見える契約は、外から見える形で検査する。** decodeした意味は形の欠落を通す | 📄 | ✅ |
| `L51` | **同じ文字列を2つが読むなら、解釈器を共有する** | 📄 | ✅ |
| `L60` | **SDKの単位は実装ではなく出力で確かめる。** 同じ値を2度取り違えた | 📄 | ✅ |

### G. 設計判断の型（8件）

| ID | 一言 | | |
|---|---|---|---|
| `L4` | 見せ方は機能名ではなく**起動契機**で決める（押した機能は理由を返す／自動は黙って劣化） | 📋 | |
| `L6` | 修正の主張は、**修正コードを別の目で読むまで**確定させない | 📋 | |
| `L16` | 製品判断が、**別の経路の検証可能性を奪う**ことがある | 📋 | |
| `L17` | 二択に見えたら**強度と時間の軸**を疑う（旧 L15 を統合） | 📋 | |
| `L18` | 段階移行の単位は型の大きさではなく**変換関数に手が届く範囲** | 📋 | |
| `L28` | Boolean は「何が起きたか」ではなく**呼び出し側が次に何をするか**で決める。6度 | 📄 | |
| `L42` | 「測る道具が要る」なら、**それがアプリの中に要るか**を先に問う | 📄 | |
| `L57` | **製品判断を、適合必須の基準で補強しない**（WCAG 1.4.1 を誤って援用した） | 📋 | |

### H. Nano制約とAI設計の初期知見（4件）

| ID | 一言 | | |
|---|---|---|---|
| `L1` | **Nano制約が全設計を規定している**（256トークン・Mutex直列化・60秒） | 📋 | |
| `L3` | AIの役割を「創作」から「要約」へ落とすと導線が要らなくなる | 📋 | |
| `L5` | AI出力を別のAI入力へ流す箇所では、**改善も劣化も乗算される** | 📋 | |
| ~~`L12`~~ | **役目を終えた（転送）** → `L22`・`L23`。計測器が入って前提が消えた | 📋 | |

---

## 3. 精査の材料

**観察と、決着した判断が混ざっている。** 決着した分は「済」と書いてある。

### 3-0. これまでに決着した分（2026-09-08）

| 対象 | 判断 | 反映先 |
|---|---|---|
| `L12` `L15` | **役目を終えた。転送へ畳んだ**（行ごと消す「取り下げ」はしない — 生きた参照が2箇所あった） | [lessons.md](../dev/lessons.md)・`roadmap`・`dependency_policy` |
| `L7` `L9` `L10` | **統合しない。相似のまま維持**（骨格は同じでも引き金と処方が全部違う） | `L7` に骨格・判定・再検討条件を記載 |

**「骨格が同じ」はテーマであって統合の根拠ではない**、というのが今回いちばんの収穫。
この形で数えると A系列14件がまるごと1つになってしまい、索引として引けなくなる。

### 3-1. 検査の有無が時期で割れている

| 範囲 | 検査あり | 割合 |
|---|---|---|
| `L1`〜`L28`（28件） | 5件（`L7` `L9` `L13` `L14` `L25`） | 18% |
| `L29`〜`L63`（34件） | **21件** | **62%** |

`L29`（規則は検査へ変えるまで守られない）を書いた前後で運用が変わっている。
**当初「2件だけ」と書いたのは数え間違いだった** — 索引の列だけを見て、本文に検査を持つ
`L7` `L9` `L13` を落としていた。差は縮んだが、**傾向そのものは残る。**

**古い教訓に検査が無いのは、原理的に無理だからとは限らない** — 当時その発想が無かっただけ、という可能性がある。
精査の軸としては「**いま検査に載せられるか**」を1件ずつ問い直すのが素直に見える。

### 3-2. 統合を検討できそうな系列

**同じ骨格が複数のIDに散っている組。** 統合するかは中身を読んで決める話で、ここは候補の提示だけ。

> **`L7` `L9` `L10`（「Xができた」≠「Xが効いている」）は 2026-09-08 に判定済み — 統合しない。**
> 判定の根拠と再検討条件は `L7` の本文が持つ。**残り6系列にも同じ3点（引き金・処方・検査）を当てる。**

| 系列 | 該当 | 共通の骨格 |
|---|---|---|
| 変異確認 | `L11` → `L53` `L55` `L61` | `L11` が原理、残り3件が「変異させられない／変異の当て方を間違えた」適用先 |
| 直した場所の隣 | `L14` `L27` `L37` `L40` `L41` | `L14` が原理、他4件は対象（文書・単位・判断・型名）が違うだけ |
| 宣言と実行 | `L29` `L32` `L33` `L38` | 「書いたことが守られない」の4変奏 |
| 外部の意味 | `L39` `L60` `L61` | SDK・定数・符号の意味を実測せず前提にした |
| 不可逆の根拠 | `L24` `L47` `L48` | 「根拠が古い／不完全なまま破壊的処理へ進む」 |
| 製品判断の副作用 | `L16` `L42` `L57` | 製品判断が検証・計測・理由づけを歪める |

### 3-3. 役目を終えているもの（**済**）

**2件を転送へ畳んだ。** どちらも本文が自分で「終わった」と書いていた。

| ID | 何が消えたか | 引き継ぎ先 |
|---|---|---|
| `L12` | 前提だった「モデルに実際に何を渡したかを見る手段が無い」が、計測器の投入で解消 | `L22`・`L23`（設計書側は `L19`） |
| `L15` | 「抑制するかゲートに載せるか」という**二択の立て方そのものが誤り**だった | `L17`（正本は `dependency_policy`） |

**行ごと消す「取り下げ」（前例 `L62`）は使わなかった** — `roadmap` と `dependency_policy` から
生きた参照があり、消すと壊れる。参照は引き継ぎ先へ付け替えた。

**残りの62件に、同じ形（本文が自ら終わりを書いている）は無い。**

### 3-3b. 検査の表記がゆれている（新規）

数え直しの過程で2件見つかった。**どちらも「検査あり／なし」の判定を読み手ごとに変えうる。**

- **`L60`** — カード本文は「**検査: なし（参考）**」と書きながら、末尾で「検査は `BookletSheetPerspectiveTest` が持つ」
  と書き、索引の検査列にも同テストが入っている。**同じカードの中で二重になっている**
- **`L38`** — `**検査:**` の行を持たず、本文の箇条書きで `AdrShapeTest` を名指ししている。索引の列にはある

### 3-4. 「20行を目安」が実質守られていない

カード36本のうち **34本が20行超**、**16本が30行超**。最長は `L55`（65行）・`L60`（57行）・`L29`（49行）。

正本の規則は「20行を超えたら詳細の正本を `features/` か `system/` に決めて要約＋リンクにする」。
**目安が守られていないこと自体は害ではないかもしれない**が、
「守られない規則を置き続けている」状態は `L29`・`L32` が指摘した型そのものではある。
**規則を実態へ合わせる／畳む／検査を付ける**のどれかを選ぶ論点として残る。

### 3-5. 形態（カードか索引内か）の基準と実態

正本の規則は「**カードを持てるのは、検査があるか、索引の外から引かれている教訓だけ**」。

参照の多い順（`CLAUDE.md`・`app/src`・`features/`・`system/`・`_wip/`・`review/` からの概算）:

`L14`(12) `L55`(10) `L29`(9) `L13`(8) `L51`(7) `L61`(4) `L24`(3) `L25`(3) `L34`(3) `L44`(3) `L53`(3) `L60`(3)

**`L13` は8箇所から引かれているのに索引内本文のまま**で、逆に参照0の教訓がカードを持っている例もある。
基準と実態が一致しているかは、1件ずつ当てないと分からない。

### 3-6. 精査の順序と進み

| | 対象 | 状態 |
|---|---|---|
| 1 | **3-3** 役目を終えた教訓（`L12` `L15`） | **済**（2026-09-08） |
| 2 | **3-2** 統合候補のうち `L7` `L9` `L10` | **済 — 統合しない**（2026-09-08） |
| 3 | **3-2** 残り6系列 | 未。**引き金・処方・検査の3点**を当てる |
| 4 | **3-3b** 検査表記のゆれ（`L60` `L38`） | 未。小さいが、件数の数え方に直に効く |
| 5 | **3-1** 検査なし36件 | 未。「いま検査に載せられるか」を1件ずつ |
| 6 | **3-4** 行数（34/36本が目安超過） | 未。規則を実態へ合わせるかどうか |
| 7 | **3-5** カードか索引内かの基準と実態 | 未。`L13` は8箇所から引かれて索引内本文のまま |
