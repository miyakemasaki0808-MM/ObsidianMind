# L29. 規則は検査へ変えるまで守られない。そして検査は、守りたい性質より狭い対象を数える

**教訓 — 運用は3つ。**

1. **規則を書いたら、「守られなかったことをどう検出するか」を同じコミットで決める。**
   決められないなら、その規則は**次も守られない**と考えてよい。
   実績として、このリポジトリで規則が守られたのは**検査に載せたときだけ**である
2. **検査を書いたら、「この検査が数えていない経路」を先に3つ挙げる。**
   記法・置き場・書式のどれかで狭めていることが多い（[L14](../lessons.md#l14-横展開は最後の1本を取り残す) の検査そのものへの適用）
3. **検査を入れたら、必ず1回変異させて落ちることを確かめる**（[L11](../lessons.md#l11-テストが効いているかは変異させて確かめる)）。
   **落ちない検査は、無い検査より悪い** — 「守られている」という認識だけが残るため

**いつ当てるか:** 文書に「〜する」「〜しない」と書いたとき。
**スクリプトにしない** — 「走らせる」という手動契約が新しく生まれ、同じ罠を1段ずらすだけになる。

**再発の証拠:**
- **2026-08-05（規則が2箇所にあっても守られなかった）** — 「総評で問題と書いたものは課題台帳へ起票する」は
  `CLAUDE.md`（常時読み込まれる）と `document_map` の**両方**にあった。それでも指摘4件が**2日間追跡されなかった**。
  うち1件は生きた読書痕跡を削除し得る P1。
  飛んだ理由は、総評を貼るコミットが「1ファイル・+211行／既存内容への変更はない」で、
  **判断する工程ではなく文書を貼る作業として処理された**こと。
  **チェックリストは自分の変更に向いているので、自分の変更に見えない作業では発火しない。**
  直ったのは **`ReviewFindingsLedgerTest`** に載せたとき
- **2026-08-05（検査自身が狭かった）** — 文書再編のリンク検査で「切れ0」を確認したのに旧パスが**3回**出た

  | 回 | 数えたもの | 落としたもの |
  |---|---|---|
  | 1 | Markdownリンクの形式だけ | リポジトリ直下（`CLAUDE.md`・`ci.yml`） |
  | 2 | `docs/` 配下 | **コード内のKDoc・コメント10件** |
  | 3 | Markdownリンクと `@` 参照 | **バッククォート内の裸パス** |

  守りたい性質は一貫して「文書への参照」なのに、毎回**記法**で引いていた。
  2回目が落としたものには**常時読み込まれる唯一の設計書参照 `@docs/design/architecture.md`** が入っていた
- **2026-08-05（検査が一度も走らなかった）** — 変異を6件当てて**1件も落ちなかった**。
  Gradle が `docs/` を入力と見なさず `UP-TO-DATE` で飛んでいた。`inputs.dir` で解消
- **2026-08-10（4度目・今回の再編の発端）** — 「change_history へ**1行**追記する」は
  `CLAUDE.md` と当該文書の**2箇所**にあり、**毎PR守られていた**。それでも中央値1,846字・188KBへ肥大した。
  **markdownテーブルの「1行」に長さの上限が無かった** → 「守られたのに壊れる規則」という別の形
- **2026-09-05（規則の置き場所）** — 「`owner/` は実装を変えたついでに直さない」は
  `owner/README.md` にしかなく、**コードを変える側はその文書を開かない。**
  一方 `CLAUDE.md` の影響面監査は `owner/` を正本として名指ししていたので、
  **常時読まれる側の厳しい指示が勝ち**、直近99コミット中7件が同伴更新になっていた。
  **規則は、それを破りうる人が必ず読む文書に置く。**

**検査:** `ReviewFindingsLedgerTest`・`PackageDependencyTest`・`AppColorContrastTest`・
`NoteExcerptThreadingTest`・`NoteSectionThreadingTest`・`InstrumentationTestShapeTest`・
`WipIssueReferenceTest`・`PromptGenerationCoverageTest`

**関連:** [L32](L32.md)（§0 廃止 — この教訓が自分自身に当たった）・[L25](L25.md)・[L30](L30.md)
