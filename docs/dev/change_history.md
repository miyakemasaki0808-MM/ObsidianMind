# 変更履歴表

**プロジェクト:** Vigilith AI（旧 Obsidian Mind）

このファイルはPR単位の変更履歴を新しい順に記録する累積文書である。

- **何を**いつ変えたかはこの表、**今どうなって**いるかは [source_code_analysis.md](source_code_analysis.md)、**なぜそう**したかは [design/](design/) を参照する。
- 運用ルール: PRごとに1行追記する。設計判断・試行錯誤があった変更のみ design/ に対応ファイルを作成または追記し、この表からリンクする。

---

## 履歴（新しい順）

| 日付 | PR | 変更内容 | 設計メモ |
| 2026-08-09 | — | **返事の退避を作り直した（同日中の再設計）＋システムBack対応**。前回入れた退避は**「返事」を1件だけ持つ形で、2つ穴があった**。**① 痕跡の新規作成が失敗した回を復旧できない** — 返事だけでは「既存の痕跡へ載せ直す」ことしかできず、ファイルがまだ無い（初読で返事まで書いた）回がまるごと落ちる。**退避するのは返事ではなく、保存しようとした `ReadingTrace` そのもの**にした。完成済みならファイルが無くても作れる。**② 単一スロットだったのでAが退避中にBで返事を書くとAが消えた** — Vault＋相対パスをキーにした Map（上限8件）へ。退避を消してよい条件も「返事の文字列が一致」から**`Reflection` 全体の一致**へ狭めた（返事が同じでも問い・日時・映し返しが違えば別物）。**テスト側で元ノートの痕跡を手動作成していたため、①が検出できていなかった** — 外したら実際に落ちた。あわせて現セッションの巻き戻しと退避の書き直しが同じファイルへ2回書くのを避け、UI側では**端末の戻るジェスチャー／システムBack**にも未保存確認を効かせ（画面内の「← 戻る」だけ守っていた）、「切り詰めました」の表示をひとことに紐づけて古い問いから持ち越さないようにした | **[reflect_remark](design/reflect_remark.md)** §11.7 |
| 2026-08-09 | — | **「保存ボタンを押した文章は必ず永続化する」を契約として固めた（5件）**。**最も痛かったのは `Held` の離脱保存が失敗したときに再試行できないこと** — `flush()` は保存コルーチンを起動した直後に `session = null` にするため、後から失敗して `owner.pendingRemark` へ戻しても**その owner はもう現役でなく**、次の契機が来ない。セッションではなく Controller が持つ退避スロットへ移し、次の書き込み契機で書き直す（Vault切替では捨てる）。あわせて**保存失敗後に「もう一度保存する」が押せなかった**（失敗時も本文を状態へ残すので isDirty が false になる）、**8,000字超をまだ黙って切っていた**、**未保存の下書きが警告なく消えた**、**入力欄が巨大化した**、**痕跡未作成時の映し返しが永続化されず返事だけ残る**を直した。なお `recordVisit` の失敗分岐にも同じ退避を書いたが、**外しても落ちるテストが無かったので削除した** —返事がセッションへ入る経路が既に退避スロットを立てるため（L11・2026-07-31 の requestId ガードと同じ判断） | **[reflect_remark](design/reflect_remark.md)** §11.7 |
| 2026-08-09 | — | **ひとこと・映し返しの冒頭から「あなた」を外した**。指示として入れていたもので、痕跡の俯瞰要約から持ってきていた。**category の取り違えが2度目** — 俯瞰要約は「あなたは3回開いています」と**読み手自身の行動を述べる**文なので二人称が要るが、ひとことと映し返しは**ノートについて話す**文なので主語に読み手を置く必要がない。二人称で名指しすると**採点者の口調**になり、文体の指針（「不足」「必要」「べき」を避ける）で避けたかったものに戻っていた。**同じ行に「日本語で」と「あなたと呼べ」の2つの判断が入っていたため、言語だけ直して二人称を残した。**指示を外すだけでは小さいモデルが従い切らないので後処理でも剥がすが、**文体の好みで文ごと捨てない** —一般論や問いのように中身が契約違反であるものとは扱いを変える（捨てると空振りが増え、中身は悪くないのに何も出なくなる）。剥がして読点始まりになる等で壊れる場合は元のまま出す | **[reflect_remark](design/reflect_remark.md)** §11.6 |
| 2026-08-09 | — | **ひとことの実機確認4巡目を反映した（長文の扱い）**。**根っこは上限を1本で決めていたこと** — 返事の400文字は `MAX_REPLY_BYTES`（1536）から逆算した値で、その1536は「1文だから」と置いた数字だった。つまり**ユーザーの文章の長さを、AIの都合ですらない思いつきで縛っていた**。本文は既に「保存は原文・AIへは抜粋」でやっているのに、返事だけ両方を同じ数字で縛っていた。**保存8,000字（完全保存）とAI入力400字（先頭＋末尾の抜粋）へ分離**し、2,000字超は**壁ではなく合図**（静かな注記）にした — 壁にすると長く書きたかった回に言葉が消える。**上限超過を黙って捨てていた**のも直した（401字を貼ると入力変更ごと無視され、無反応に見えていた）。**`Held` を「保存済み」と呼んでいた**のも誤りで、まだファイルに無いうえ離脱時の門番を通らなければ本当に消える —表示を4値（未保存/保存中/保存済み/保存失敗）にし、Boolean 2本を `ReplyStatus` へ畳み、下書きとの差も見る。**返事を預かっているときは離脱時の門番（10秒・1ブロック）を通す** — 書いた事実はスクロールより強い関与。長文で保存ボタンが押し出されていたのでボタン列を固定した。あわせて**上限どうしの整合を計算で固定**（保存できたファイルを次回読めなくなる壊れ方は、テストが緑のまま起きる） | **[reflect_remark](design/reflect_remark.md)** §11 |
| 2026-08-09 | — | **ひとことの実機確認3巡目を反映した（返事の保全＋1往復で閉じる＋Rediscover連携）**。**最優先は「返事を失う経路を3つ作っていた」こと** — 前日に「書いた言葉は作り直せない」と書いて即時保存にした直後に、`saveReply` の戻り値を Boolean にして false を「痕跡側が預かっている」と読んでいた。**預かるのは4経路のうち1つだけ**で、Vaultキー無し・保存失敗・例外は何も残らないのに画面へ「保存済み」と出していた。下書きも `remember` で画面回転で消えた。**Boolean へ畳んだのが誤り**で、「書けた／預かった／失った」で呼び出し側の次の行動が違う（L28 は境界の戻り値の話だが、**失敗の種類にも同じ問いが当たる**）。`ReplySaveOutcome` の3値へ分け、Lost は画面へ出して押し直せるようにした。**保存済みの返事へ辿る導線も無かった** — ノート切替で Idle に戻るとAIタブが「もらう」になり新規生成へ進むだけで、**「画面を開いたときだけ読む」判断が入口自体を非Idle状態に閉じ込めていた**。入口を常に専用画面へ向けて解いた。あわせて**「返事をAIへ再送しない」を一部撤回し、1往復だけ許す映し返し（schema v5）**を入れた —**問いを返させないことをプロンプトではなく検査で担保**する（疑問符を含む応答を捨てる）。返事を先に保存してから生成するので、生成が失敗してもユーザーの言葉は残る。Rediscover の再会カードには**1行の手がかりだけ**置き、中身は専用画面で読む（カードが重くなると1文で伝える役目が壊れる） | **[reflect_remark](design/reflect_remark.md)** §10 |
| 2026-08-09 | — | **ひとことの実機確認2巡目を反映した（言語バグ＋返事欄）**。**ソースコードだけのノートで英語の問いが返っていた** — プロンプトが「本文と同じ言語で」だったため。**category の取り違えで、要約はノートの内容を写すので本文の言語に従うのが正しいが、ひとことはアプリがユーザーへ話しかける文なので従うべきは読み手の言語**だった。痕跡の俯瞰要約が先に同じ判断（in Japanese）をしていたのに、旧補記の文言を引き継いでいた。**性質で数え直したらセクションチャットが同じバグ**を持っており（AIピッカーは正しく「リクエストの言語で」と書けている）、こちらも直した。あわせて**「問いのみで終わるのはモヤっとした」**というオーナーの所感から**返事欄**を入れた —`feature_ideas` N-6「ユーザーの言葉を受け取る口が一つも無い」が体験として表に出たもの。**`Reflection`（ひとこと＋返事）の1組で持つ**（schema v4）。別々の文字列にすると片方だけ残る状態を型で作れてしまう。**返事はAIへ再送しない**（往復させると「AIと会話するアプリ」になる）。**返事だけ即時保存**にしたのは、生成物は作り直せるが**書いた言葉は作り直せない**ため。痕跡ファイルが未作成なら離脱時の書き込みへ預け直すので、その場合も失われない。読み返しは専用画面を開いたときだけ（ノート表示の経路にSAF読みを増やさない）。**返事を入れたことで痕跡が「ユーザーが書いたもの」に変わったため、退避手段の不在を課題として起票した** | **[reflect_remark](design/reflect_remark.md)** §9 |
| 2026-08-09 | — | **ひとことの実機確認1巡目を反映した（4件）**。**うち1件は自分の仕様に実装が反していた** — 設計書は「ノート固有の言葉を必ず含める」と例外なしで書いているのに、実装は「実在ノートへのリンクを含むなら根拠検査を免除」を持っており、地の文が丸ごと一般論の「[[X]]と並べると、まったく別の角度から捉え直せるかもしれません」が通っていた。**免除は仕様の追加であって、設計書に無い免除を実装で足すと仕様と実装のどちらが正か分からなくなる。**あわせて根拠は `[[...]]` を除いた地の文で測るようにした。**空振りと書式失敗を畳んでいたのも分けた** — 次の行動（再試行が効くか）が違うため。**表示場所の判断は撤回した** — 「専用画面をやめてパネル1枚にするのが本質」は半分しか正しくなく、撤去すべきは「Markdownファイル全体を開く重い画面」であって、**1文に presence を与えること自体は否定していなかった**。要約→蒸留→ひとことの順で最下段になり、いちばん短い結果がいちばん埋もれていた。1文専用の軽い結果画面（非タブルート）を作り、AIタブは入口ボタン1つへ戻す。実装中に `VibrantTextUsageTest` が発火し、グラデーション直上へ `onVibrant` を置いていたことを検出（見出しは `GradientHeader` へ移した）。**候補はタイトル8件から3件＋本文抜粋へ** — 関連ノートAIが再ランクで既に読んでいたスニペットを通すだけなので追加I/Oは無い | **[reflect_remark](design/reflect_remark.md)** §8 |
| 2026-08-09 | — | **「AI補記メモ」を「ノートへのひとこと」へ作り直した**。出発点は「補記の評価内容がわかりにくい」という実感で、起票済みの3件（4項目の重複・入力を渡して使わせない・空でも保存が通る）を個別に直そうとしていた。**git履歴まで遡ると1本の因果だった** — 出力枠（256トークン）はゼロサムなのに、2026-07-20 の途切れ対策（`f5ec7be`）が可変費である補記3行だけを削り、**分類ラベル4行は実装時（2026-05-31）から1文字も変わっていなかった**。削る対象を価値ではなく削りやすさで選んでいたことになる。そこで枠を1文へ集中させた。分類を全廃し、文体を「不足・必要・べき」から考えを開く問いへ寄せ、関連ノートは候補として実際に使わせる（生タイトルではなくIDを返させ候補集合と照合。蒸留・関連ノートと同じ契約）。**一般論の禁止は指示ではなく検査で担保する** — 原文と4文字以上一致しない応答は捨てる。保存はVaultの`.md`をやめ読書痕跡サイドカー（schema v3）へ移した。**生成直後に単独保存せず痕跡の書き込み契機へ預けるのが要点** — 痕跡は離脱時にしか作られないため、初読の最中に押されるこの機能では「生成できたら保存」だと必ず黙って失われる。結果は専用画面をやめてAIタブのパネルへ出し、未確認管理（`isViewed`）と完了✓・失敗!の塗りバッジが対象ごと消えた（**下部ナビ帯の上で判別できなかった塗りの課題も同時に解消**）。作成経路（`AnnotationFileWriter`・`createAnnotationFile`・`AnnotationComposer`）は撤去したが、**既存の`_AI補記/*.md`を消さない方針なので一覧・削除は残した**（作らないが片付けられる）。実装中に別件を1つ発見 — `trimIndent()`付きテンプレートへ複数行の値を補間すると字下げが全行に残り、**関連ノート・蒸留の既存プロンプトが今その状態にある**（別途対応） | **[reflect_remark](design/reflect_remark.md)**（新規） |
| 2026-08-08 | — | **CIでのエミュレータ実行を再検討し、見送りで確定した。あわせて「件数をトリガーにしない」を教訓へ足した（L31）**。当初は「instrumentation が20件を超えたら再検討」を条件に置いていたが、**同じ日に34件へ到達して発火し、しかも一度「入れる価値が出た」という誤った結論を出させた**。見直した結果は見送り — ①踏んだ2件（Manifest不備の0件実行・`@Test` の戻り値でクラス全件が起動しない）はどちらも**基盤の初期設定ミス**で、後者は既に `InstrumentationTestShapeTest` で再発防止済み。**普段の開発で実行を忘れて流出した実績は1件も無い** ②**最も実機固有な端末AIの9件はCIで守れない**ので、エミュレータが緑でも実機確認が残り確認が二重になる ③無料でも保守（不安定な赤・PR待ち時間・KVM/system image・除外クラス・タイムアウト）が要り、Linux実行にはハードウェア仮想化の設定も要るため「ジョブを1つ足すだけ」は運用としては軽すぎる。**再検討条件を件数から実害へ変えた**（実行忘れで実害が出た／複数人になった／頻度で明確に苦痛になった／配布ゲートが要る）。**件数トリガーの失敗は2度目**で、Controller共通化でも同じ形を踏んでいる — 件数は作った量であって困っている度合いではなく、閾値は必ずいつか超えるので発火自体が理由に見えてしまう。**閾値を書きたくなったら観測か予測かを先に問う** | **[instrumentation_testing](design/instrumentation_testing.md) 判断4**・[lessons](lessons.md) L31 |
| 2026-08-08 | — | **TEST-2 段階4a〜4c — 端末AIの生成・再生成・タブ遷移の instrumentation を10件足した**。**4a（端末AI）で分かったのは、既存の `PromptTokenBudgetTest` が計測しかしておらず `generate()` を本番プロンプトで通す経路が1つも無かったこと** — `maxOutputTokens` の「1〜256」制限で全AI生成が落ちた前例はここが空いていたために本番まで抜けた。**上限に収まることと生成が返ることは別**なので、要約・クイズ・関連ノート・セクション要約の4経路で実際に生成させる。**assert するのは「空でない応答が返る」まで**で、端末AIの出力は非決定的なため書式の一致は assert せず、パーサへ通した結果は logcat へ出して人間が読む材料に留める（パーサについて assert するのは実出力で例外を投げないことだけ）。**4b（再生成）と 4c（タブ遷移）は本番変更が不要だった** — 着手前は `MainActivity` の `by viewModels()` に差し替え口が要ると見ていたが、確かめたい振る舞いはどちらも **Vault 未選択のまま観測できる**ため実依存で起動してよく、「先回りで seam を作らない」判断を維持できた。4b は**同一プロセス内のActivity再生成**（回転・Fold開閉）でオープニングを再生し直さないこと（`showOpening` が `savedInstanceState == null` で決まる）と、再生成を繰り返しても描画が壊れないこと。**プロセス死亡後の復元は覆っていない** — `ActivityScenario.recreate()` は同一プロセス内で作り直すだけで、Application・静的状態・プロセス内キャッシュは生き残る（当初「プロセス復元」と書いていたのを外部レビューで指摘され、同日中に狭めた）。**`VigilithHost` の位置（`rememberSaveable`）の復元は入れていない** — ドラッグと座標の突き合わせが要り、実測のぶれで壊れていないのに赤くなりやすいため、入れるなら観測方法を先に決める。4c で**オプション画面の見出しがタブのラベルと同じ「オプション」で `onNodeWithText` が2件に当たる**ことが分かり、画面内の項目名を目印に使う形にした（画面へ文言を足すときはタブのラベルと衝突しないか見る）。**実機確認済み**（34/34 成功・0 skipped）| **[instrumentation_testing](design/instrumentation_testing.md)** |
| 2026-08-08 | — | **instrumentation 段階1〜3の実機確認が完了した（24/24 成功・0 skipped）。あわせて「CIにエミュレータジョブを足さない」判断を撤回した**。**判断4の再検討条件（20件超）へ同日中に到達した** — 条件を書いた時点の内訳は7件でうち5件が端末AI依存であり、「エミュレータで走らせても大半が skip される」ことが「入れない」の根拠だった。段階1〜3で17件増えて内訳が逆転し、**19/24 が端末AI非依存で実際に走る**（SAF・`BitmapFactory` 13件、Compose の実測4件、土台の疎通2件）。しかも走る側は実配線でしか壊れが出ないものばかりで、ゲートに載せる価値が最も高い。**根拠が消えたので判断を維持しない**が、**端末AI依存のクラスは対象から外す**（通しても skip で緑になるだけでCI時間だけ増える）。CI時間の増加を払うかは技術的当否ではなくオーナー判断なので、実装は保留 | **[instrumentation_testing](design/instrumentation_testing.md)** |
| 2026-08-08 | — | **TEST-2 段階3 — `NoteImageGateway` を実物のSAF・実物の `BitmapFactory` で通す instrumentation を7件足した**。**IMG-3 のレビューで唯一満たせず TEST-2 へ寄せた受理条件**（実物の Gateway で `TooLarge` と `Broken` を分ける）を埋めるもの。純関数 `imageDecodeFailureFor` はJVMで固定済みだったが、**Gateway がその判定を正しい材料で・正しい位置から呼んでいるか**は覆えていなかった。**最初に書いた「16MiBのゼロ列を食わせる」テストは無意味だと分かって書き直した** — `BitmapFactory` はヘッダを読んだ時点で画像でないと判断して読むのをやめるため、`BoundedInputStream` が上限に到達せず境界を1バイトも通らない。`truncated` が立たないので結論は `Broken` になり、**修正前の実装でも同じ結果**＝退行を検出できない。代わりに**非圧縮の24bit BMP**を組み立てる形にした（寸法からファイル長が決まり、復号器が画素データを最後まで読むので上限の内外を狙える）。2400×2300＝16,560,054バイトは上限内で復号成功、2400×2400＝17,280,054バイトは上限超で `TooLarge`。**サイズを申告しないプロバイダとして置くのが要点** — メタデータ判定（`size > MAX_INPUT_BYTES`）を素通りさせないと `BoundedInputStream` 側の判断まで届かない。偽プロバイダに `COLUMN_SIZE` を返さない切り替えとバイナリ配置を足した。**「復号器がどこまで読むか」を意識しないと境界のテストは空振りする** — 入力を大きくしただけでは上限に触れない。**実機実行は未実施** | **[instrumentation_testing](design/instrumentation_testing.md)** |
| 2026-08-08 | — | **TEST-2 段階2 — テスト用 `DocumentsProvider` を `src/debug/` へ置き、実物のSAF経路を通す instrumentation を6件足した**。`SafVaultBrowser` と `NoteRepository` を**本番のまま**動かし、相手だけを偽プロバイダにする形。**`androidTest` ではなく `src/debug/` へ置くのが要点** — `androidTest` 側は別APK・別UIDになるため tree URI のたびにURI権限の付与が要る。debug ソースセットならアプリ本体と同じUIDなので `DocumentsContract.buildTreeDocumentUri` の結果をそのまま渡せる（**release には入らない**）。**読取失敗は例外ではなく「null カーソル」で作る** — `querySafChildren` が `contentResolver.query(...) ?: SafChildren.UNREADABLE` の形なので、実物の失敗はこれであり、例外にすると本番が通らない経路を試すことになる。これで**「ノートが無い」と「そのフォルダを読めなかった」の区別**を実配線で確かめられるようになった（TRACE-1・TRACE-2 で純関数側は固めたが、`NoteRepository` から `VaultScan` までは手動確認しか担保が無かった）。**`isChildDocument` の override が必須**だった — tree URI 配下の入れ子で `DocumentsProvider.enforceTree()` が呼ぶため、無いとルート直下しか読めない。実体はシステムが生成しテストから掴めないので、木の組み立てと失敗注入は `companion` の API に置いた。内容は入れ子ノートの相対パス収集・読取失敗フォルダの不完全扱い・ルート読取失敗でのVault全体不完全・第一階層フォルダの名前順・補記の作成/一覧/削除・同名補記の実名一致の6件。**実機実行は未実施** | **[instrumentation_testing](design/instrumentation_testing.md)** |
| 2026-08-08 | — | **TEST-2 に着手し、読書画面の instrumentation を4件足した（段階1）。あわせて規模感を「大」から「中」へ改めた**。着手前に実物を当てたところ、**「大」は前提が無かった頃の見積もりのまま**だったことが分かった — ①instrumentation の稼働は 2026-08-01 に実証済み ②`VaultBrowser` が**インターフェース**になっている ③`androidTest` から `internal` が見える（既に `selectDistillCandidates` 等で利用中）④`ActivityScenarioRule` と `ui-test-manifest` が稼働中。**段階1は本番コードの変更ゼロで書けた** — `NoteReaderTab` / `FullscreenNoteScreen` が `NoteUiState` と `LazyListState` を素の引数で受け取るため、ViewModelもFakeも組まずに画面を描ける（UIに業務ロジックを置かない規約の副産物）。内容は「解析結果が届くまで本文を描かない」（描くと `precomputedBlocks ?: parseMarkdownBlocks(content)` のフォールバックがMainで解析し直す）・「解析後は描く」・「全画面がタブ側のスクロール位置を引き継ぐ」・「読書進捗が総ブロック数と整合する」の4件。**可視位置は `LazyListState` の内部値ではなく本文の文言で判定する** — `ReadingProgressReporter` が `visibleItemsInfo.last().index` をそのままブロック番号として報告しているため、本文を「段落0」「段落1」…と並べれば画面に出ている文字で特定できる。**`MainActivity` 経由では依存を差し替えられない**（`by viewModels()` でデフォルトファクトリ直取り）ので画面コンポーザブルを直接 `setContent` した。**ViewModelFactory の seam は先回りで作らない**（使い道が確定していない抽象を本番へ足すことになる）。**CIにエミュレータジョブは足さない判断**もした — 端末AI依存のテストはエミュレータで `Assume` skip され緑になるだけで何も守らず、CI時間の増加に見合わない。代わりに「PR前にAndroid Studioで実行する」を運用とし、**instrumentation が20件を超えたら再検討**という条件を置いた（運用が検査に劣ることは承知の上）。**実機確認済み**（2026-08-08・Pixel 10 Pro Fold / Android 17 で **11/11 成功・0 skipped**、段階1の4件は初回から緑で修正不要）。**副産物として端末AIのテストが `Assume` で飛ばされずに実行された**（`PromptTokenBudgetTest` 5/5 — この端末では Nano が `AVAILABLE`）が、CIのエミュレータでは skip されるという判断の前提は変わらない | **[instrumentation_testing](design/instrumentation_testing.md)** |
| 2026-08-05 | — | **受付漏れ検査の抜け道・遮断器の包含判定・上限つきストリームの境界を直し、外部レビュー後に3件をクローズした**。**①受付漏れ検査**（`ReviewFindingsLedgerTest`）— 受付IDが日付部分だけで、同じ日に2本目のレビューを置くと別々の指摘が同じIDへ潰れ、受付簿を更新しなくても検査が通った。IDをファイルstem全体に変え、見出し側・受付行側それぞれの重複を明示的に拒否する。`起票` の参照先も `none` から**すべて検証**へ変え、あわせて `ISSUE_ID_PATTERN` を英字始まりに絞った（`[A-Z0-9]+-\d+` は処遇欄の日付「2026-08」にも当たり、全件検証にすると日付入りの起票行が落ちる）。**導入時の変異検証6件が全部落ちたのに欠陥が残っていた理由は「変異がすべて既存データを壊す形だった」こと** — 新しいデータを**足す**形を試していなかった。実際、`none`→`all` の変更は混在データを足したときにしか差が出ない。**②遮断器の包含判定** — `isDescendantFolder` はルートを全パスの祖先として扱うのに `breakerGroupPaths` はルートを祖先として数えないため、`blocked` にルートと `ideas` が同時に入ると `ideas` が「より浅い」に負けて消え、しかし `ideas/` 配下の候補は `shallowest` と一致せず**保留されずに孤児候補として出ていた**。包含判定を `breakerGroupPathsOfFolder` へ寄せ、グループの定義と覆う範囲が構造的にずれない形にした（条件を1つ足す直し方は、同じ意味の判定が2つ残るので採らない）。**③上限つきストリーム** — 上限到達だけで打ち切り扱いにしていたため、`size > MAX_INPUT_BYTES` だけを拒否する契約（**上限ちょうどは許可**）と食い違い、16MiBちょうどの正常な画像が `TooLarge` へ化け得た。上限に達したら1バイト先読みして真の超過だけを立てる。**外部レビューで、その先読みを「次に読もうとした時」だけに置いたのが穴だと指摘された** — `skip()` で末尾まで進めて read せずに戻ると超過が観測されない。先読みを `settleOverflowAtLimit()` に切り出し、read・配列read・skip の**全経路が上限到達の時点で呼ぶ**形にした。**テスト自身が欠陥を隠していた点も直した**（旧 skip テストは直後に `read()` を呼び、その read が先読みを肩代わりしていた）。`InputStream` の契約違反2件（`len == 0` が `-1`、`available()` 無丸め）も同時に修正。`TooLarge`/`Broken` の切り分けは純関数 `imageDecodeFailureFor` へ寄せ、`readBounds` と `decode` の両方から使う（`BitmapFactory` は打ち切られたストリームを「復号できなかった」としか言わないので、null を先に見ると大きすぎる画像がすべて `Broken` に化ける）。**`NoteImageGateway` を実物で通すテストは作らなかった** — `BitmapFactory`・`ContentResolver` 直結で、Robolectric もモックライブラリも無いため JVM から動かせない。**1機能の都合でテスト戦略を変えない**と判断し、不足は TEST-2 へ寄せた（オーナー判断）。変異検証は計10件（足す型3・戻す型7）。JVMテスト **747件成功**（+33）、Lint 0 error / 0 warning | [lessons](lessons.md) L11・L14 |
| 2026-08-05 | — | **TRACE-1 の実機確認をオーナー判断で省略し、課題台帳からクローズした**。判定ロジックは純関数のJVMテストで覆えており、UI変更も文言追加だけなので、実機でしか確認できない要素が無いと判断した。`current_issues.md` から TRACE-1 の項目を削除し、`docs/review/findings.md` の対応する受付行の処遇を `起票 TRACE-1` から `解消` へ更新した（`ReviewFindingsLedgerTest` が参照先の実在を検査するため、台帳から消すと同時にこちらも直す必要があった） | — |
| 2026-08-05 | — | **TRACE-1 を修正した — 削除直前の再確認を三値化し、生きた読書痕跡を消し得る経路を塞いだ**。判定が `stillMissing: Boolean` だったため、結果が `DELETED`/`NOT_ORPHAN_ANYMORE`/`FAILED` の3つへ潰れ、**「確かめられなかった」を表現できていなかった**。実害は2つ — ①`isUnderUnreadableFolder("ideas", setOf(""))` が **`"ideas".startsWith("/")` を満たさず偽を返す**ため、ルートが読めずノートが1件も見えない状態でも「まだ無い」と判定して**削除した**（ルート直下の候補だけ偶然守られていた）②通常のサブフォルダ読取失敗は削除こそ止まるが `NOT_ORPHAN_ANYMORE` へ畳まれ、**候補が一覧から消えて再試行できなかった**。**洗い出し側は同じ状況を `Blocked` にできており、同じ意味の判定が2箇所にあって片方だけ強い状態だった**ので、安全判定を純関数 `notePresenceAfterRescan`（`PRESENT`/`MISSING`/`INDETERMINATE`）へ一元化した。ルートは全パスの祖先として `isUnderUnreadableFolder` の中で特別扱いする（区切りを足す比較では表現できない）。画面では**「消せなかった」と「確かめられなかった」を別の文言・別の件数にする** — 原因も次の行動も違う（前者は再試行、後者は同期の完了待ち）。**これは L28 の5度目で、しかも同じ機能の中だった** — 効いた見分け方は分岐の数ではなく「**同じ意味の判定が2箇所にあるとき両方が同じ強さか**」で、弱いほうは常に後から書いたほうだった。**変異検証4件**（ルートの特別扱い・不在確認の順序・INDETERMINATE の畳み込み2種）でいずれも対応テストが落ちることを確認済み。**受付簿の検査が実運用で1回効いた** — 台帳から TRACE-1 を消した時点で `起票 TRACE-1` が宙に浮き、`ReviewFindingsLedgerTest` が落ちて処遇の更新漏れを検出した。JVMテスト **727件成功**（+9）、Lint 0 error / 0 warning。**実機確認は未実施** — 判定は純関数でJVMテストが覆っておりUI変更も文言追加だけなので「不要」と考える余地はあるが、**要否はオーナー判断**であり、実装完了で台帳から消さないのが規約なので課題は残してある | **[reflect_reading_trace](design/reflect_reading_trace.md)**・[lessons](lessons.md) L28 |
| 2026-08-05 | — | **§0 の1回目の判定を記録し、L29 を追記した**。**判定は「発火しなかった」** — 画像表示の段階1〜5で毎回 §0 を当てて報告へ1行残したのに、レビューが見つけた P1「確保した高さを成功経路へ渡していない」は**問4（同じ変更を2箇所目に書いた）がそのまま当たる形**だった（3つの表示状態のうち2つへ `heightDp` を渡し、3つ目を落としている）。**問いの言い回しではなく、当てたつもりで当たっていなかった。** §0 自身の指示に従い、**文書を足すことを主な対策にしない**と明記し、残り2回で同じ結果なら廃止を検討すると書いた。**L29 は3つの実例を1本にまとめたもの** — ①起票の規則が `CLAUDE.md` と `document_map` の2箇所にあって守られなかった（総評を貼るコミットが「既存内容への変更はない」を目標にしており、**自分の変更に見えない作業ではチェックリストが発火しない**）②文書再編でリンク検査を書きながら旧パスを3回に分けて落とした（記法で引いて**性質**で引いていない。3回目に落としたのは常時読み込まれる唯一の設計書参照）③受付簿の検査を入れた直後、Gradle が `docs/` を入力と見なさず**一度も走らない**状態だった。運用は「規則を書いたら守られなかったことをどう検出するか同じコミットで決める」「検査を書いたら数えていない経路を先に3つ挙げる」「検査を入れたら必ず1回変異させる」の3つ。**本節は守らせる力を持たない**ことも明記した | **[lessons](lessons.md) §0・L29** |
| 2026-08-05 | — | **文書を owner / dev / review へ再編し、レビュー指摘の受付簿と受付漏れ検査を入れた**。分類の軸を「読み手」ではなく**文書の役割**で定義した（担当するAIが変わると読み手ベースの定義は崩れる）。`owner`＝プロダクトの説明・`dev`＝作るための知識・`review`＝評価とその追跡。**`_wip/` だけは役割ではなく寿命（リリース時に丸ごと捨てる）で切ってあるので据え置いた** — この性質は他のどのフォルダでも表現できない。913行あった品質レビューを日付別4本へ分割し、`review/README.md` を索引にした。**分割で指す先を失うメタ記述（「上に保存」「ここから下」「本節」）だけ最小修正し、評価・指摘・根拠・結論は1行も変えていない**（消えた行・増えた行を全件照合済み）。**受付簿（`review/findings.md`）は過去4本の全21件を遡って埋めた** — 処遇は `起票`/`統合`/`解消`/`見送り`/`誤検知` の5語に限定し、`起票` は実在する課題IDを併記する。**内容は持たない**（根拠は日付別レビュー、進行中の対応は課題台帳が持つ。両方に書くと必ず片方が古くなる）。**検査は `ReviewFindingsLedgerTest` が行う** — スクリプトにすると「走らせる」という手動契約が新しく生まれ、同じ罠を1段ずらすだけになるのでJVMテストにした。**導入直後の変異検証で、変異が1件も落ちない穴が判明した** — Gradle は `docs/` をテストの入力と見なさないため、**文書だけを直したときにテストが UP-TO-DATE で飛ぶ**。`inputs.dir` で明示して解消し、6変異すべてで落ちることを確認した。**再編中に3度、同じ見落としをした** — 「文書への参照」を性質ではなく記法（Markdownリンク → `@` 参照 → バッククォート内の裸パス）で数えていたため、リポジトリ直下のファイル・コード内のKDoc・裸パスを順に落とした。最後は記法を問わず文字列で全走査して残存0。**移行前から壊れていたリンクも1件見つかった**（`MarkdownBlocks.kt` の `../` 不足）。Markdownリンク576本・コード内の文書参照10件とも解決し、表示名と実体の食い違いも0。JVMテスト **710件成功**、Lint 0 error / 0 warning | **[review/README](../review/README.md)**・[document_map](document_map.md) |
| 2026-08-02 | — | **N-3 の外部レビュー指摘7件を修正した（P1が3件）**。**①成功経路で確保した高さが失われていた** — プレースホルダと失敗表示には確保した高さを渡していたが、復号できた画像は幅だけを指定していたため、最低高さで留めた分（＝横長の画像）が**成功した瞬間に縮み**、後続ブロックが繰り上がって到達率が水増しされる。3つの状態すべてに同じ高さを渡す形へ直した。**②寸法が分かるまでの固定高さが小さすぎた** — 160dp 固定だと索引走査とヘッダ読みの間だけ後続ブロックが画面に入り、最深到達点は下がらないのでその一瞬の誤りが固着する。**画面の高さを確保する**形にした。**確保の誤りには向きがある** — 大きすぎる確保は到達率を低く見積もるだけで正しい値へ追いつくが、小さすぎる確保だけが誤りを固着させるので、**分からない間は大きい側へ倒す**。**③要求元のキャンセルでキャッシュの記帳が飛んでいた** — 「待って、返ってきたら予算へ計上する」形だと、待っている間に要求元がキャンセルされる（＝画像を素早くスクロールする）と取り下げも計上も飛び、結果は実行中のまま残って**予算の外で生き続け、追い出しの対象にもならない**（失敗なら以後ずっと同じ失敗が返る）。**記帳を読み込み側へ移した** — キャッシュの整合を要求元の寿命に依存させてはいけない。修正前のコードで**2件のテストが落ちること**を先に確認してから直した。**④入力バイトの上限が実質無効だった** — `COLUMN_SIZE` を返さないプロバイダでは照会が null になり素通りする。上限つきストリームで包んで実際に読んだ量で打ち切り、**打ち切りを別に持って理由を「大きすぎる」へ直す**（`BitmapFactory` は途中で終わったストリームを「復号失敗」としか言わない）。`measure()` 側にも同じ照会を足した。**⑤外部URLと空の参照でも全走査が走っていた** — どちらも Vault を見ずに結論が出るので短絡させた（外部画像しか無いノートを開いただけで全走査が走る状態だった）。**⑥同じファイルを上書きしても古い画像が残った** — 鍵が参照と表示幅だけで、参照は上書きで変わらない。索引が走査時に運ぶ**更新日時を鍵へ含めた**。**⑦読み上げと失敗文が原文の参照先をそのまま出していた** — `![[zu.png\|400]]` が「zu.png\|400」、リンク記法はフォルダ込みのパスになる。既存の `imageRequestOf` を借りて最終ファイル名を出す（記法によって `\|` の意味が違うので独自に切ると必ずずれる）。**変異検証で自分のテストの穴が1件見つかった** — 世代の伝播を確かめるテストが**ファイル名一致の枝しか通っておらず**、完全パス一致の枝が無防備だった（照合の2つの枝それぞれで確かめる形に直した）。JVMテスト **714件成功**（+8）、Lint 0 error / 0 warning、`assembleDebugAndroidTest` 成功。**段階1〜5 の実機確認は完了済み**（レビュー指摘の修正は実機確認の後に入れたため、再確認が要る） | **[note_image_rendering](design/note_image_rendering.md) §6・§7・§8・§9** |
| 2026-08-02 | — | **N-3 段階5「描画」を実装し、画像が画面に出るようになった（実機確認待ち）**。`ui` は依存の規約上 `data` を import できないので、読み込み口 `NoteImageLoader` を `ui` で宣言し、`data` の `NoteImageGateway` を包むアダプタを**ルートパッケージ**へ置いた（`ui` と `data` の両方を import できるのはそこだけ）。**§6 の高さ確保が読み込み口の形を決めた** — 寸法を中身より先に知る必要があるため、`measure`（ヘッダだけ読む）と `load`（復号）の2段に分けた。1本にすると高さが決まるのが中身の到着と同時になり、それまでを高さ0で埋めることになる（＝読書痕跡の到達率が水増しされ、最深到達点として固着したまま永続化される）。**縦横比どおりに確保するので復号が終わってもレイアウトが動かない**。極端に横長な画像では比どおりの高さが1dpを割るので最低高さで下から留める。寸法は `load` の中でももう一度読むが、**共有しないのは `load` がキャッシュから返る場合はそもそも読まないため**（経路をまたいで寸法を持ち回す仕掛けのほうが、ヘッダ1回ぶんより高くつく）。失敗は**枠＋記号＋理由の文**で出し、`ui_design_principles` §1 へ **WCAG 1.4.1（色の使用）の行を新設**した — 既存の4行はすべてコントラスト比の話で、「色が唯一の手がかりになっていないか」は別の基準である。判定は「その色を灰色にしても伝わるか」で行う。**alt が空なら読み上げでファイル名を読む**（ビューアなので画像は装飾ではなく内容で、読み飛ばすと「そこに何かある」ことすら伝わらない）。**`imageLoader` を null 許容にした**ので、補記結果の画面など画像を持たない文脈は原文のまま描いて既存の見た目を保つ。**変異検証5件**（寸法不明時の高さ0・最低高さの下限・高さ計算の桁溢れ・不在と確認不可の文面分け・alt空の読み飛ばし）でいずれも対応テストが落ちることを確認済み。**うち1件は最初の変異が落ちず、テストが Int の範囲内でしか溢れを試していないことが判明した**ので、関数の契約の水準（呼び出し側の寸法チェックに依存しない）で試す形に直した。JVMテスト **706件成功**（+14）、Lint 0 error / 0 warning、`assembleDebugAndroidTest` 成功。**実機確認は未実施**（§11 の確認項目を一巡する必要がある） | **[note_image_rendering](design/note_image_rendering.md) §6・§9**・**[ui_design_principles](design/ui_design_principles.md) §1** |
| 2026-08-02 | — | **N-3 段階4「復号とキャッシュ」を実装した（見た目は変えない・実機確認不要）**。`BitmapFactory` の二段復号（`inJustDecodeBounds` → `inSampleSize`）を `NoteImageGateway`（data）に置き、方針（間引き倍率・拒否条件・復号可否）は Android 非依存の純関数として `domain/image/ImageDecodePolicy.kt` へ分けたのでJVMで検証できる。**minSdk 26 のため `ImageDecoder`（API 28+）は使えず、二段復号は省略できない**（12MPの写真を原寸で展開すると ARGB_8888 で 48MB）。**SAFのストリームは2回開く**（`reset()` できる保証がない）。**上限は4つを別々に持つ** — 入力バイト・寸法・復号後ピクセル数・LRU総バイト。ファイルサイズ1つでは画像爆弾を防げない（圧縮率の高い小さなファイルが巨大な寸法を宣言できる）。**間引きは2段階** — 表示幅まで落としたうえで、なおピクセル数の上限を超えるならさらに落とす。2段目が要るのは極端な縦横比のため（幅1,000・高さ200,000 は表示幅基準では間引かれないのに復号すると数億ピクセルになる）。ピクセル数は `Long` で数える（`Int` だと 46,341 四方で溢れて負になる）。**認識できる拡張子と復号できる拡張子を分けた** — SVG を索引で引き当ててから復号に失敗させると「壊れています」と出るが、正しくは「形式が非対応」なので、ファイルを開く前に分岐する。`heic`/`heif`/`avif` は端末次第で失敗するが**静的には落とさない**（復号できる端末では出せるので、判定は実際に試した結果に任せる）。**失敗の語彙を `model/NoteImageFailure`（8種）に統一し、`ImageResolution` を `Resolved` / `Failed(reason)` へ寄せた** — 解決の失敗と復号の失敗を別の型にすると、表示側が2つを突き合わせることになり同じ意味の分岐が2箇所へ散る。**`ByteBudgetCache`（バイト単位LRU＋single-flight）は既存の `KeyedMemoCache` と共通化しなかった** — 追い出しの単位（件数/バイト）と同一キー同時要求の扱い（両方走る/1回だけ）が違い、`KeyedMemoCache` の利用側は要素の大きさが揃っていて重複キーも出ないため、バイト計上と `Deferred` 管理はコストにしかならない。**共通化の判断は「形が似ているか」ではなく「制約が同じか」で決める**（Controller で2度出した結論と同じ）。**追い出しで `recycle()` しない**（描画中の Bitmap を壊すと落ちる）ので、キャッシュは値の型に何も要求しない汎用実装にでき、そのままJVMで検証できる。**単体で予算を超える値も入れた直後は残す**（落とすと「入れた直後に無い」が起きて呼び出し側が無限に読み直す）。失敗は復号側でもキャッシュしない（寸法読みで失敗するので安く、載せると一時的な失敗がVault切替まで固定される）。**テストの期待値が3件誤っていた**（間引き倍率・追い出し後のバイト数・入れ子 `runTest`）ので実装を確認して直した。**変異検証7件**でいずれも対応テストが落ちることを確認済み。JVMテスト **692件成功**（+21）、Lint 0 error / 0 warning | **[note_image_rendering](design/note_image_rendering.md) §8・§9** |
| 2026-08-02 | — | **N-3 段階3「画像索引」を実装した（見た目は変えない・実機確認不要）**。`traverseMarkdownPaths` を受理条件つきの `traverseVaultFiles` へ一般化し（`MarkdownScan`/`MarkdownEntry` → `VaultFileScan`/`VaultFileEntry` へ改名）、`traverseMarkdownPaths` は `::isMarkdownFile` を固定した別名にした。**歩き方（BFS・除外・循環対策・完全性の記録）はノートと画像で完全に同じで、違うのは受理条件だけ**だったため、走査を2本書くと片方だけ直す形の失敗（L14）が確実に起きる。**一般化の前に等価性テストを置いた** — 同じ木に対して特化版と一般版が同じ結果を返すこと、受理条件を変えると画像だけが取れること、読めなかったフォルダの報告が受理条件によらないことの3件。`VaultImageIndexStore`（data）が索引のキャッシュと解決を持つ。**解決までストアが持つのは、再走査の規則がキャッシュの規則だから** — 呼び出し側へ配ると歯止めを書き忘れた経路が必ず出る。**歯止めは「解決に失敗し、かつ索引がTTL（60秒）より古いときだけ1回」**。無条件に再走査するとリンク切れの画像1つで再コンポーズのたびに全走査が走り、逆に再走査しないとObsidian側で画像を足してもVault切替まで出ない。再走査してもなお見つからなければ読込時刻を更新するので、壊れたリンクが再走査を誘発し続けることもない。**設計書の「不完全な索引はキャッシュしない」を撤回した** — この規則は「無い」と断定させないためのものだが、**段階2で `Unverifiable` を型として分けた時点でその保証は型が持っている**。禁じたままだと読めないフォルダが1つあるだけで**画像1枚ごとに全走査**が走る（`_ReadingTraces` の索引が「次回読み直し」で済んでいたのは再取得がノートを開くたび1回だからで、画像はその前提を満たさない）。**同じ型の失敗を避ける規則でも、避け方が2つあるなら安いほうを採る。** `invalidate()` は書いたうえで削除した — **世代照合が既に同じ仕事をしており、Vault切替で呼ぶ相手が存在しなかった**（2026-07-31 に requestId ガードを冗長と判断して削除したのと同じ）。除外は `_AI補記` / `_ReadingTraces` / `.obsidian` の3つ。**変異検証10件**（TTLの歯止め・miss再走査・世代照合・走査失敗の扱い・完全性の伝播ほか）でいずれも対応テストが落ちることを確認済み。JVMテスト **671件成功**（+15）、Lint 0 error / 0 warning。**索引の除外フォルダだけはJVMテストで担保できない**（`ContentResolver` を要するため実機確認側） | **[note_image_rendering](design/note_image_rendering.md) §2・§7** |
| 2026-08-02 | — | **N-3 段階2「パス正規化と照合規則」を純関数として実装した（見た目は変えない・実機確認不要）**。`domain/image/` に `imageRequestOf`（リンク→解決要求）と `resolveImage`（要求×索引→結果）を置き、Android型を持たないのでJVMテストで全経路を検証できる。**正規化は索引を作る側にも同じ関数を通す** — 片方だけ通すと `a//b.png` のような揺れで同じファイルが別のキーになり永久に一致しない。**`java.net.URLDecoder` を使わない**（`+` を空白へ変換するクエリ文字列の規則で、パスに当てると `a+b.png` が壊れる）。自前のデコーダは**非 `%` 区間をまとめて符号化する** — 1文字ずつUTF-8へ変換すると絵文字を含むファイル名でサロゲートペアが割れる。**サイズヒントを落とすのは埋め込みだけ**（`![alt\|400](path)` では `\|` が alt 側に付くので target には現れない。一律に落とすと対象を誤って切る）。外部URL判定は `://` を要求してWindowsのドライブレター（`C:/...`）を誤検出しない。**設計書に無い判断を1つ足した — 「見つからない」を2種類に分ける。** 索引が不完全なとき（走査で読めなかったフォルダがある）に「Vaultにありません」と言い切るのは、§7 が索引をキャッシュしない理由と同じ誤りを表示側で犯すことになるので、`NotFound`（完全な索引で不在）と `Unverifiable`（断定できない）を型で分けた。**完全性を見るのは見つからなかったときだけ**でよい（見つかったなら不完全でも答えは正しい）。**変異検証で自分のテストが2件弱いことが判明した** — 「索引側の正規化」と「照合側の小文字化」はどちらも**ファイル名フォールバックに救われて**変異させても落ちなかった。同名を2件置いてフォールバックを塞ぐ形と、**要求側が大文字の場合を索引側と分けて置く**形に直して、最終的に**変異10件すべてで対応テストが落ちる**ことを確認した。JVMテスト **656件成功**（+28）、Lint 0 error / 0 warning | **[note_image_rendering](design/note_image_rendering.md) §3・§4** |
| 2026-08-02 | — | **N-3 段階1「単独行の画像をブロックとして解析する」を実装した（見た目は据え置き・実機確認不要）**。`MarkdownBlock.Image(alt, target, isEmbed)` を足し、`![alt](path)` と `![[img.png]]` を単独行のときだけブロック化する。**`target` は原文のまま持つ**（サイズヒント `\|400`・パーセントエンコード・`./` を落とさない）— 正規化は解決側の関心事で、解析側で畳むと往復が原文へ戻らなくなる。**wiki埋め込みは画像拡張子を持つものだけ受理する**（`![[note]]` は他ノートの埋め込みという別機能）。判定に使う値だけ `\|` の前を取るが、**リンクの別名（`[[note\|別名]]` は後ろ）と前後が逆**なので既存の `inlineMarkdown` の `last()` は流用できない。**リンク記法には拡張子を要求しない** — 外部URLは「ネットワーク権限が無いので出せない」という理由を出すために画像として認識されている必要がある（拡張子で落とすと理由が「見つかりません」へすり替わる）。**実装して初めて分かったのは、正規表現の最長一致が誤認を作ること** — `.*` のままだと `![図](zu.png) と説明(補足)` の対象が `zu.png) と説明(補足` に、`![[a.png]] ![[b.png]]` の対象が `a.png]] ![[b.png` になる。**どちらも往復テストは通る**（原文どおり復元されるため）ので緑では気づけない。対象から `[` `]` `)` を除外し、**曖昧な行は段落へ倒す**（段落は変更前の扱いなので、外した場合の挙動が現状維持になる）。**空行を挟まない画像行が段落を切るようになった** — 実際のノートに普通に現れる書き方で、吸収すると画像として描けないため。これによりAI入力は `説明 ![[zu.png]]` → `説明\n\n![[zu.png]]` と変わる（**「無回帰」と言えるのはパスの扱いについてであって出力が1バイトも変わらない意味ではない**ので設計書へ明記した）。`blocksToMarkdown` と描画は共通の `sourceText()` を使い、2箇所で別々に組み立てて片方だけ直る形を避けた。**変異検証5件**（拡張子ガード・段落の区切り・サイズヒントの切り落とし・忠実復元・最長一致の除外）でいずれも対応テストが落ちることを確認済み。JVMテスト **628件成功**（+16）、Lint 0 error / 0 warning | **[note_image_rendering](design/note_image_rendering.md) §5・§10・§11** |
| 2026-08-02 | — | **N-3（ノート内画像の表示）の設計を確定した（実装は未着手）**。**難所は描画ではなくパス解決だった。** Obsidian の既定は wikilink 埋め込み（貼り付け画像は `![[Pasted image ....png]]`）で、**この形はファイル名しか持たないためディレクトリを1段ずつ辿る方式では原理的に解けない**。したがって **Vault横断の画像索引1本**で解き、「まずwalk、外れたら索引」の二段構えは採らない（索引が必ず要る以上、walk は純粋な追加コスト）。走査の骨格は `traverseMarkdownPaths` が既に持っている（**全フォルダを歩いて `.md` 以外を捨てているだけ**なので受理条件を引数へ出せばよい。孤児掃除が依存しているため等価性テストを先に置く）。**照合は「正規化した完全パス一致 → ファイル名一致」の2段とし、ノート相対は v1 に入れない** — 既定設定では相対パスが生成されないうえ、`NoteState.Success` は相対パスを持たず `openNote` 経路では表示後に非同期確定して**読書痕跡のセッションにしか渡らない**ため、通すと「画像だけ遅れて解決し直す」挙動を表示経路へ持ち込む。入れなくても**ファイル名一致が大半を救う**（索引を持つ以上この段は無料）。**同名が複数あるときは解決せず候補数を出す**（それらしい答えを1つ選ぶと誤りが誤りとして見えない＝L24 と同じ向き）。**AI入力は無回帰とし、`blocksToMarkdown` は `![alt](path)` を忠実に復元する** — 着手前の検算で **feature_ideas の「alt が抜粋から丸ごと落ちている」が誤り**と判明した（単独行の画像は段落として原文のまま往復するので alt は既に届いており、パス文字列まで一緒に予算を食っている）。パスを落とすにはレンダラ2本が要るが、利得は予算超過ノートで画像1つあたり数十文字にとどまり、**ai_input_excerpt §14 が「予算を600→800にしても品質は動かない」と実測済み**なので払わない。**最大の発見は読書痕跡の汚染経路**（レビュー指摘には無かった） — `visibleFractionOfBlock` は **`itemSize <= 0` を「全可視」として扱う**ため、読み込み待ちで高さ0の画像ブロックを置くと①そのブロックが「全部読んだ」と報告され②一度に入るブロック数が増えて `deepestBlockIndex` が先へ進み③**「最深到達点は下げない」規則で水増しが固着し永続化される**。よって**プレースホルダの高さ確保は「安全のため」ではなく必須要件**で、寸法取得に失敗しても高さ0のブロックを作らない。索引は Vault 単位なので**ノート単位の契約2箇所には登録せず**、`vaultGeneration`（`private set` だが外から読めるので既存の `() -> Long` と同じ配線）＋ TTL で管理し、**miss時の再走査には「索引がTTLより古いときだけ1回」の歯止めを置く**（無条件だとリンク切れ1つで再コンポーズのたびに全走査が走る）。**不完全な索引はキャッシュしない**（SCAN-1 と同じ型）。復号は minSdk 26 のため `BitmapFactory` 二段固定で、上限は**入力バイト・寸法・復号後ピクセル・LRU総バイトの4つを別々に**持つ（ファイルサイズ1つでは画像爆弾を防げない）。**EXIF回転・SVG・アニメーション・インライン画像・外部URLは非対応**として理由つきで記録（外部URLはネットワーク権限が無く構造的に不可能）。ローダの戻り値は `ImageBitmap?` ではなく `Success` / `Failure(reason)`（null では6種類の失敗を区別できず「理由を出す」要件と型が矛盾する）。着手時に `ui_design_principles` §1 へ **WCAG 1.4.1** の行を足す。**コード変更なし** | **[note_image_rendering](design/note_image_rendering.md)（新規）**・[markdown_rendering](design/markdown_rendering.md) |
|------|----|----------|----------|
| 2026-08-02 | — | **ノートの放置期間に応じて本文の紙を生成り色へ寄せる演出を追加した（既定オフ）**。Rediscover の再会演出強化案のうち、アニメーションで驚かせる案ではなく**佇まいで時間差を伝える**別案。**SAF は作成日を持たない**（`COLUMN_LAST_MODIFIED` だけ）ため、意味を「作成からの経過」ではなく**「最後に手を入れてからの経過」**と読み替えて `NoteFile.lastModified` をそのまま使う — 手入れしたノートが白へ戻るのは手触りとして正しく、追加I/Oも新規パーサも要らない。段階は絶対年数ではなく**Vault内の相対順位**の4段階（絶対閾値だと若いVaultは全部白・古いVaultは全部生成りで差が消える）。**最大の制約は `panelChip #EEF0FF`（L=0.8772）が地色の下限になること** — 弱い文字トークンの基準面が「文字が載る面のうち最も暗い」ことに置かれているため、紙がこれを下回ると既存の全テキストトークンが一斉に基準を割る（判断5・6 で2度差し戻された「面の取り違え」と同じ型）。初期値は `#FDFEFF`／`#FDFDF9`／`#FCFAF3`／`#FAF6EB`（L 0.9899〜0.9223）で**意図的に弱く**、実機で見てから強めるかを決める（上限は `#F7F1E2`＝0.8818）。色を載せるのは本文カードのみでグラデーションは触らない（場の性質と古さが同じ面に混ざるため）。**ダークでは無効**（暗所配色は別配色として設計されており、黄ばみの比喩が前提と衝突する）。オプション画面にトグルを置き**既定オフ**（本文が載る面の色なので既存ユーザーの見た目を勝手に変えない）。段階は `NoteUiState` のノート単位状態として契約2箇所へ登録し、色へ写す係はテーマ側の窓口1つに閉じたので**トグルはノートを開き直さずに反映される**。さがす経由は走査キャッシュが冷えていると段階が決まらないが、**表示後に埋め直す形は採らない**（見えている色が数百ミリ秒後に変わると不具合に見える。誤った色より無色を選ぶ）。テスト583→605、Lint 0 error / 0 warning。**変異確認5件すべてで落ちることを確認**。加えて `NoteSessionCoordinatorTest` のリフレクション契約テストが**足場を直すまで効いていなかった**ことが判明し（`fullyPopulatedState()` に新フィールドを積むまで初期値どうしの突き合わせだった → lessons L25 の予告どおり）、積んだ後は3件が落ちるようになった。副次的に、**CLAUDE.md の逆引き表に `ui_design_principles` が載っていない**抜けを発見して修正し（README §5 にはあった。常時読み込まれるのは CLAUDE.md 側）、同じ索引ドリフトが再発しないよう README へ「直す表は2つある」と明記した。**2026-08-02 に実機確認済み**（強度は初期案のまま確定＝「控えめのほうがよい」）。**確認中に「トグルが切り替わっていないのでは」という疑いが出たが、配線ではなく値が原因だった** — `Fresh` は `panel` と完全に同値（仕様）なので**最も新しい四分位ではトグルが数学的に無反応**で、Settling も青が6/255と実質不可視、つまりノートの約半分で何も起きない。一時的に色相を大きく変えた診断用の値（水色/黄/橙/赤・`Fresh` も別色）を入れてトグル・ノート切替・ダーク無効のすべてが動くことを実機で確認してから戻した（診断中は `AppColorContrastTest` が4件落ちるので戻し忘れを検知できる）。**外部レビューで4件修正** — ①割合の分母は一覧の件数なので**対象自身を含めるかで結果が変わる**（対象4・一覧`[1,2,3,4,5]`は0.20でFresh、自分を抜くと0.25でSettling）。KDocとテストは「影響しない」と書いていたが誤りで、たまたま同じ段階に落ちる値でだけ通っていた。契約として明記し差分を固定するテストへ差し替え。②オプションの説明文が「しばらく開いていない」で**閲覧履歴を見ている機能に読めた**（実際は最終更新日時）ので「更新から時間が経った」へ。③`RelatedNote` は `lastModified` を**持っている**（検索側が設定済み）のに走査キャッシュだけを見ており、使える値を捨てていた。候補側を優先し、キャッシュに対象が居なければ分母へ1件補う形へ。④設計書の「分布をVault単位の新規状態として`vaultGeneration`で管理」が実装と食い違っていた — 実装は既存`cachedNotes`を分布に使い新規状態も世代IDも足しておらず、**算出結果のほうがノート単位**。**分布とその結果で寿命が違う**ことを取り違えていたので、実装どおりに書き直した。あわせて「どんなVaultでも必ず分布が出る」の過大表現を「更新日時が複数あるVaultなら」へ限定。**③の修正過程で、候補側の分岐が `NoteViewModel` の private にあり JVMテスト不能だったことが判明**し（優先順位を逆にする変異が`-Werror`の未使用引数で偶然落ちるだけだった）、`domain` の`notePaperToneForCandidate` へ切り出して変異3種すべてが落ちることを確認。テスト605→611 | **[note_age_paper](design/note_age_paper.md)（新規）**・[lessons](lessons.md) L25 |
| 2026-08-02 | — | **`lessons.md` の運用を「着手前の通読」から「diff完成後の5問」へ置き換えた（コード変更なし）**。同日の L28 が引き金 — **畳んではいけない2つを型で分けた実例（`ReadingTraceKeyListing`）の60行下**へ、同じファイルを頭から読んだ直後に3つの結果を畳む Boolean を足していた。**必要な知識は同じ画面にあった**ので、これは検索の失敗ではない。欠けていたのは**作ったものを見返す瞬間**だった。実績を並べ直すと差ははっきりしていて、**外部レビュー（9件・4件・2件を毎回検出）と、成果物に紐づく手順（L11 の変異確認。当日も指示なしで2回実行し2回とも検出）は効いており、分類の認識を要する散文教訓は 0/2** だった。そこで発火を**話題**（「これはキャッシュの問題か」）ではなく**自分の編集の形**（「境界に戻り値を足した」「ガードを足した」）に結び付けた §0 を新設し、L1〜L28 は事例アーカイブとして日常運用から外した。**CLAUDE.md は追加ではなく置換** — 旧文（着手前に通読）を残すと全文読込に5問が上乗せされるだけで狙いと逆になる。あわせて**「別の目」を具体化した** — 実装したのとは別のモデル／エージェントが**コミット前に**diffをレビューする恒久工程とし、**自己の2回目のパスは数えない**（L6 はこれを曖昧に許していた）。**§0 の初版は、その §0 自身が捕まえるべき今回の失敗を素通りさせていた** — 「分岐が3つ以上なら Boolean をやめる」では、呼び出し側の行動が2つしかない今回に当たらない。`true`/`false` の**両側**を「次にすること」で言い換える形へ直した。**検査を書いたら、まず元の失敗へ当て直す**という一点を L28 へ追記。**未検証の仕組みなので歯止めを置いた** — 次の3回のコード変更で5問のどれかが当たるはずの指摘がレビューから出たら発火していないと判定し、**文書を足すのをやめてレビュー工程へ重心を移す**。判定できるよう、変更の報告に `§0適用: …` の1行を残すことを完了条件へ加えた。**この整理自体に §0 を当てたところ4・5が該当し、`docs/README.md` の件数が27のまま／役割が旧運用のままだったのを検出した**（自己適用の初回で1件検出） | [lessons](lessons.md) §0・L28 |
| 2026-08-02 | — | **SYNC-2（`_ReadingTraces` 索引が外部同期の追加を認識しない）を解消した**。索引はVaultごとに1回しか読まれず、別端末で作られた痕跡が同期されても**プロセス再起動まで見えなかった**。実害は2つで、どちらも索引にキーが載っていない時に起きる — 再会カードが出ない（照合）と、**未存在と判断して同じキーのファイルを2つ作る**（保存）。**採ったのは「不在だった時にだけ索引を捨てて作り直す」**（60秒に1回まで）。TTL単独は劣る（ヒットしていても定期的に全列挙する。不在駆動なら同じノートを読み続けている間は列挙ゼロ）。ContentObserver は採らない（SAFプロバイダの通知は実装依存で信頼できず、購読の管理が増える）。上限が要るのは**まだ一度も読んでいないノートを開くと必ず不在になる**ためで、制限しないと開くたび全走査になり索引キャッシュの意味が消える。**判定は Android 非依存の `ReadingTraceStore` 側へ置き**、Gateway には `prepareIndexRebuild(vaultKey): Boolean` の1つだけを足した（捨てるだけで列挙はしない）。**この戻り値の意味をレビューで1度直している** — 当初「実際に索引を捨てたら true」で書いたが、**判定を「索引の現在の状態」で行ったために2つの境界ケースを誤った**。①`read` はキャッシュ済みUriの読込に失敗すると**自力で索引を捨ててから** null を返すので、その直後は「既に無い」＝false になり**作り直せば読めるはずの痕跡をその回だけ取りこぼす**。②旧Vault A の索引を抱えたまま B へ切替え、B へ触れる前に遅れた A 向けの読み出しが届くと、索引は A のものなので true になり**現在は B なのに60秒の枠を食う**。正しい基準は**「要求Vaultが現在のVaultか」だけ**で、現在のVaultなら索引の有無によらず作り直す価値があり、そうでなければ作り直しても `read` の Vault照合で弾かれる。**「何を返すか」は「何を知りたいか」から決める** — 呼び出し側が知りたいのは「もう一度読んでよいか」であって「捨てたかどうか」ではなく、実装の内部状態をそのまま返すと*状態が同じで意味が違う2つの場合*を区別できない。名前も `invalidateIndex` から `prepareIndexRebuild` へ変えた。**「枠を進めるのは捨てた後」という順序が効いている** — 先に進めると「枠は消費済みなのに索引はまだ古い」窓が一瞬でき、その隙の要求が新規作成へ進む。`loadByKey` はメソッドごと `@Synchronized`（照合は Controller の Mutex の外側を走るため）。**この直列化には落ちるテストを書けなかった**ので、`@Synchronized` を残したうえで**本当に効いている不変条件は順序のほうだ**と設計書とKDocへ明記した。掃除の `listKeys` からは枠を進めない（置き場が無い場合の `Available(emptySet())` は索引を更新していないのに Store から区別できず、「Available なら索引は新しい」という誤った前提を持ち込むため）。**60秒の窓に重複作成の余地は残る**（同期の着地から次の作り直し許可までの間に書いた場合）。塞ぐ案は痕跡を初めて作るノート1件につき1回の全列挙を持ち込み **PERF-1 と同じ形の負荷**になるため、実測が出るまで払わない。テストのFakeへ**ディスクと索引を分ける構造**を入れ（分けないと陳腐化を再現できない）、Vault切替時の索引作り直しと読込失敗も再現できるようにした。**変異検証: 作り直しの1行を消すと7件、Fakeの契約を旧仕様へ戻すと2件が落ちる**ことを確認済み。JVMテスト **593件成功**（+10）、Lint 0 error / 0 warning。**実機確認は必須としない**（同期プロバイダを2端末で用意するのが難しく、外部同期の追加を認識できないという不具合そのものは機能の劣化であってデータを壊す種類ではないため。オーナー判断）。教訓 **L28 を追記** — この「畳んではいけない2つを1つの値にした」誤りは同じファイル群で**4度目**で（`querySafChildren` の失敗と空・`RootFolderLookup` の不在と読み取り失敗・`ReadingTraceKeyListing`）、過去3件を「失敗と不在」という語で覚えていたために**「捨てたか」という別の語をまとった4件目に気づかなかった** | **[reflect_reading_trace](design/reflect_reading_trace.md) §15 判断19**・[lessons](lessons.md) L28 |
| 2026-08-02 | — | **バッジの記号に当てる基準を 4.5:1 から 3:1 へ改め、✓ を白へ戻した。あわせて UIデザインの指針を新設した**。2026-07-29 に「緑の上の白い✓が2.49」として黒へ変えたが、**そもそも当てる基準が違っていた** — WCAG は実装ではなく機能で分類するので、バッジの ✓ は読む文字（1.4.3・4.5:1）ではなく状態を示す記号（**1.4.11 非テキスト・3:1**）である。3:1 を当てると現在の緑 `#109384` の上で白は **3.80 で通る**（2.49 だったのは当時の緑が `#16B8A6` と明るかったためで、緑を暗くした結果 同じ白の条件が変わっていた）。`onStatusBadge` を新設し、**同じ塗りの上でラベル（黒・4.5:1）と記号（白・3:1）を分けた** — 1つに揃えると記号側が不必要に制約される。Success/Error で共有し明暗で反転する（ライト白: 緑3.80／赤5.89、ダーク黒: 緑8.44／赤7.57。ダークで白へ戻すと2.49／2.78で3:1すら割る）。副次的に**ライトで Error が白・Success が黒という不揃いが解消**した。テストは「記号は3:1」と「明暗で反転している」の2本へ作り替え、ダークで白へ戻す変異を禁じる根拠も固定した。**あわせて [ui_design_principles](design/ui_design_principles.md) を新設** — 同日に輪郭線を実装して差し戻しており（**数値基準を満たす変更を見た目で差し戻すのは判断7の暗幕に続いて2度目**）、原因は「満たすべき数値しか文書に無かった」ことだったため、国際規約の当て方と**差し戻した実物の記録**（小さい部品に輪郭線・濃い記号・暗幕を置かない）を着手前に読める形で分離した。JVMテスト**583件成功**、Lint 0 error / 0 warning。**2026-08-02 に実機確認済み**（ライトモードでSuccess/Errorのバッジ記号が白で揃って表示され、問題報告なし） | **[ui_design_principles](design/ui_design_principles.md)（新規）**・[theme_and_ui_refactor](design/theme_and_ui_refactor.md) 判断8 |
| 2026-08-02 | — | **FEAT-1 のMarkdown部分（リスト構造）を解消した**。`ListBlock` が `items: List<String>` の単一型だったため、**①入れ子段数 ②番号 ③箇条書きか番号付きかの種別**の3つが同時に落ちていた。**これは表示だけの問題ではなく、同じパーサが `buildNoteExcerpt` 経由でAI入力にも使われる**ため、手順書のように順序自体が意味を持つノートで**その意味がモデルへ届いていなかった**。`ListItem(depth, marker, text, checked)` と `ListMarker` を導入。**原文尊重は非対称に適用する** — 番号は原文のまま保持し（`number` を `Int` ではなく `String` で持つのは、区切り記号 `.`/`)` と先頭ゼロ `01.` を失わず桁溢れも起こさないため。`Int` に落とすのは「原文尊重」という判断自体の裏切りになる）、箇条書き記号 `-`/`*`/`+` の違いは意味を持たないので正規化する。**段数はCommonMarkにもObsidianにも準拠しない寛容規則**（規格は親のマーカー直後の列位置で入れ子を定義するが、幅が2/4スペースで揺れる実Vaultを取りこぼすため、幅の絶対値を見ず相対的な深浅だけで判定する）。規則5つをKDocに明記し1規則1テストで固定、タブは4文字加算ではなく次の4列境界まで展開する。**`TaskListBlock` は廃止して項目の属性 `checked` へ統合** — 別ブロックのままだと `- 親`/`  - [ ] 子` の混在で入れ子が2ブロックへまたがり段数の追跡が切れる（段数を持たせた結果、この分断が実害になった）。**`1. [ ]` は受理しない**（型がOrdered+checkedを表現できることを仕様拡大の理由にしない）。**既存テスト1件は追加ではなく置換**（`ordered listは箇条書きへ正規化する` が現仕様を固定していた）。予算不変条件は増加文字数の固定ではなく `assertWithinBudget` を全予算で通す形にした（記法が増えるたびに壊れる固定値を避けるため）。JVMテスト **581件成功**、Lint 0 error / 0 warning。**実機確認済み**（ネスト箇条書き・番号付きリスト・タスク混在、問題報告なし） | **[markdown_rendering](design/markdown_rendering.md)（新規）**・[ai_input_excerpt](design/ai_input_excerpt.md) §6 |
| 2026-08-01 | — | **N-1（読書痕跡の孤児削除）の完了を受け、SYNC-1 を課題台帳から削除し roadmap の Now を空けた**。手動削除まで実装済み・外部レビュー指摘9件も修正済みの状態で、**実機確認はオプション機能でクリティカルでないため必須としない**（オーナー判断）。台帳のルール（未対応のものだけを置く）に沿って SYNC-1 を除去。**PERF-1 の記述も直した** — 「孤児は掃除しない方針なので単調増加する」は事実と異なるため（手動削除が入ったが押されるとは限らないので「単調増加しうる」へ）。roadmap は X-3（move/rename追従）を「N-1完了・着手しない」へ更新（改名で残った痕跡は手動削除で掃除できる側に回ったため実害がさらに減った）、X-4（同期索引）は N-1 と独立であることを整理。**コード変更なし** | [roadmap](../_wip/roadmap.md)・[reflect_reading_trace](design/reflect_reading_trace.md) §14 |
| 2026-08-01 | — | **外部レビューの指摘9件（高2・中3・軽微4）をすべて修正した**。**高①遮断器が「直接の親」でしか数えていなかった** — 判断17 に「故障はフォルダ単位で起きるので遮断器もフォルダ単位に置く」と書きながら実装は葉の階層でしか数えず、**上位フォルダが成功したまま空を返す**と（BFSが配下へ潜らないので `unreadableFolderPaths` にも出ない）`ideas/a/x.md` と `ideas/b/y.md` が別グループになって両方候補になった。**変異検証では捕まらない種類の誤りで、テストが同じ誤解の上に立っていた**（「直接の親で数える」実装をどう変異させても「直接の親で数えるテスト」は落ちない）。数える単位を**直接の親（ルート含む）＋ルートを除く全祖先**へ変更。ルートを祖先に数えないのは、数えると全候補が共通祖先を持ち「Vault全体で常に1件まで」となって**正当な孤児が2件溜まった時点で永久に掃除できなくなる**ため。**それでもルート直下の静かな欠落は塞げない**ので、**一括削除を撤去して削除を1件ずつに限定した**（§4 が許容するのは「稀に1件」なので、一度に1件しか消せない形なら仕様の範囲を出ない。実績が集まったら戻せる）。**高②SAF I/O が Main で走っていた** — `listKeys` / `loadByKey` / `deleteByKey` は同期I/Oで `scope` は `viewModelScope`。**同じ機能の `ReadingTraceController` は `ioDispatcher` を注入しているのに新しい方だけ抜けていた**（L13・L14 の型）。**中③`CancellationException` を一般エラーへ畳んでいた** — CLAUDE.md の必須原則違反で、`state.set` は suspend しないため**キャンセル後も偽エラーが書き込まれる**。**このガードには落ちるテストが無く**（再throwを外しても1件も落ちなかった）、キャンセル注入テストを新設した（冗長ではなく検証漏れ）。**中④判定後にノートが復活しても古い候補を消していた** — 削除の直前に走査し直し、**ノートが現れていたら消さずに候補から外す**（もう孤児ではない）。フォルダが読めなくなっていた場合も消さない。**中⑤同じキーの重複ファイルを1件に潰していた** — 索引を `key → List<Uri>` にして全実体を消し、**1つでも残ったら失敗**として扱う。**軽微4件も全て事実**（コードKDocが「シャドーモードで削除しない」のまま／台帳が「削除手段が一切ない」のまま／「ノート一覧はTTLキャッシュ」が誤り＝`collectAllNotes()` はRepositoryへ直行し毎回フル走査／確認文「すでに見つからないノートです」が断定過剰で、**L24 を書いた本人が確認文で断定していた**ため「現在の走査では見つかりません」へ）。あわせて**設計書の構造崩れも直した**（段階3の追記時に段階2の変異検証の続きと「既存機能は壊さない」段落が別の節へ流れていた）。JVMテスト **564件成功**（+5）、Lint 0 error / 0 warning / 12 hint、`assembleDebugAndroidTest` 成功、`git diff --check` 成功。**実機確認はオプション機能でクリティカルでないため必須としない**（オーナー判断） | **[reflect_reading_trace](design/reflect_reading_trace.md) §14 外部レビュー**・[lessons](lessons.md) L11・L13・L14・L24 |
| 2026-08-01 | — | **N-1 完了に伴い恒久文書を追随させた（コード変更なし）**。**`architecture.md` の Controller 系統図が 2026-07-31 から更新漏れしていた** — `NoteSectionController`（2026-07-31）と `ReadingTraceCleanupController`（2026-08-01）が載らず件数も7のままだった。実数9へ直し、**Vault単位のControllerはノート単位の契約（`cancelNoteScopedJobs` / `withNoteScopedReset`）に登録しない**ことを明記（契約2箇所への登録は「ノート単位の状態を足したとき」の定型であって、全Controllerが従うものではない）。あわせて8・9件目での共通化再判定を1行で済ませた（再検討条件は「`markViewed()` と Snackbar を持つ3つ目」なのでどちらも当たらない。**件数をトリガーにしないと決めた判断がそのまま効いている**）。`source_code_analysis.md` は Controller 9つ・テスト559件・ファイル一覧・世代照合の記述を更新。教訓を2件追記した — **L25「網羅テストがある」と「そのフィールドが網羅されている」は別**（リフレクションで全フィールドを走査する契約テストは、比較の起点である `fullyPopulatedState()` に非初期値を積まないと**初期値と初期値を突き合わせるだけ**になり、登録が正しくても間違っても通る。足場を直して初めて契約テストになる。同じ構造が系統図・一覧にも現れ、実際に architecture.md が漏れていた）、**L26 非同期の要求は「今」を読み直さない**（判断10 の保存経路で守っていた規律が削除経路で2度目として現れた。世代照合が見るのは「操作中の切替」だけで「操作と操作のあいだ」は見ない。**内容から導く識別子（ハッシュ）はスコープをまたぐと衝突する**ため危険が跳ね上がった。**状態リセットを安全装置に数えない**）。**段階5（限定自動化）は着手しない** — シャドーモードで保留がほとんど発生せず、遮断器が実データで働くところを一度も観測していないため | [architecture](design/architecture.md)・[lessons](lessons.md) L25・L26 |
| 2026-08-01 | — | **N-1 段階4「手動削除」を実装し、段階3 の実機確認を完了した（段階4 は実機確認待ち）**。個別削除（各行の×）と**候補だけの一括削除**を開けた。どちらも確認ダイアログを挟み、**「ノート本文は削除されません」を必ず併記**する（痕跡とノートの区別が付かないと「ノートが消える」と受け取られる）。**保留した分は候補一覧に入らないので「すべて削除」でも消えない** — 判断13 が懸念した「候補をそのまま全選択して事故る」経路は、遮断器が候補を絞っていることでここで塞がる。**テストが実装の穴を1つ捉えた** — 当初 `vaultGeneration` の照合だけを置いたが、これは**操作中の切替**しか見ておらず**洗い出しと削除のあいだの切替**が抜けていた。しかも**キーは相対パスのハッシュなので、別のVaultに同じ相対パスのノートがあればキーも一致する**ため、旧Vaultの候補キーで**新Vaultの生きた痕跡を消し得た**。`assessedVaultKey`（洗い出した時点のVault）を保持し削除はその値に対して行う形へ直した（**要求時点のVaultを要求自身に持たせる規律**は判断10 の保存経路と同じで、同じ理由が削除でも現れた）。実運用では状態変換が先に候補を消すので二重の守りになるが、**状態リセットに依存した安全性はリセットを1つ書き忘れた瞬間に消える**。**削除に失敗した候補は一覧に残す**（SAFプロバイダは失敗し得るので、消えたことにすると再試行できない。索引からも失敗分は外さない）。**削除後に洗い直さない**（Vault全走査は重く、残った候補は同じ判定で評価済みなので消えた分を落とすだけで正しい）。変異検証は削除時に現在のVaultを読み直すと1件、候補一覧への固定を外すと1件、削除失敗分を一覧から外すと1件。**段階5（自動化）の門は未達であることを明記した** — シャドーモードでは痕跡がまだ少なく**保留がほとんど発生せず、遮断器が実データで働くところを一度も観測していない**。必要なのは運用期間であって段階4 が動いたことではない。JVMテスト **559件成功**（+5）、Lint 0 error / 0 warning / 12 hint、`assembleDebugAndroidTest` 成功 | **[reflect_reading_trace](design/reflect_reading_trace.md) §14 段階4**・[roadmap](../_wip/roadmap.md) N-1 |
| 2026-08-01 | — | **N-1 段階3「一覧画面（削除ボタンなし＝シャドーモード）」を実装した（実機確認待ち）**。オプション画面に「読書痕跡を整理」を追加し、専用画面で**削除候補と、遮断器が保留した一群を理由つきで**並べる。**削除ボタンは置かない**（段階4 で足す）。`ReadingTraceCleanupController` は8つ目の Controller で、**Vault単位なのでノート単位の契約（`cancelNoteScopedJobs` / `withNoteScopedReset`）へは登録しない**（ノートを開き直しただけで洗い出しが消えるのは誤り）。世代照合は `activeRequestId` ではなく **`vaultGeneration`**。**判定の型を `domain` から `model` へ移した** — `model.state` の画面状態が判定結果を持つため、`domain` に置いたままだと**葉であるはずの `model` が `domain` を import する**ことになる。`NoteExcerpt`（model）と `buildNoteExcerpt`（domain）と同じ切り分けにし、判定関数だけ `domain` に残した。**Vault切替のリセットは Controller ではなく状態変換（`withVaultScopedReset`）に置いた** — 補記一覧は `AnnotationController.onVaultChanged()` が落とす前例があるが、こちらは旧Vaultの候補が残ると**「別Vaultのノートを消しませんか」と尋ねる状態**になり補記一覧と違って誤削除に直結するため、Controller に任せず構造的に保証する（Controller は走行中Jobのキャンセルだけ持つ）。**「候補ゼロ」と「判定できなかった」を画面でも別の言葉にする** — 型で分けても文面が同じ「何も無い」だと遮断器が働いている状態を「掃除するものが無い」と読み違えるため、文面を純関数 `ui/ReadingTraceCleanupText.kt` へ切り出し、**内部語（孤児・遮断器・列挙）を出さないことをテストで固定**した。`VaultHandle.collectAllNotes()` を追加（完全性つきの全走査。既存の `collectNotesInScope` は完全性を持たない）。**リセット網羅テストは、フィールドを足しただけでは効かなかった** — `NoteSessionCoordinatorTest` はリフレクションで全フィールドを突き合わせるが、`fullyPopulatedState()` に**非初期値を積まないと素通りする**。積んだうえでリセット登録を外す変異を当て2件落ちることを確認した。**「網羅テストがあること」と「そのフィールドが網羅されていること」は別。** 変異検証は世代照合を外すと1件、列挙失敗を空の成功へ畳むと1件、Vault切替のリセット登録を外すと2件。JVMテスト **554件成功**（+14）、Lint 0 error / 0 warning / 12 hint、`assembleDebugAndroidTest` 成功。**実機確認は未実施** | **[reflect_reading_trace](design/reflect_reading_trace.md) §14 段階3**・[roadmap](../_wip/roadmap.md) N-1 |
| 2026-08-01 | — | **N-1 段階2「孤児判定の純関数」を実装した**。`domain/ReadingTraceOrphans.kt` に `assessReadingTraceOrphans` を追加（Android型ゼロ・JVMテスト18件）。**設計時に見落としていたのは「遮断器はパスを要求するが、孤児はキーしか持たない」こと** — フォルダ単位の判定も不完全走査サブツリーの判定も候補の相対パスが要り、それは痕跡ファイルを読まないと分からない（キーはハッシュで不可逆）。**急増ガードを resolve より前に置くことで解決した** — 候補が多すぎる時点で判定ごと見送るので「Vaultが丸ごと見えない」状況で全ファイルを読みにいくことがなく、**安全ガードがそのままコスト上限を兼ねる**。遮断器は5種: ①ルート列挙の失敗 → `Blocked(VAULT_ROOT_UNREADABLE)` ②候補の急増 → `Blocked(TOO_MANY_CANDIDATES)` ③不完全な走査のサブツリー → `UNREADABLE_FOLDER` で保留（**他の枝は通す** — 失敗をパス集合で持った理由がこれで、1フォルダ読み損ねただけで掃除が全面停止しない）④同じフォルダから複数件 → `FOLDER_WIDE_ABSENCE`（故障の粒度と一致させた本体）⑤中身を読めない候補 → `UNRESOLVABLE`（**何を消すか確認できないものは消さない**）。**`Blocked` を空の `Assessed` で代用しない** — 「判定できなかった」を「孤児は無かった」と同じ形で返すと、段階0・段階1 で型に固定してきた区別がここで崩れる。**前方一致は区切りを足して比較する**（素の `startsWith` だと `ideas2` が `ideas` の配下と見なされる）。**代償を設計として明記した — フォルダごと削除した場合の痕跡は掃除されない**（列挙失敗と区別できないため保留）。狙いは日常的に発生する単発の孤児であって一括削除の後始末ではない。**変異検証は6箇所すべてで落ちるテストがあることを確認**（フォルダ一括2件／不完全走査3件／ルート不読1件／急増ガードを resolve の後ろへ回す2件（**ファイルを読まないことの検証を含む**）／読めない候補の保留2件／区切り無し `startsWith` 2件）。UIから到達する経路がまだ無いため**実機確認は不要**。JVMテスト **540件成功**（+18）、Lint 0 error / 0 warning / 12 hint、`assembleDebugAndroidTest` 成功 | **[reflect_reading_trace](design/reflect_reading_trace.md) §14 段階2**・[roadmap](../_wip/roadmap.md) N-1 |
| 2026-08-01 | — | **N-1 段階1「痕跡の列挙API」を実装し、段階0 の実機確認完了で SCAN-1 を台帳から削除した**。`ReadingTracePersistence` に `listKeys(vaultKey): ReadingTraceKeyListing` と `loadByKey(key, vaultKey)` を追加。**孤児の特定にファイル読み込みは要らない** — キーが `sha256Hex(相対パス)` なので、Vault走査で得たノートのパスをハッシュして**集合差**を取れば孤児キーが出る（N件のサイドカーを開かずに済む。遠いプロバイダでは読み込み回数がそのまま体感になる）。`loadByKey` が要るのは、孤児と分かった**少数**について画面に出す名前を得るときだけで、**孤児はパスが分からない**（ハッシュは不可逆）ため `load` の相対パス指定では引けない。**`ReadingTraceKeyListing` を `Available(keys)` / `Unavailable(reason)` に分けた** — `Set<String>` 1本にすると列挙失敗が空集合として返り**全痕跡が孤児に見える**。段階0 と同じ誤りを今度は型で書けなくしてある。**列挙は索引キャッシュを経由しない**（キャッシュ経由だと外部同期で増えたファイルを見落とす → SYNC-2。掃除は1回だけ実行される重い操作なので鮮度を優先してよい）。**逆にフォルダは作らない** — 置き場が無ければ痕跡ゼロが正しい答えで、列挙の副作用で空フォルダができるのは筋が悪い。新鮮で完全な一覧が取れたときだけ索引キャッシュも更新するので、**掃除の下見をした瞬間だけ SYNC-2 の陳腐化が解消する**（副次効果）。`load` は `loadByKey` へ委譲しつつ**相対パスの完全一致を追加で確認**する（`loadByKey` はパスを知らずハッシュ一致までしか見られないので、委譲だけにすると既存の検証が弱くなる）。**変異検証: 列挙失敗を `Available(emptySet())` へ畳むと2件のテストが落ちる**ことを確認済み。**段階1 に実機確認は不要**（UIから到達する経路がまだ無く、既存動作を変えていない）。あわせて**段階0 の実機確認が完了**したので SCAN-1 を台帳から削除した。JVMテスト **522件成功**（+8）、Lint 0 error / 0 warning / 12 hint、`assembleDebugAndroidTest` 成功 | **[reflect_reading_trace](design/reflect_reading_trace.md) §14 段階1**・[roadmap](../_wip/roadmap.md) N-1 |
| 2026-08-01 | — | **N-1 段階0「走査を正直にする」を実装した（SCAN-1・実機確認待ち）**。`querySafChildren` が `SafChildren(items, isComplete)` を返し、**`query()` が null なら `UNREADABLE`** を返す。従来は `?.use { }` で null を素通りさせて空リストにしていたため、**「読めなかった」と「本当に空」が呼び出し側で区別できず**、あるフォルダの列挙が失敗すると配下のノートが**走査成功のまま静かに0件**になっていた。`traverseMarkdownPaths` は失敗したフォルダの**相対パス集合**を返し（全体1フラグにしないのは、孤児判定が「読めなかったサブツリー配下だけ候補から外す」と精密に書けるようにするため。1フラグだと1フォルダ読み損ねただけで掃除が全面停止する）、`collectNotes` は `VaultScan` でノート一覧と一緒に運ぶ。**走査は中断しない** — 他の枝のノートは正しいので表示系はそれで動き続ける（Rediscover・さがす・関連ノート・補記一覧は `.notes` / `.items` を明示的に取る形にし、**破棄が grep できる**ようにした）。**実装して初めて分かったのは、同じ穴が「重複を作る」側にも空いていたこと** — `findRootChildFolder` が**不在と読み取り失敗をどちらも null で返していた**ため、呼び出し側の `find(...) ?: create(...)` が**ルート列挙に失敗しただけで2つ目の `_ReadingTraces` / `_AI補記` を作りうる**状態だった（SYNC-2 が「重複作成の余地」と書いていたのは外部同期だけでなくこの経路でもあった）。`RootFolderLookup` を `Found` / `Absent` / `Unreadable` の3値にし、**`Absent` のときだけ作成する**形へ変更。**`find(...) ?: create(...)` という書き方自体が温床**なので探索と作成を1回の探索へまとめ、この形をコードから消した。痕跡索引にも `isComplete` を持たせ、不完全なら**新規作成を見送る**（書けなかった訪問は判断10 の巻き戻しで次の契機に書き直される）。ただし**不完全な索引はキャッシュしない** — 載せると一度読み損ねただけでVault切替まで新規作成が止まり続けるため、完全なときだけキャッシュして次回読み直させる。**`EXTRA_LOADING` は今回の範囲外**（プロバイダの実挙動を検証できないまま分岐を足すと段階0 の完了判定が曖昧になるので、段階3のシャドーで観測してから判断）。**変異検証: `if (!children.isComplete) unreadable.add(path)` の1行を外すと5件のテストが落ちる**ことを確認済み。JVMテスト **514件成功**（+6）、Lint 0 error / 0 warning / 12 hint、`assembleDebugAndroidTest` 成功。**実機確認は未実施** | **[reflect_reading_trace](design/reflect_reading_trace.md) §14 段階0**・[roadmap](../_wip/roadmap.md) N-1 |
| 2026-08-01 | — | **SYNC-1 の着手対象を「孤児のみ削除」に確定し（オーナー判断）、段階0〜4 の計画を roadmap N-1 として起こした**。検討途中で「全リセット」へ寄せていたが、**元の課題台帳（`922a3dd` 以前）の対応候補①も「孤児の一括削除」**であり、全リセットは後から入った案だったことを確認して戻した。**両者は代替物ではない** — 全リセットは判定不要で軽いが、**SYNC-1 の問題（ファイル数の単調増加）には答えない**（判断13 自身が明記していた）。判断13 の位置づけも「全リセットのほうが良い」ではなく**「判定が信用できないなら全リセットしか置けない」**へ直し、§14 段階0 で条件付きに上書きされることを明記した（**判断13 と §14 段階2 が正面から矛盾して読める状態を放置していた** — §14 の候補は証拠付きで判断13 が却下した候補とは品質が違うのに、どこにもそう書いていなかった）。**§14 の実装順を2箇所訂正した。** ①**SYNC-2 は前提ではない** — 「削除の安全性」からは導けず、次に挙げた「列挙の完全性」も誤りで、**古くなるのはセッション中のランタイム索引**であり掃除は `_ReadingTraces/` を新規列挙すれば足りる（SYNC-2 は再会照合の課題で順序の制約を持たない）。②**証拠索引は手動削除の前提ではない** — 判断11 への答えは2つあり、**根拠を直す（走査を正直にする）**と**根拠の性質を落とす（不在を票として積む）**は両方要るわけではない。手動削除には前者で足り、後者は自動化の門にする。**この分割で規模が中→小〜中に戻った。** 実装順は `走査を正直にする → 列挙API → 孤児判定（純関数） → 一覧（削除ボタンなし＝シャドー） → 手動削除 →〔自動化〕`。**シャドーモードは専用機構を作らず、同じ画面の削除ボタンを塞いだ状態で実現する。** 遮断器がフォルダ単位の一括欠落を候補から外すので、**段階4 では全選択しても事故らない**（判断13 が懸念した経路がここで塞がる）。**既存機能は壊さない** — Rediscover・さがす・関連ノートは不完全でも見つかった分で動き続け、完全性フラグを見るのは掃除だけにする（混ぜると同期の遅いVaultでランダム表示が止まる別の不具合を作る）。あわせて**`querySafChildren` の null 畳み込みを SCAN-1 として独立起票**した（N-1 段階0 で直る予定だが、掃除を作らなくても残る欠陥。`collectNotes` のBFSがこの経路を通るため、あるフォルダの列挙が失敗すると**配下のノートが走査成功のまま静かに0件になり**、Rediscover の母集団と検索結果が理由なく減る。**失敗が痕跡を残さないので報告されても再現条件を作れない**。頻度は未確認だが、静かに壊れることが問題であって頻度の低さは緩和にならない）。roadmap は X-3 を「move/rename 追従」だけに絞り（孤児削除が入れば改名で残った古い痕跡は掃除される側に回るため、**追従しないことの実害が減る**）、X-4 から N-1 の前提という記述を外した。**コード変更なし** | **[reflect_reading_trace](design/reflect_reading_trace.md) §13〜§14**・[roadmap](../_wip/roadmap.md) N-1 |
| 2026-08-01 | — | **SYNC-1 の決着を同日中に更新した — 「手動で固定」ではなく「観測してから自動化へ昇格する段階案」を採る**。§13 は撤回しない（判断11〜12 はそのまま有効）が、**それらを回避する第三の道が出た**。要点は**根拠を直すのではなく根拠の性質を落とすこと** — 不在を「証明」ではなく**証拠の1票**として扱い、複数セッション・複数日にまたがって初めて候補にする。**効いているのは票数ではなく粒度の一致**で、`querySafChildren` の null 畳み込みは**フォルダ単位で起きる**ため、遮断器も**「同じフォルダから複数件が同時に欠けたら保留」**とフォルダ単位に置くと故障の粒度と一致する（票を重ねるだけでは持続的な故障を排除できない — 同じフォルダが毎回失敗すれば票は貯まる）。**閾値は時間ではなく観測回数**で持つ — TTL 60秒は定期実行ではなく**再走査の上限**で（`collectAllNotesCached` は期限内なら早期returnするだけ）、**経過時間は走査回数の代理にならない**（半年使わなければ走査ゼロ回で全件が候補になる）。票を入れるのは実際に `collectNotes` が走ったときだけ。**§13 判断12 はこの案を否定しない**ことを明記 — 「価値と逆を向く」と言ったのは**ユーザーの閲覧**を信号にした場合で、本案は**走査での存在**を使うため2年開いていないノートも毎回確認されて候補にならない。**レビューで欠落を1つ足した — 証拠索引は `vaultKey` で区切る。** 区切らないと Vault A の走査が **B の全ノートを不在として記録**し、`onVaultChanged` がキャッシュを捨てて切替のたび実走査が走るため**数回往復で B の痕跡が全滅する**（急増ガードには掛かるが、それは事故ではなく正常な運用を止めているだけで機能が永久に前へ進まない）。**遮断器の穴も明記** — ノート1件だけのフォルダの列挙が失敗するとフォルダ一括判定に掛からず「一度に最大1件」も通過する（§4 の許容範囲だが完全ではない）。**実装順の理由付けを補正** — SYNC-2 を先に置くのは削除の安全性からは導けず（索引が古いと起きるのは取りこぼしであって誤削除ではない）、**強い根拠は列挙の完全性**（同じ列挙APIを全リセットも使うので古いと消し残す）。段階は**シャドーモード（削除せず候補と再出現を記録＝誤削除の反証が取れる）→ 手動確認 → 孤立した少数だけ限定自動化**。**「誰も押さないボタンはGCにならない」は段階3で解ける。** 規模感は全リセットのみなら小、段階案まで含めると**中（複数フェーズ）**へ訂正 — 「台帳で一番軽い」という選定理由は成立しなくなった。教訓は lessons **L24 追記**（「根拠が信用できない」と言うときは**どの粒度でどう間違うか**まで書く。そこまで書けば根拠を直さずに設計側で受け止められることがある）・**L17 追記**（二択で詰まる型の2度目。1度目は**強度**の軸、今回は**時間**の軸。「今は A、条件を満たしたら B」は片方を選ぶより強い。恒久仕様と検証フェーズを混同しない）。**コード変更なし** | **[reflect_reading_trace](design/reflect_reading_trace.md) §14**・[lessons](lessons.md) L17・L24 |
| 2026-08-01 | — | **SYNC-1（読書痕跡の孤児掃除）を「手動・自動を問わず採らない」で決着させ、性能懸念を PERF-1 として分離した**（**同日中に §14 で段階案へ更新。上の行を参照**）。検討は当初「自動削除は危険だから手動確認を挟めば安全か」の形で進んでいたが、**そこは分岐点ではなかった**。`querySafChildren` は `contentResolver.query(...)?.use { ... }` なので **`query()` が `null` を返すと空リストになり、「読めなかった」と「本当に空」が区別不能に潰れている**（`SafDocuments.kt`）。孤児判定の唯一の根拠が壊れているため、**候補一覧をユーザーに確認させても一覧自体が壊れたシグナルから作られ、全選択すれば自動と同じ事故が同じ確率で起きる**。検討中に採りかけた「全体件数が0や激減なら弾く」健全性チェックも破棄した — **弾けるのは走査が丸ごとこけた場合だけで、小さなサブツリーが1つ欠けても全体件数はほとんど動かない**（集計量で部分欠損は捉えられない）。**「最後に確認できた日時」も向きが逆**で、この機能は Rediscover と対で成立する以上**長く再訪していないノートほど再会時に価値が高く**、閾値を何日にしても直らない。検討中に一度「全走査時に更新すると走査コストが戻る」という理由付けを挟んだが**これは誤り**（全走査は Rediscover のため `NoteViewModel` が TTL 60秒で既に回している）で、同期誤判定と価値の逆相関だけで結論は立つ。置くとしても意味が明確な**「現在のVaultの読書痕跡をすべて削除」1本**に限る（誤判定ではなく明示的な履歴破棄なので判定の信頼性に依存せず、§4「痕跡は永久保存装置ではない」とも整合）。**ただしこれは元の懸念を解かない** — 押されるか分からないボタンはGCにならないので、SYNC-1 は「リセット導線が無い」へ書き換えて優先度を中→低に下げ、**索引作成がファイル数に比例する問題は PERF-1 として分離**した。PERF-1 の解は**削除ではなく分割**（ファイル名が `sha256Hex(相対パス)` なので先頭1桁で16フォルダへ分ければ列挙が 1/16 になり、**生きた痕跡を1つも消さずに性能だけ守れる**）。実測で問題化するまで着手しない（移行リスクの先払いを避ける）。**再検討の条件はUIではなく `querySafChildren` に置いた** — 列挙の失敗を空と区別して返すようになれば不在シグナルは初めて根拠になりうるが、その場合でも着手の契機は実測。教訓は lessons **L24**（判定の根拠が壊れているならユーザー確認を挟んでも訂正されない。**失敗を正常値へ畳む下位関数が、その上に建てられる機能設計の選択肢を静かに奪う**。削除で解く前に削除しない解を探す）。**コード変更なし** | **[reflect_reading_trace](design/reflect_reading_trace.md) §13**・[lessons](lessons.md) L24 |
| 2026-08-01 | — | **AI-1 を実機確認まで終えてクローズし、MAINT-1 を優先度「超低」へ落とした**。関連ノート 600 → 800 の実機確認で**生成時間・推薦品質とも体感差なし**。**「変わらない」は2つのことを同時に言っている** — ①懸念していた劣化は起きない（入力1.05倍は体感に出ない）②**改善も体感できない**（冒頭＋末尾を+66%にしても推薦は良くならない）。②は [ai_input_excerpt](design/ai_input_excerpt.md) §9.1「効いたのは量ではなく配置」の**2度目の追認**で、1度目は予算を減らしても良くなったこと、今回は増やしても良くならなかったことにより、**両方向から同じ結論に当たった**。したがって1,000・1,200へ進む根拠は無く、**予算調整という方向自体を打ち止めにする**（さらに上げても効かない可能性が高く、生成時間だけが伸びる）。600へは戻さない — 劣化が無く文脈は増えており、戻す作業にも実機確認が要って得るものが無い。**判定の弱さも記録した**（数ノートの主観比較なので、大きな効果が無いことは言えるが小さな効果が無いことは言えない）。**AI-1 は「対応候補として書かれていた施策を両方とも捨てる」形で閉じた** — 短縮注意書きは効果44トークン未満、予算引き上げは体感できず。教訓は lessons **L23**（台帳に長く残った課題ほど着手時には前提が古い。AI-1 は2回連続で最優先と指摘されながら**前提が2つとも誤っていた**。「2回指摘された」は正しさを補強せず**同じ古い前提を2回読んだだけ**。着手時の最初の30分を前提の検算に使う。**「変わらなかった」は必ず記録する** — 書き残さないと次に同じ課題が新鮮な顔で立ち上がる）。MAINT-1 は最大の未知だった beta4 の調査が終わり「上げない」と判断が付いたので**優先度を超低へ**（残るのは「古い」という状態だけで実害は無い。**着手の契機は新版が出たことではなく、構造化出力や `SystemInstruction` を実際に使いたくなったとき**）。**コード変更なし** | **[ai_input_excerpt](design/ai_input_excerpt.md) §14**・[lessons](lessons.md) L23 |
| 2026-08-01 | — | **関連ノートの抜粋予算を 600 → 800 へ上げた（AI-1 の1歩目）。予算値の変更はこれ1件のみ**。2026-07-28 の抜粋化で意図的に先送りしていた予算調整に、実測が付いて初めて着手できた。**掃引の結論は「トークンでは値を決められない」だった** — 600/800/1000/1200/1500/2000 を1回の実行で測ると、100文字あたりの限界費用は全域でほぼ一定（日本語で約60トークン）で**逓減しない**。骨格ラベルと注意書きの固定費は600の時点で払い終わっており、そこから先は線形。**2,000でも余裕835で通ってしまうため、「トークン効率が落ちるからここまで」という自然な停止点が存在しない。** 高密度混在プロファイルは約50トークン/100字で、**日本語のほうが高いので日本語が制約側**になる。したがって判断軸は生成時間（ノート表示で自動起動しMutexで直列化されるため遅延が他機能へ波及する）と推薦品質（§9.1 の「同じ予算なら構造を伝える不連続な断片のほうが強い」に照らすと、圧縮を緩めることが改善とは限らない）の2つになるが、**どちらも計測できていない**。**測れないものが2つ残る状態で大きく動かさない**という理由で800に留めた（オーナー判断）。1,200や1,500へ一気に動かすと、体感が悪化したときに時間か品質かを切り分けられない。配分は**冒頭＋末尾が 228 → 378文字（+66%）**、骨格上限も 76 → 126 になり、注意書きの占有率は 38% → 28%。入力2,530・余裕1,566（上限の36%が空き）。**beta4 より先にこの調整をやる** — beta4 は `maxOutputTokens` の既定値が4096になり現状のままでは全経路が上限を超えるため、先に入れると基準線を全部引き直すことになる。あわせて beta4 の危険度を実測値で上方修正した（`getTokenLimit()`=4,352 なので入力に許されるのは256トークンだけになり、**最小の読書痕跡要約349すら通らない**。「動作互換ではない」ではなく「`maxOutputTokens` の明示設定なしには採用不可」が正しい）。JVMテスト508件成功（`NoteExcerptBuilderTest` は定数を参照しているので追随）、Lint 0 error / 0 warning。**800の実機体感は未確認** | **[ai_input_excerpt](design/ai_input_excerpt.md) §14**・[dependency_policy](design/dependency_policy.md) §5 |
| 2026-08-01 | — | **計測器を実機で緑にし、AI-1 の前提が誤りだったことを数字で確定した（実機確認完了）**。初回の実機実行は `GenAiException [ErrorCode 10003] Tokenization failed` で失敗した。`FeatureStatus` は `AVAILABLE`、`baseModelName` は `nano-v3`、AICore も ready、公式対応端末（Pixel 10 Pro Fold / Android 17）で生成も普段どおり動く — **ここまでの材料では「beta2 の計測APIが Android 17 と非互換」という読みになり、実際 beta4 更新を検討しかけた**（既定値 256→4096 の動作非互換を抱える重い変更）。**正解は AICore がバックグラウンド利用を拒否していただけ**で、instrumentation テストがフォアグラウンドの Activity を持たないことが原因だった。`ActivityScenarioRule` を足して 6/6 緑。**解いたのは推論ではなく対照実験** — 能力を1つずつ独立に試し、**落ちないはずの `generate()` を対照として同じ実行へ入れた**ところ、それが `[ErrorCode 30] Background usage is blocked` で落ちて前提の誤りが一撃で判明した（`getTokenLimit()` はメタデータ照会なのでバックグラウンドでも通り、この非対称が手がかりになった）。**同じ原因が経路によって別の顔で出る** — `generate()`/`warmup()` は原因を明示するが `countTokens()` は `10003 Tokenization failed` としか言わず、しかも 10003 はSDKのマッピングに無い。**先に見たのが `countTokens()` だったせいで誤読した。** 教訓は lessons L20（端末AIのテストではActivityの有無が前提条件そのもの）・L21（切り分けが止まったら「動くはずのもの」を対照に置く）。**計測結果は AI-1 の前提を覆した** — `getTokenLimit()`=4,352・`maxOutputTokens`=256（beta2 の既定値。予告どおり）で、関連ノートは全経路で最大の入力2,412ながら**余裕1,684トークン**。**注意書きの実コストは44トークン＝完成プロンプトの1.7%**であり、設計書 §9.3 が「予算の38%」としていたのは**文字数の比率**で、しかも分母が抜粋予算だけだった。**2つの誤差が同じ向きに効いて問題を実際より大きく見せていた**ため、「関連ノート専用の短縮注意書き」案を撤回した（浮く量が余裕に対して意味を持たない）。課題 AI-1 は「注意書きが38%を占める」から「**上げる余地が1,684トークンあるのに使っていない**」へ書き換え、規模感も中→小へ下げた。教訓は lessons L22（文字数の比率とトークンの比率は別物。分母と単位を明示する）。**高密度混在プロファイルのほうがトークンが少ない**という副産物もあった — コード・URL・UUID は英数字中心で日本語より1文字あたりが安く、**プロファイルの狙いは外れたが言語がトークン数を支配することの実測にはなった**。予算値は1つも変えていない | **[ai_input_excerpt](design/ai_input_excerpt.md) §13**・[lessons](lessons.md) L20〜L22 |
| 2026-08-01 | — | **プロンプトの実トークン余裕を測る計測器を入れ、`genai-prompt` beta4 をAARで調査した（AI-1 の前提を解消・MAINT-1 の未確認を解消）**。AI-1（関連ノートは予算600のうち226文字を注意書きに払う）は「実トークン計測が前提」で止まっており、**その計測手段の存在すら未確認**だった。AARを展開したところ **`countTokens()` / `getTokenLimit()` は現行 beta2 に既に存在**し、SDK更新は前提ではなかった。**設計上いちばん危なかったのは判定式**で、`countTokens()` が数えるのは入力だけ、`getTokenLimit()` は**入出力の合計上限**なので、`inputTokens < tokenLimit` で見ると出力予約を二重に使えると誤読する（症状は「入りきらない」ではなく**応答が途中で切れる**側に出るため原因に辿り着きにくい）。`headroom = 上限 − 入力 − 出力予約` で判定する形にした。`maxOutputTokens` は定数を書かず**SDKが埋めた実値をリクエストから読む**ので、SDK版を上げれば自動追随する。**注意書きのコストは単体計測ではなく「あり − なし」の差分で測る**（結合でトークン境界が変わるため単体値は完成プロンプトへの寄与と一致しない）。計測ケースは**12**（機能経路8＋セクション3種・クイズ3形式への分解）で、コード上に3通り混在していた経路の数え方を固定し、**`dependency_policy` の「AI 7経路」が実際にAIを呼ぶ検索ピッカーを数え落としていた**ので8経路へ訂正。**skip 判定は生の `FeatureStatus` のみで行う** — `checkAvailability()` は例外まで `Unavailable` へ畳むため、これで判定すると**SDKの回帰が「非対応端末」に化けて見逃される**（計測呼び出しの例外は skip せず失敗させる）。**これは定義済み計測プロファイルの回帰テストであって入力長の上限保証ではない**点を設計書とKDocの両方に明記した（チャット履歴・候補タイトル・クエリには本番側の上限が無く、関連候補はタイトルだけで予算超過すると収まらないまま返る）。生成と計測が同一リクエストを使うよう `buildRequest()` を切り出し、あわせて `maxOutputTokens` の既定値に関する**誤ったコメントを訂正**（beta2 の既定値はちょうど256で、未指定と256明示は同義だった）。**beta4 調査の結論は「ソース互換だが動作互換ではない」** — 公開APIの削除はゼロで現行の呼び出しは全て健在だが、**未指定時の既定値が 256 → 4096 へ変わる**ため、依存の1行を上げるだけで応答長・推論時間・60秒タイムアウトの余裕・`MAX_TOKENS` の発生頻度・生成Mutexの占有時間がまとめて動く。許容範囲の文字列だけを見て既定値の変化を取り落とさないよう、確認手順も残した。beta4 の追加（`SystemInstruction`・構造化出力・thinking mode・マルチターン `Content`）はいずれも独立した作業が要り依存更新だけでは回収できないため、判断4に沿って**今回は上げず理由を記録**した。予算値は1つも変えていない。JVMテスト508件（変化なし・計測器はandroidTest側）、Lint 0 error / 0 warning / 12 hint、`assembleDebugAndroidTest` 成功。**実機での計測は未実施**（数値が出たら AI-1 の判断へ進む） | **[ai_input_excerpt](design/ai_input_excerpt.md) §13**・**[dependency_policy](design/dependency_policy.md) §5** |
| 2026-08-01 | — | **VERIFY-2（C案の実機確認2件）を検証不要と判断し、課題台帳から削除した**。運用実態として1MB超の巨大ノートを含むVaultが存在しないため、切り詰め文言の表示確認・関連ノート生成速度の確認のどちらも対象ケースが発生しない。実機での動作未確認という点は変わらないが、発生し得ない条件の検証を台帳に残す意味がないと判断した。**コード変更なし** | — |
| 2026-08-01 | — | **`VaultBrowser` 移行（N-7 段階7）の実機確認が完了した（VERIFY-4クローズ）**。さがすタブでのフォルダ列挙・キーワード検索・ランダム、Vault切替時のchipsと検索結果の入れ替わり、補記の生成・一覧・個別削除・一括削除、Vault未選択時の「Vault が選択されていません。」表示を一巡し、問題なし。`ContentResolver` と Vault ルートを `VaultBrowser` の裏へ束ねた変更（世代照合・ハンドル取得契約を含む）が実機のSAF経路でも移行前と変わらず動くことを確認した。これで N-7 は設計・実装・JVMテスト・実機確認のすべてが揃い、TEST-2（実端末を通す統合テストの中身が無い）の着手前提2つがどちらも満たされた状態になった。課題台帳から VERIFY-4 を削除した。**コード変更なし** | **[saf_boundary_gateway](design/saf_boundary_gateway.md)** |
| 2026-08-01 | — | **さがす／補記の Vault と `ContentResolver` を `VaultBrowser` へ束ね、世代照合をJVMテストで固定した（N-7 段階7）**。設計書の処方は「Vault の解決を repository 側へ束ねて controller の引数から消す」で、効果は依存の見た目が整うことだと考えていた。**着手前に測ったら、それだけでは新しく書けるテストが0件だと分かった。** Vault スコープの経路をJVMで検証できない壁は3つある — ①`vaultUri()` が非nullの `Uri` を要る（全テストが `{ null }` を渡していた）②`NoteRepository` が具象クラスで差し替え口が無い ③全公開メソッドが `ContentResolver` を取る（**どのテストも渡していない**）。**1つ外しても残り2つで詰まる。** 証拠はテスト側のコメントに残っていた — `SearchControllerTest` は「検索の実行と `loadFolders` の世代照合は検証できない…実機確認で担保する」、`NoteSessionCoordinatorTest` は `ContentResolver` を作れず **private フィールドへリフレクションで番兵を積んでいた**。3つを同時に外す形として `VaultBrowser`（未選択なら null）と `VaultHandle`（選択中Vaultに束縛された操作）を `data` へ置いた。**新しい発明ではない** — 蒸留と読書痕跡は既に `Saf*Gateway` が `ContentResolver` を構築時に束ねる形で、**7機能のうち2つだけ解けていて残りが取り残されていた**（テーマ8）。**「Vault未選択のとき何を出すか」は controller に残した** — さがすは黙って返り補記はエラーを出すので、下へ押し込むと差が消える。**ハンドルは開始時に1回だけ取る**（`ReadingTraceStore` と同じ規約。モデルDL完了後の再開だけは数分後になり得るので引き直す）。**JVMテスト +21件**（`SearchControllerTest` +11・`AnnotationControllerTest` +9・`NoteSessionCoordinatorTest` +1）（世代照合3種・検索とランダムの実行・走査キャッシュのヒットと破棄・削除失敗の件数・ハンドルを取り直さない契約）で、**5つのガードを1つずつ削る変異で対応テストが落ちることを確認済み**。走査キャッシュの破棄は番兵をやめて実際に走査させる形に置き換えた（Jobの停止だけは外から観測できないので番兵が残る）。副次的に、使われなくなった `vaultUri` パラメータと `ContentResolver` の import が Coordinator から落ち、**`controller` パッケージから `android.*` が完全に消えた**ので `PackageDependencyTest` の Android 非依存リストへ `controller` を加えた（`model`・`domain`・`controller` の3層。import 1行の注入で落ちることを確認済み）。`android.net.Uri` を import するファイルは 17→8 になり、**残る8は `data` 7・`NoteViewModel` 1 でいずれも境界そのもの**。教訓は lessons L19（設計書に処方だけ書くと効果の見積もりが検証されないまま残る。**効果の検算を1つ書く**）。テスト487→508件、Lint 0 error / 0 warning。**実機確認は未実施** | **[saf_boundary_gateway](design/saf_boundary_gateway.md)**・[architecture](design/architecture.md)・[lessons](lessons.md) L19 |
| 2026-08-01 | — | **ドキュメント参照を `DocumentRef` へ移し、`model` / `domain` / `ui` から Android を追い出した（N-7 段階1〜6）**。`model` の4型と `domain` の1ファイルが `android.net.Uri` を持っていたため、**`Uri` を含む型を素のJVMテストで組み立てられず**（`Uri` はユニットテストではスタブで例外を投げる）、検索実行や補記保存の世代照合は実機確認だけが担保だった。`NoteFile`・`RelatedNote`・`HistoryEntry`・`AnnotationState.savedUri` を不透明な `@JvmInline value class DocumentRef` へ置き換え、`Uri` との変換は `data/SafDocuments.kt` の2関数に閉じた。`import android.net.Uri` を持つファイルは **17→10**（`model` 4→0・`domain` 1→0・`ui` 1→0）。**段取りは設計書のとおりにならなかった** — 「最小の `HistoryEntry` から1型ずつ」という当初案は実行不可能で、3型を同時に移すしかなかった。`ui/SearchScreen` が `HistoryEntry` から `RelatedNote` を、`domain/RelatedNotesUseCase` が `NoteFile` から `RelatedNote` を組み立てるため、片方だけ移すと組み立て側に新旧の変換が要る。ところが `ui` と `domain` は依存規約上 `data` を import できず変換関数に手が届かない。**「変換を `data` に閉じる」という判断そのものが部分移行を禁じていた**（教訓は lessons L18 — 移行の単位はサイズではなく「同じ変換関数に手が届く範囲」で切る）。**副次的に往復変換が2つ消えた** — `SavedAnnotation` は gateway が返す不透明文字列を一度 `Uri` へ戻していた、`reloadNoteBody` は `Uri.toString()` 由来の `targetUri` を `Uri` へ戻していた。どちらもそのまま参照へ包む形にした。**完了条件として `PackageDependencyTest` に「`model` と `domain` は Android に依存しない」を追加**。従来の依存テストは `com.example.newproject.*` の import しか見ておらず **`android.*` を数えていなかったのが穴の正体**で、`ui` を対象にしないのは Compose 自体が `androidx.*` だから。`model`・`domain` それぞれへ import を1行注入して落ちることを確認済み。**payoff の実例**として、`NoteSessionCoordinatorTest` にあった `listOf("旧Vaultの履歴") as List<HistoryEntry>`（「`HistoryEntry` は `Uri` を要るので作れない」というコメント付きの強制キャスト）を実物の組み立てに直した — **この refactor が狙っていた痛みが、そのままコメントとして残っていた**。**永続化のスキーマは変えていない**（`NoteHistoryStore` のJSONキーは `"uri"` のまま。保存済みの文字列表現に型を付け直しただけ）。テスト486→487件、Lint 0 error / 0 warning。**2026-08-01 に実機確認済み**（Rediscover・さがす検索/ランダム・今日読んだノート・補記の生成/削除・関連ノートの一巡、問題報告なし。移行前に保存された履歴JSONも問題なく読めた＝互換の証明） | **[saf_boundary_gateway](design/saf_boundary_gateway.md)**・[lessons](lessons.md) L18 |
| 2026-08-01 | — | **N-7 の段階7（Vaultルートを controller の引数から外す）には意図的に進まない**。段階1〜6でドキュメント参照を `DocumentRef` へ移し実機確認まで終えたところで、残る段階7を評価し直した — `vaultUri: () -> Uri?` という素通しの引数を1段削るだけの整理で、型を変える話ではなく、L-3（実端末を通す統合テストの中身）着手の前提でもない。**作業スコープを広げずに TEST-2 本体へ進める状態が既に整っている**ため、ここで一区切りとした。設計書・ロードマップ・課題台帳の記述を「未着手」から「意図的に進まない」へ揃えた。必要になれば独立した改善として再検討する。コード変更なし | **[saf_boundary_gateway](design/saf_boundary_gateway.md)** |
| 2026-08-01 | — | **instrumentation の土台が動くことを Android 16 で実証した（TEST-1 クローズ）**。2026-07-31 の初回実行は 0 success / 2 failure（`NoSuchMethodException: `android.hardware.input.InputManager.getInstance []`）で、失敗はテスト本体へ入る前のアイドル待機初期化で起き、Contextを見るだけのテストにもクラス共通の `createComposeRule()` が効いていたため2件とも巻き添えになっていた。`6f21419` の対策（Context確認とCompose描画のクラス分離＋`espresso-core` 3.6.1→3.7.0・`ext:junit` 1.2.1→1.3.0 の限定更新）を入れた状態で **2026-08-01 に `connectedDebugAndroidTest` を再実行し 2/2 成功**（Pixel_10_Pro_Fold AVD・Android 16 / API 36。`ComposeRenderingSetupTest` 3.9秒・`InstrumentationSetupTest` 0.001秒）。**クラス分離と依存更新を1コミットで同時に入れたため、どちらが効いたかは切り分けていない** — 実害が消えたので追わない判断とした（必要になるのは再発時）。これで「Runnerが起動しComposeを描画できる」が初めて実証され、以後の instrumentation の失敗は**土台の故障とテスト対象の故障を切り分けられる**。**CIは `assembleDebugAndroidTest`（組み立てまで）なので、この種の失敗は今もCIを素通りする**点は変わらない — 常時保証したいならエミュレータを回すジョブが要るが、現時点では入れていない。課題台帳から TEST-1 を削除し、ロードマップの N-6 も閉じた（L-3 の前提として残るのは `Uri` の gateway 化のみ）。**コード変更なし** | — |
| 2026-08-01 | — | **SAF境界を gateway の裏へ入れる設計を確定した（実装は未着手）**。2026-07-27 の `PackageDependencyTest` は `com.example.newproject.*` の import しか見ておらず **`android.*` を数えていなかった**。そのため `model` は「プロジェクト内の何も import しない」を満たしながら `android.net.Uri` だけは import する状態で残り（4型）、`domain` にも1ファイル残っている。`Uri` は素のJVMでスタブが例外を投げるため、これらの型を組み立てられず**検索実行・補記保存の世代照合は実機確認だけが担保**になっていた。**葉であることを「向き」だけで定義したのが穴**で、テスト容易性の観点ではプロジェクト内依存も外部フレームワーク依存も等しくその層をJVMで扱えなくする。設計の要点は**`Uri` が2種類あり直し方が違う**こと — Vaultルート（controllerが解釈せず repository へ素通しするだけ）は**不透明化しても何も良くならない**ので controller の引数から消す側、ドキュメント参照（`distinctBy`・`!=`・`in` でキーとしてのみ使う）は `model` の `@JvmInline value class DocumentRef` にする側。先行2 gateway は生の `String` だが、今回は `model` のフィールドになるため `title` との取り違えを防ぐ型を付ける。**永続化のスキーマ変更は不要**（`NoteHistoryStore` も蒸留も既に `uri.toString()` で保存済み＝既にある文字列表現へ型を付け直すだけ）。**完了条件は `PackageDependencyTest` へ `model` / `domain` の `android.*` 禁止を足すこと** — 「追い出した」は主張だが「戻ってこない」はテストにしかできない。段取りは**葉から7段階**で、2段目に `HistoryEntry`（2フィールド・消費2箇所）を置き、**最小の型で効くことを確かめてから `NoteFile` 本体へ入る** | **[saf_boundary_gateway](design/saf_boundary_gateway.md)**・[architecture](design/architecture.md) |
| 2026-08-01 | — | **依存更新の方針を確定し、Lint更新系3チェックを `disable` から `informational`（hint）へ変えた**。`NewerVersionAvailable`・`GradleDependency`・`AndroidGradlePluginVersion` は方針が決まったら `disable` から外すつもりだったが、**素のまま有効化すると12件すべてが Error になり `lintDebug` タスクが失敗する**（`warningsAsErrors` ＋ `abortOnError` のため。落ちるのは Lint タスクとCIジョブで、APKのコンパイルではない）。更新系は他のLint警告と種類が違い、**自分のコードを1行も触っていないのに上流が新版を出すだけで指摘が生える** — ゲートに載せると赤の意味が薄まり、「依存を一括更新しない。機能単位で上げ、実機確認を伴う」とも衝突する。**当初はここで「`disable` を維持するしかない」と結論したが、これは誤りだった** — Lint の severity は `error`/`warning`/`informational`/`ignore` の4段階で、`informational` は `warningsAsErrors` の昇格対象にならない。実測すると**「0 errors, 0 warnings, 12 hints」で BUILD SUCCESSFUL**、12件はレポートに残る（`--offline` でも同じ）。**「警告0」を保ったまま催促だけ残せる**ので `disable` より厳密に良く、一時有効化して戻す手順（戻し忘れでCIが赤になる）も不要になった。更新の単位を**7グループ**に分け、それぞれの上げ方と確認範囲を定義（ML Kit GenAI は必ず単独＋AI 7経路の実機一巡＋**上げる前にAARで制約確認**、Compose BOM は BOM ごと、AndroidX Test は本番非影響の唯一の枠、など）。棚卸しの**契機はカレンダーではなくマイルストーン**（この課題は既に一度「いつか決める」で放置されており、日付基準は守られなかったときに気づく仕組みが無い）。**初回棚卸しの結果を §4 に記録** — Gradle本体1件＋ライブラリ11件が古く、注目は `genai-prompt` beta2→beta4（アプリの心臓部・変更点未確認）と、`platform()` 宣言のため**Lintの自動検出から漏れる Compose BOM**。`NewerVersionAvailable` は毎回ネットワークへ問い合わせるためLintが概ね2倍遅くなるが、hint を残す価値を優先した。あわせて課題台帳の実態ずれを解消した（instrumentation の対策2件は `6f21419` で適用済みだったのに「対応候補」のまま／`build.gradle.kts` のコメントずれは既に解消済み）。**コード変更はLint設定1箇所のみ** | **[dependency_policy](design/dependency_policy.md)**・[lessons](lessons.md) L17 |
| 2026-07-31 | — | **ReadingTrace v1 を完了とし、残っていた実機確認4件をクローズした**。到達率・背面化復帰・Vault分離・検索フォールバックの4項目は、**いずれも対応するJVMテストが既にある**（設計書 §10）。実機でしか確かめられないのはロジックではなく境界だけ — 実 `ContentResolver` と実 Activity の `pause`/`resume` が Fake と同じ順序・粒度で振る舞うか、の一点に絞られる。加えて**再会カードは Rediscover 限定なので、痕跡を記録したあと結果を目視するには同じノートを 1/N の純粋ランダムで引き当てる必要があり**、数百ノートのVaultでは実質不可能だった。引きを操作できるようにするのは守るべき性質のほうを壊す。壊れた場合の検知は `_ReadingTraces` のファイル数とカード文面の違和感で事後に足りると判断し、**課題台帳から削除して通常運用へ戻した**。違和感が出たら症状つきで起票し直す（症状があれば再現条件を作れる）。判断の3条件と適用範囲は設計書 §12、教訓は lessons L16 | **[reflect_reading_trace](design/reflect_reading_trace.md)**・[lessons](lessons.md) |
| 2026-07-31 | — | **AI補記の書込失敗時に後始末し、消せなければ残骸を伝える（F-2の2件目）**。`createAnnotationFile()` は `createDocument()` の直後に対象URIへ書き込むだけで、ストリームを開けない・途中で例外になった場合の削除も保存後の検証も無かった。画面には生成失敗と出るのに `_AI補記` に残骸が残る。手順（作成→書込→検証→失敗時の後始末）を `AnnotationFileWriter` へ切り出し、SAFの4操作だけを `AnnotationDocumentGateway` の裏へ寄せて**Android非依存にしたので失敗注入テストが書ける**（読書痕跡の gateway と同じ形。参照は `Uri` ではなく不透明な文字列で扱う）。どの段階で失敗しても作成済みファイルの削除を試みる。**削除はSAFプロバイダ側の都合で失敗し得るので「必ず消える」とは言えない** — 消せなかった場合は失敗メッセージへ「不完全なファイルが残った可能性」を足して伝える（削除失敗で例外にはしない。元の失敗理由が後始末の失敗にすり替わるため）。**握りつぶさず、元の理由に足す**のが要点で、レビュー指摘（削除結果を捨てると利用者が残骸に気づけない）を受けて `delete()` は `Boolean` を返す形にした。**中断（`CancellationException`）でも作りかけを消してから再throwする。** 書込後は読み直して全文一致を確かめ、部分書込を失敗として扱う。**表示名は予測をやめ保存後のメタデータから取る** — 同じノートを同じ分に再生成するとプロバイダが改名し、`AnnotationState.Success.fileName` と実名がずれていた。書込失敗時の削除・読み直し検証・実名取得・中断時の削除・削除結果の無視・過剰通知の6変異で対応テストが落ちることを確認済み。テスト474→486、Lint 0 errors / 0 warnings。**2026-07-31 に実機確認済み**（問題報告なし） | — |
| 2026-07-31 | — | **蒸留の復旧チェックを追跡Jobへ載せる（F-2の1件目）**。`DistillController.checkRecovery()` だけが裸の `scope.launch` で、Jobも世代も持たなかった。復旧確認はSAF I/Oを伴うので遅れることがあり、その間にユーザーが蒸留を始めると「復旧警告を出す → 走行中の分析が候補を返して警告を上書きする」順序が成立していた（保存層が未解決レコードを再確認するので危険な書き込みは止まるが、データ安全性の警告が消えて不要なAI実行も走る）。専用の `recoveryJob` を持たせ、次の `checkRecovery()` が始まったら取り下げる。**分析の `activeRequestId` へ相乗りさせないのは寿命が違うため** — 復旧レコードはアプリ内部の未解決1件でノートにもVaultにも紐づかないので、ノート切替で打ち切ると警告が黙って消える（`cancelForNoteChange()` は触らない）。復旧結果の反映は `applyRecoveryResult()` へ集約し、**表示の直前に分析世代を無効化する**（先に表示すると直後の候補が同じ世代のまま上書きする）。3種の `RecoveryRequired` 構築を純関数へ寄せ、起動時確認と復元操作後の表示で共有した。世代照合は変異確認により冗長と判明したので削除。テスト471→474、Lint 0 errors / 0 warnings。**2026-07-31 に実機確認済み**（問題報告なし） | [architecture](design/architecture.md) |
| 2026-07-31 | — | **表示用Markdownの解析をMain外へ出し、通常表示と全画面で1回だけにする（F-1）**。`NoteReaderTab` と `FullscreenNoteScreen` がそれぞれの `remember` で `buildNoteSectionModel()` を同期実行しており、表示フォールバックは最大1MB・同じパーサのAI入力側実測は1MB約460ms／200KB約20msだったため、長文を開く瞬間と全画面へ入る瞬間にUIが止まり得た（Composableが別なので結果も共有されず二重解析）。`NoteSectionController` を新設して `Dispatchers.Default` で1回だけ解析し、`StateFlow<NoteSectionModel?>` を `MainActivity` から両画面へ配る構成にした。**状態を `NoteUiState` へ入れないのは、`model` パッケージが葉で `domain` を import できないため**（テーマと同じく独立フロー）。本文が変わる経路は `setNoteState` と `applyReloadedBody` の2つしかないので解析開始を Coordinator の同2箇所へ集約し、停止は `cancelNoteScopedJobs()` へ登録した。**解析中に本文を描かないガードが要点** — 描くと `MarkdownNoteContent` のフォールバックが働き最大1MBをMainで解析し直すため、タイトルと枠だけ先に出して本文の到着を待つ（プレースホルダ文言は数十文字なのでフォールバックのまま）。**requestIdガードは書いた上で変異確認により冗長と判明したので削除した**（`parse`／`cancelAndClear` がどちらも先にJobをキャンセルするため到達経路が無い）。解析の非同期化・切替時の破棄・蒸留差し替え時の再解析・描画ガードの4つは、1行ずつ消して対応テストが落ちることを確認済み。テスト459→471、Lint 0 errors / 0 warnings。**2026-07-31 に実機確認済み**（問題報告なし） | **[architecture](design/architecture.md)** |
| 2026-07-29〜30 | — | **ライト配色のAA是正とリリース構成の整備（D案・E案）。レビューで2度差し戻し、3度目で通した**。**①検証形式**: 当初は全色を最も明るい `panel #FDFEFF` の上で測っており、実際に文字が載る `panelBlue` では 4.19〜4.41、ボタンが載るグラデーション直上では 1.21〜1.84 と、いずれも基準を割ったまま「準拠」と判定していた。1度目の指摘で「トークン×実際に載る面」の対応表による総当たりへ作り直し、**2度目の指摘で「対応表に載せ忘れた経路」が残っていることが分かった**（グラデーション直上の白文字を最も有利な `Indigo` 停止色でだけ測っていた／`copy(alpha)` と `onVibrant` を覆えていない／停止色がテストと実装で二重管理）。停止色を `AppColorScheme` の単一ソースにし、代表を選ばず総当たりする形へ。**②文字色**: グレーを5→3段階へ統合し、基準面を弱い文字が載る最も暗い `panelChip #EEF0FF` に統一して `onSurfaceFaint #6D6D6D`・`onSurfaceMetaBlue #656C85`・`aiHeading #0C796C`・`relatedHeading #6054DE` へ再決定（見出し2色は11spで大文字例外に入らず、見分けの根拠が色相差しか無いため**3色とも色相を1度も動かしていない**）。**③グラデーション上の文字**: 白文字は Aqua 停止色で 2.07、濃い文字にすると Indigo 停止色で 2.62 と**文字色では解けない**。共通部品 `GradientHeader` が背景ごと持つ形にし、最初は `LogoNavy` α=0.42 の暗幕を敷いたが**実機で帯として重くカードにしか見えず差し戻し**、`Panel` α=0.35 の白ヘイズ＋`LogoNavy` タイトル＋`#202124` 副題へ反転（最悪 Indigo 上でタイトル6.15／副題5.12）。角丸をやめメイン領域の全幅へ抜き、フェード10dp。ダークは何も敷かず白文字のまま据え置くため**面と文字が明暗で反転する**ので、反転していること自体もテストで固定。**④帯の外**: 検索欄・フォルダチップ・空状態・進捗インジケータ・戻るボタン・linkedバッジ・ナビ未選択・字幕など12箇所を不透明面＋対の前景へ寄せ、`accentGlass` もライトを不透明化。**⑤禁止の強制**: `VibrantTextUsageTest` を新設し、画面からの `onVibrant` 直接使用と文字色への任意の `copy(alpha)` をソース走査で禁じる（許可は背景を所有する部品だけで、許可リストへ足したら対応する比を `AppColorContrastTest` へ足す決まり）。**⑥ボタン**: グラデーション直上では3役すべてが未達（Ai は Indigo 停止色と同色で 1.00）。全停止色に3:1を満たす塗りは L≤0.007 か L≥1.47 しか無く色では解けないため、WCAG 1.4.11 の隣接輪郭（`LogoNavy` 1dp）で解いた。パネル上のボタンには付けない。**⑦導線**: Vault未選択時に目立つピンクが機能しない「別のノートをひらく」に付いていたので役割を入れ替え、未選択時は無効化。空状態の本文パネルが `weight(1f)` で伸びて白が画面の大半を占めていたのを内容なりの高さへ。**⑧リリース構成**: `buildTypes`（R8は未有効・keepルール検証が要る）・`lint {}`・androidTest基盤とスモークテスト・Java 11・`applicationId` を `com.vigilith.ai` へ。**⑨警告ゲート**: `lint { warningsAsErrors }` は Android Lint にしか効かないという指摘を受け Kotlin の `allWarningsAsErrors` を追加。CIへ `assembleDebugAndroidTest` も追加。**⑩その他**: 補記のファイル名日時を `Locale.ROOT` へ固定（仏暦ロケールで `yyyy` が2569になり名前予測とずれる潜在バグ）、`Date()` の二重生成を1回に、旧OPの未参照ロゴ686KBを削除。テスト450→459。色・輪郭線・警告ゲートはいずれも旧値へ戻す変異で落ちることを確認済み。**2026-07-31 にエミュレータで5画面＋ダークモードを一巡し、その過程で暗幕→白ヘイズの反転と操作ボタンの配置是正を行った**（ダークは据え置きのとおり変化なし）。**E案も同日確認済み**で、`applicationId` の変更どおり別アプリとしてインストールされることを確認した（旧 `com.example.newproject` は端末に残るため、アンインストールが要る） | **[theme_and_ui_refactor](design/theme_and_ui_refactor.md)**・[dark_mode](design/dark_mode.md) |
| 2026-07-28 | — | **現行コードの品質を再評価**。`ac53749`を対象にJVMテスト450件全成功、Lint 0 errors / 21 warnings、release APK組み立て成功（unsigned・約11MB）を確認し、総合評価を7.2→8.0へ更新。前回の主要指摘だった非同期世代管理、依存・状態境界、用途別読込上限は解消済み。新たに、表示用Markdown解析のMain実行、蒸留復旧チェックの要求管理外、AI補記の書込失敗時に空・部分ファイルが残る可能性を課題登録した | — |
| 2026-07-27 | — | **AI入力の先頭固定長切り出しを、見出し骨格＋冒頭＋末尾の抜粋へ置換**。`PromptBuilder` 7経路から本文の `take()` を除去し、呼び出し側が用途別上限（要約/クイズ1200・補記/セクション1500・関連600文字）で `NoteExcerpt` を作る構成へ変更。予算内はMarkdownを解析せず従来プロンプトと1文字単位で同一、超過時だけlevel 3までの見出しを全体から均等選抜し、冒頭60%・末尾40%を明示ラベル付きで渡す。単一巨大ブロックも捨てずに両端を切り出し、コード断片は閉じフェンスを復元する。抜粋注意書きまで既存予算内へ予約し、フォーカスセクション推薦と予算値変更は別判断として見送った。**レビュー対応**: 最大1MBのMarkdown解析がMainを占有する問題を、5クラス・7経路の抜粋生成だけ `Dispatchers.Default` へ移して解消し、全呼び出しをソース走査テストで固定。正規化だけで全ブロックが収まる場合にも合う注意書きへ改め、本文との区切り改行1文字も共通prefixとして予算へ含めた。末尾追加・均等選抜・注意書き予約の3変異で対応テストが落ちることを確認。テスト425→450、Lint warning増加なし。抜粋の中身（捨てる場所・骨格・予算配分・正規化の副作用・モデルへの伝え方）は設計書1本へ独立させた。**2026-07-28 に実機確認完了**（[PR #43](https://github.com/miyakemasaki0808-MM/ObsidianMind/pull/43)）、問題報告なし | **[ai_input_excerpt](design/ai_input_excerpt.md)**（新規）・[architecture](design/architecture.md)・[related_notes_ai](design/related_notes_ai.md) |
| 2026-07-27 | — | **依存と状態の境界を型とCIで守る（B案・3段階）**。**①依存注入と調停分離**: `NoteViewModelDependencies` へ Repository・AiClient・UseCase・永続化・設定/履歴・`CoroutineScope` の組み立てを出し、Android境界だけを `NoteViewModel` に残した。7 Controllerの生成・状態所有・ノート/Vault切替はAndroid APIを呼ばない `NoteSessionCoordinator` へ移し、実物Controllerを束ねたJVMテストで一斉停止・一斉初期化・世代更新を固定した。当初の「ViewModel越し」は、素のJVMではAndroidスタブを生成できずRobolectric/モック依存が要るため「調停クラス越し」へ読み替えた。**②状態所有**: 状態型を `model/state/` へ分割し、`NoteUiStateStore` だけが全体Flowを所有。Controllerには機能別 `*StateWriter` だけを渡して担当外フィールドを書けなくした。テーマは独立Flowへ出し、ノート切替のリセットと`Loading`遷移は1回の更新へ原子化した。**③依存方向**: 共有データ型を葉の `model` へ移して4組の循環を解消し、`model`は依存なし／`ai→model`／`domain→model,ai`／`data→model,domain`／`controller→model,data,domain,ai`／`ui→model,domain` をimport走査テストでCI固定した。`model→data` の変異でテストが落ちることも確認。画面APIと見た目・挙動は変更なし。Vault/ノート切替・テーマ・蒸留保存・補記/クイズ通知・再会カードの実機回帰確認済み。テスト410→425、Lint warning 28件で変化なし | [architecture](design/architecture.md) |
| 2026-07-26 | — | **入出力に用途別の予算と結果の扱いを持たせる（読込予算・累計回数の分離・保存スコープの3件）**。3件とも原因は同じで、**入口で量を決めず、出口で結果を見ていない**こと。**①読込予算**: `readNoteContent()` が `readText()` でEOFまで読んでいた。この無制限経路は「大きすぎて蒸留できないノートの表示フォールバック」と「関連ノート候補の本文読込」で使われる。皮肉なことに、蒸留を256KBで止めたノートほどこちらへ落ちてくる。関連ノートは候補40件を最大8並列で読むため、巨大ファイル1つでAI入力の文字数上限が効くより前にメモリとI/Oを消費していた。用途ごとに入口を分けた（`readNoteForDisplay` 1MB／`readNoteSnippet` 8KB／`readNoteSnapshot` 256KB）。`maxBytes` を引数で受けないのは、呼び出し側が「とりあえず大きめ」を渡せると上限を置く意味が薄れるため。上限で切ると多バイト文字が割れるので `dropIncompleteUtf8Tail()` で末尾の不完全なシーケンスを落とす（日本語は3バイト文字が並ぶため上限がどこに来ても割れうる）。`readAtMostBytes()` は上限に達した時点で読むのをやめ、続きの有無は1バイト覗いて判定する（上限ちょうどのノートを「切り詰めた」と誤表示しないため）。**②累計回数**: `visits` は直近30件しか残さないのに、その件数を「これまで開いた回数」として使っていた。表示が「30回」で止まるだけでなく、`needsAiSummary` の判定も止まるため**31回目以降どれだけ読んでもAI俯瞰要約が二度と更新されず、古い要約が「最新」として出続けていた**。`totalVisitCount` を足して schema v2 へ。既定値を `visits.size` にしたので既存の構築箇所は無改修。ただし **`copy()` は既定値を再評価しない**ため、背面化→復帰→離脱で訪問を差し替える経路は累計だけ二重に増える。この不変条件を `withoutLastVisit()` に閉じ込めた（保持件数は30で頭打ちになってこの誤りを隠すので、累計そのものを見る回帰テストを置いた）。v1→v2 の移行は decode 側で、**v1 の checksum を v1 の正規形で照合してから**現行版へ寄せる（順序を逆にすると既存の痕跡が全部「破損＝孤立」になる）。v1 に `totalVisitCount` が書き足されていても読まない（v1 の checksum はこの値を含まず、読むと任意の回数を名乗れる入口になる）。**③保存の寿命**: 保存が `viewModelScope` に載っており、タスクスワイプでは `onStop()` → `pause()` の直後に `onCleared()` が走ってIOへ渡る前にキャンセルされていた（背面化のみなら成立するので気付きにくい）。アプリ寿命のスコープへ移し、`ReadingTraceSaveResult` を受けて失敗時は `dirty` を戻す。`ProcessLifecycleOwner` を使わないのは `lifecycle-process` の依存追加に対し必要なのが「`onCleared()` で死なないスコープ」1つだけだから。`persistSummary` は対象外（書けなくても次回の再会で作り直される自己修復）。**テストは全12ケースで変異検証を行い**、対応する修正を1行外すと落ちることを確認した。うち初版の再試行テストは `resume()` が無条件に `dirty` を立てるせいで巻き戻しを経由せず通っていたため書き直している（A案と同じ型の落とし穴が再発した）。テストは391→410、Lint warning 28件で変化なし | [reflect_reading_trace](design/reflect_reading_trace.md) |
| 2026-07-26 | — | **非同期の境界を世代IDで揃える（後着する4経路とエラーの握りつぶし）**。裸の `scope.launch` のまま要求単位に追跡されていなかった4経路を塞いだ。**着手前の再確認で優先度付けを訂正した** — 「要約のモデルDLだけが実害まで到達する」と起票していたが、この経路が走るのは `SummarizeUseCase` が `AiNeedsDownload` を返す**モデル未DL時のみ**で、DL済みなら `Available` 経路（`CancellationException` を再throw済み）に入り `cancel()` だけで止まる。日常的に踏むのは `selectFolder` だった。**①スコープ切替**: 検索実行中に別フォルダのchipをタップすると前のスコープの結果がそのまま出ていた（`selectFolder` は `selectedFolder` を差し替えるだけだった）。`startRequest()` で採番を進めて `searchState` を Idle へ戻す。同じchipの再タップは早期returnして取り違えで結果を失わないようにした。**②Vault世代の新設**: 補記一覧・フォルダ一覧は**ノート切替では無効化してはいけない**（補記管理画面はノートと無関係）ため、既存の `cancelAndClear()` に相乗りできない。`NoteViewModel` に `vaultGeneration` を持たせ `saveVault()` で採番し、Vault単位の要求だけがこれを `update` 直前に照合する二層構成にした。`vaultUri` の比較で代用しないのは A→B→A の選び直しで同じ値になるため（キャッシュは破棄されるので無効化したい）。**③補記一覧・削除**: `listJob` 1本に載せ、`deleteAll` は削除対象を起動時の一覧で固定したうえで1件ごとに世代を見る（永続URI権限が残っている端末では切替後も旧Vaultを消せてしまうため）。**④要約DL**: ViewModel直書きのこの1本だけがController化から取り残されていたので、最小修正ではなく Quiz/Annotation/Distill と同型の `SummaryController` へ切り出した。関連ノートは走査キャッシュ（Uriを持つ `NoteFile`）に依存するためViewModelに残し、DL完了後の再実行は `onModelReady` で呼び戻す。結果としてこのクラスはAndroid依存を持たず、**唯一 Uri 非依存でフル検証できる経路**になった。**⑤3-2**: `deleteDocument()` の Boolean を集計して失敗件数を一覧の上に出し（Error に倒すと一覧が消えて再削除できない）、`loadFolders` の空 `catch` をやめて chips の下に注記を出す。**テストの落とし穴を1つ潰した** — `SummaryControllerTest` の初版は `cancelAndClear()` 経由で書いたため `downloadJob` が止まってガードに到達せず、**ガードを外しても落ちなかった**。DLを生かしたまま次の要求だけを進める形に直し、`setStateIfCurrent` と DownloadCompleted のガードそれぞれを外すと対応するテストが落ちることを確認した。**保証していない範囲**: `loadFolders` と補記一覧・削除の世代照合は非nullの `Uri` を要するためJVMテストで固定できず、実機確認で担保する。テストは379→391、Lint warning 28件で変化なし | [architecture](design/architecture.md) |
| 2026-07-26 | — | **検索の世代管理**。`SearchController` だけが Job も requestId も持たず無条件に `scope.launch` していた（他5 Controller は実装済み）。SAF走査はスコープキャッシュを跨ぐと所要時間が大きく変わり完了順が保証されないため、連続検索でも**検索⇄ランダム切替でも**古い結果が新しい結果を上書きし得た。他5本と同じ `activeRequestId` ＋ `Job?.cancel()` 方式へ統一し、検索とランダムは同じ `searchState` を奪い合うのでJobは1本を共有する。あわせて `catch` から `CancellationException` を除外した（①だけ直すと「キャンセルがエラー表示に化ける」が顕在化するため分割不可）。**その過程で `SearchPickerUseCase.pick` も `CancellationException` を `PickerResult.Error` へ畳んでいたことが判明**。中断せず正常に戻るため呼び出し側の `catch` では捕まえられず、`cancel()` だけでは防げない型だった（`SummarizeUseCase` / `RelatedNotesUseCase` は既に再throw済みで、本UseCaseだけが外れていた）。UseCase側の再throwと Controller の requestId ガードの二重で塞いだ。Vault切替（`onVaultChanged`）でも走行中の要求を止めるようにした（旧Vaultのノートが、切替後に Idle へ戻したはずの `searchState` を上書きして現れるため）。`scopeNotesCache` が素の `mutableMapOf` で成立している前提（Main単一スレッド）もコメントで明文化。**自動テストは付いていない** — `SearchController` は `ContentResolver` を、`NoteFile` は `Uri` を要求し、`Uri.EMPTY` がスタブでnullになるためJVMテストで生成できない（既存テストが全て「Uri非依存」なのも同じ理由）。モック導入か `NoteViewModel` の依存注入によるテスト用シーム整備が前提になる。テスト379件・Lint warning 28件で変化なし | [ai_picker](design/ai_picker.md) |
| 2026-07-26 | — | **軽量課題5件の一括消化**。**未使用import**: コメント内で型名に触れるためだけに残っていた未使用import 6件を削除し、コメント側はバッククォートで型名を残した。うち `NoteSections`→`ui.markdown`、`AnnotationComposer`→`controller` の2件が残存依存だったため、**これで「ビジネスロジックからUIへの依存が消えた」（PR #37の主張）が実コード上も成立**（パッケージ間循環3組は型の移動が要る別課題として残る）。**可視性**: `Float.fractionBetween` を `internal`→`private` へ。呼び出しは `VigilithOpeningMotion.kt` 内5箇所のみで、同パッケージへ公開したままだと `VigilithMascotMotion` 側に同名の別定義が生まれる余地があった。**テスト網羅漏れ**: `Summarizing` の `lensGlowAlpha` が `Idle` を上回る（ベース 0.18 vs 0.08）という意図的な輝度差は、既存テストが各モード**内**の変化しか見ておらずモードを跨いだ大小関係を固定していなかったため、同一 `loopFraction` 同士の比較を追加。**CI導入**: `.github/workflows/ci.yml` を新設し、PR と main への push で `testDebugUnitTest` / `lintDebug` を実行。JDKはローカルの Android Studio 同梱 JBR に合わせて 21、失敗時の追跡用にテスト・Lintレポートを artifact 化、同一ブランチの連続pushは古い実行を打ち切る。**バックアップ除外**: `allowBackup="true"` かつ除外ルール未指定で `random_note_prefs`（Vault の SAF URI・テーマ・当日履歴のタイトル）がクラウドバックアップに含まれ得た。`dataExtractionRules` は **API 31以上でしか効かず minSdk 26 を覆えない**ため、API 30以下向けの `fullBackupContent` と両方を指定。API 31以上では cloud-backup に加え device-transfer からも除外した（SAFの永続URI権限は移行先端末で無効になり、復元しても壊れた参照が残るだけのため）。`applicationId` の変更は正式ID決定が要るため見送り。テストは378→379、Lint warning は28件のまま | — |
| 2026-07-26 | — | **ダークモード（オプション画面での明示切替）**。2026-07-22に「独自配色との整合コストに対し不要」として死亡宣告した項目を、整合コストの正体が色設計の負債と判明したため再オープンし実装。**切替はオプション画面のトグルのみ**（`isSystemInDarkTheme` によるOS追従は不採用。ブランド由来の独自配色がOS設定で勝手に変わるのを避ける）。`SharedPreferences` に真偽値1つを保存し、プロセス再起動なしで即時反映。**暗色パレットの土台は `QuizScreen` が単独で持っていた既存のダーク配色**（背景 `#1A1C2E` ／面 `#2A2D45`）。机上で新色を起こすより、実装済みで目視を通った値から広げた。**`Indigo` はボタン以外にも約30箇所で文字色として使われており**（AIタブ見出し・チャット・関連ノート・補記結果・再会カード）、暗面では 2.83 で読めないため役割トークン `AccentText` を新設。文字は4.5:1が要るので塗り用 `#8A80FF`（4.24）とは別に `#A79EFF`（5.76）を使う。下部ナビ／レールも `Indigo` 固定ではダークで明るい帯が残るため `NavBar` をトークン化。`themes.xml` の statusBar/navBar の `@color/indigo` 直指定を透明化し `enableEdgeToEdge()` に委譲。Vigilithは暗背景で胴体が溶けるため、WebPは再生成せず背後に `LogoPurple` 22%のhaloを敷いて輪郭を確保。既存の役割トークンを `@Composable` の窓口へ変えることで**呼び出し側163箇所を1行も書き換えずに**テーマ追従へ移行した。クイズ画面のエラー2色（`#CC0000`＝2.86／`#555555`＝2.25で読めなかった）も修正。テストは372→378（明暗両方のコントラストを固定）。**レビュー指摘3件を修正**: ①引数なしの `enableEdgeToEdge()` はシステムバーのアイコン明暗を**OSの uiMode** から決めるため、「OSライト＋アプリダーク」で暗い背景に暗いナビアイコンが描かれていた（`isAppearanceLightNavigationBars` を明示固定）。②`errorText` / `dangerAction` を Badge・チップの `containerColor` に流用したうえ白文字を固定していたため、暗所で「明るい赤に白文字」（2.52〜2.78）になっていた（塗り用の `errorSurface` / `onErrorSurface` / `onDangerAction` を新設し塗りと前景を対で持たせた）。③Vigilithの Ready バッジが `Indigo` 固定で暗背景に対し 2.20（`accentSurface` へ）。**2026-07-26に実機で一巡し問題なしを確認**（色・余白定数の追加調整なし）| [dark_mode](design/dark_mode.md)・[theme_and_ui_refactor](design/theme_and_ui_refactor.md) |
| 2026-07-26 | — | **テーマ基盤とUI構造のリファクタ（R-1〜R-4／ダークモードの土台）**。**R-2**: 同一概念だった `SectionFabStatus`（NoteReaderTab内）と `VigilithActionStatus` を後者へ統一し、`NoteReaderTab.kt` の2箇所に逐語コピーされていた導出ロジックを `VigilithMode.kt` の純関数 `sectionChatStatus` / `fullscreenAiStatus` へ集約。テストゼロだった「要約×クイズ」合成に11ケースを新設。**R-1**: `NoteReaderTab.kt`（626行）を タブ本体247行／`FullscreenNoteScreen.kt` 248行／共用部品 `NoteComponents.kt` 175行へ分割。あわせて `AnnotationManagerScreen` の `IconPillButton`（`contentDescription` をSemanticsへ設定しておらずTalkBackで読み上げ不能）を共用 `IconPill` へ統合し、見た目を変えずにa11yを修正。**R-3**: `MainActivity` の `setContent{}` に直書きされていたVigilithの導出・保持・タップ配線を `rememberVigilithState()`（`VigilithState.kt`）へ抽出。タップ分岐は stale closure を避けるため `rememberUpdatedState` 経由。**R-4**: `Color(0x…)` 直書き61箇所／9ファイルを全廃し、`AppColors.kt` を「ブランドパレット／役割トークン／クイズ暗色面／グラデーション」の4区画へ再編。`LinkBlue`→`LinkText`、`ErrorRed`→`ErrorText` へ改名（名前に色を含めない）。塗りボタンのラベル色を明示（Material3の既定 `onPrimary`＝純白に暗黙依存しており、ピンク3.41／緑2.49とAA基準割れだった → 黒ラベルで6.16／8.44へ）。`AppTheme`＋`LocalAppColors` を新設し `setContent{}` 直下へ挿入。コントラストを固定するテスト9件を追加。**唯一の意図的な見た目変更はボタンのラベル色（白→黒）**、他は現状維持。テストは352→372 | [theme_and_ui_refactor](design/theme_and_ui_refactor.md)・[dark_mode](design/dark_mode.md) |
| 2026-07-25 | #35 | **ReadingTrace レビュー指摘4件の修正**。**①到達率の誤100%**: ブロック数だけで測っていたため、長大な段落・コード・表が1ブロックとして描画されると冒頭しか見ていなくても `1/1=100%` になり、カードが「最後まで読んでいます」と誤断定していた。`(最深ブロックindex + そのブロックの可視割合) / 総ブロック数` の**切り捨て**へ変更し、100%を「最終ブロックの末端が画面へ入った」場合だけに限定。可視割合は LazyList座標(Int)だけを受ける純関数 `visibleFractionOfBlock`（`ui/ReadingProgressGeometry.kt`）へ切り出し、報告は5%刻みへ量子化してスクロール中の連続emitを抑えた。**②背面化・復帰**: `onStop` の flush でセッションが永久に消費済みになり、復帰後に読み進めた位置を失う／10秒未満なら背面時間が閾値へ混ざる問題を、`pause()`/`resume()` による**能動読書時間の積算**へ変更。背面化時の書き出しは維持しつつセッションを残し、復帰後の読み進めで**同じ訪問を差し替える**（ホームボタンのたび「これまで◯回」が膨らまない）。差し替えは保存済み末尾が自分の訪問と一致する場合に限定し、別端末の追記は上書きしない。**③Vault分離**: 保存が非同期起動で保存先を書込時点の `vaultUri` から解決していたため、切替後の新Vaultへ旧ノートの痕跡が書かれ得た。ノートを開いた時点のVault識別子(String)を要求へ添えて運び、Gatewayが書込直前に照合して不一致なら捨てる方式へ変更。当初の実装は確認後に `vaultUri()` を読み直しており対策が効いていなかったため、**読み取りを1回に集約**して修正した（[bugfix_reports](bugfix_reports.md) #4 に収録）。`NoteViewModel.vaultUri` は `@Volatile` へ。**④検索フォールバック**: 候補40件以下では並べ替えを通らずSAF列挙順の先頭3件を返しながら「キーワード一致で表示しています」と表示していた。候補数に依らずbigramスコア順へ統一し、**一致0件は返さない**（0件時は既存の「見つかりませんでした。」）。採点・選抜を `domain/SearchKeywordMatching.kt` の純関数へ切り出し、フォールバック（0件は落とす）と再現率カット（Nanoへ渡すので0件も残す）を分離。1文字クエリがbigramを作れず全件0点になる穴も部分一致で解消。テストは282→317ケース | [reflect_reading_trace](design/reflect_reading_trace.md) |
| 2026-07-22 | #32 | **蒸留（Distill）v1**: Reflectで、AIが重要文を選び（生成しない）ユーザー確認分だけを元ノートで太字化するプログレッシブ要約支援を新設。UTF-16オフセット保持＋Markdown構造認識の専用モデル（`DistillSourceModel`）、AI不使用のサリエンス＋構造的重みスコア、境界regexのID応答パース、原バイトSHA-256の二重競合確認＋`noBackupFilesDir` 復旧レコード＋起動時4分岐＋v1最小復旧UI、`openNote()` を使わない本文専用リロード（要約/関連/補記は維持・チャット/クイズは破棄）。累積太字上限は編集対象本文比率30%＋短文1文例外。保存後の状態遷移バグ（旧セクション文脈の維持で誤フォールバック・旧エラー再表示）も修正。**クイズ生成エラー削減**: 常に4択2問+解説を強要して出力上限超過→`MAX_TOKENS`全破棄を、`QuizInputProfile` の入力量分類で○×2問/3択2問/4択1問へ適応化し解説を削減して回避。多択の正解解釈を単語境界regexへ変更し崩れた応答を救済（BugFix #3） | [reflect_distill](design/reflect_distill.md)・[reflect_reading_trace](design/reflect_reading_trace.md)・[section_ai_chat](design/section_ai_chat.md)・[bugfix_reports](bugfix_reports.md) |
| 2026-07-21 | #31 | 全画面ノート（#30）の潜在不具合修正（レビュー対応）。**①スクロール書き戻し競合**: 離脱時 `scrollToItem` を起動しっぱなしにするとpop→scopeキャンセルで書き戻しが完了せず消えていた（フリング中に顕著）。`action()`（閉じる処理）を `scrollToItem` 完了後へ移動。**②進入前Snackbar残存**: `SnackbarHost` は全画面ルートでも描画されるため、進入時に `currentSnackbarData?.dismiss()` で既存通知を消す。**⑤アクセシビリティ**: `IconPill`（✕/⛶）のデッドだった `contentDescription` をSemanticsへ設定し絵文字を読み上げ除外（二重読み上げ回避）、`FullscreenAiFab` を48dp（Material推奨タッチ領域）へ拡大し `role`/`onClick`/`contentDescription` を付与しスクリーンリーダー操作可能に。**⑥多重遷移防止**: `note_fullscreen` 遷移へ `launchSingleTop` を付与。※Aquaコントラスト指摘はラベルがIndigo地上・アイコンが絵文字のため非該当として見送り。**⑦折りたたみ解除後に閉じられない不具合**: ①で `action()`（閉じる処理）を suspend の `scrollToItem` 完了後へ移したことの副作用で、Fold開閉によるActivity再生成後は `scrollToItem` が完了せず✕・システムバックとも無反応で全画面を解除できなくなっていた。非suspendの `requestScrollToItem` で保留位置を積むだけにして `action()` を即実行し、閉じる処理を書き戻しから切り離した（①の「pop→書き戻し消失」の意図は原理的に保持）。あわせて状態遷移でボタン無反応になる潜在バグの知見を `bugfix_reports.md` に新設（⑦＋Claude Fable 5混入のSectionFab古クロージャ固定を収録） | [note_fullscreen](design/note_fullscreen.md)・[bugfix_reports](bugfix_reports.md) |
| 2026-07-21 | #30 | UIUX改善。**②スケルトンUI**: 要約生成中の表示をスピナー＋文言から自前shimmerの3行スケルトンへ（accompanist非推奨のため`Brush.linearGradient`＋`infiniteRepeatable`で自作）。**④スクリム**: `ModalBottomSheet` のスクリムを既定より濃く（`alpha=0.5f`）し背景グラデーションの透けを抑制。**タイトル体系の統一**: 各タブH1を機能説明から英語コンセプト語へ（ランダムAIノート→Rediscover／さがす→Explore／関連ノート→Connect／AIアシスト→Reflect）。「英語H1＝概念／日本語サブ＝意味補助／日本語ボタン＝操作」の3層役割分担でブランド(Obsidian Mind)-概念-機能タブ(日本語)の階層を明確化。主ボタン文言も「動詞＋を＋名詞」へ統一。**ナビ選択ピル**: 下バー/レールの選択インジケータを `Panel@22%`（Indigo地に埋もれ視認性不足）から `Aqua` へ変更し選択状態を明確化（タブは絵文字アイコンのため色指定が効くのはピルのみ・ラベルはIndigo地上のため白を維持）。**全画面ノートの独立ルート化**: `content` 内オーバーレイ（バー/レールを覆えず・システムバー残存・額縁・スクロール先頭復帰）を非タブルート `note_fullscreen` へ移行。進入時に `systemBars()` 没入・離脱時ナビバーのみ復元、ノート色で全ブリード＋本文720dp中央寄せ、通常表示とスクロール位置を継承（遷移中の同時コンポーズを避けるため専用state＋開始継承/離脱書き戻し）。読書中も要約＋クイズの合成状態を最小FABで表示、全画面中はSnackbar抑制 | [note_fullscreen](design/note_fullscreen.md) |
| 2026-07-21 | #29 | 関連ノートAI推薦の Phase 3。**3a**: 採番順の並べ替えを全Vaultのタイトル話題スコアへ置換（文字bigram Dice係数＋採番近接の加点）。**3b**: 上位40件を現ノートの tags/snippet/title 類似で再ランクする二段ランキング（件数維持・並べ替えのみ／Nanoの位置バイアス対策）。採点戦略を注入する汎用ランキング基盤 `rankByScore` を分離し3a/3bで共有。実機で「jetpackcompose」ノートの関連が全てKotlin系で揃うことを確認 | [related_notes_ai](design/related_notes_ai.md) |
| 2026-07-20 | #28 | 関連ノートAI推薦の Phase 2。候補をタイトルに加え本文スニペット・タグ・aliasesで肉付けし入力予算内へ動的短縮（2a）／候補本文を `URI+lastModified` でキャッシュ（成功時のみ格納）し `Semaphore(8)` 上限付き並列で読込（2b） | [related_notes_ai](design/related_notes_ai.md) |
| 2026-07-20 | #27 | 関連ノートAI推薦の Phase 1。候補に一時ID(C01..)を採番しNanoにはID応答させるID応答方式（言い換え・同名衝突での解決失敗を解消）。※当初この行には「フォーカスセクション文脈での推薦（長ノートの先頭切り出し解消）」も記載していたが、2026-07-24の実装突合で**未実装**と判明したため削除（現ノートの文脈は全文の先頭600文字のまま） | [related_notes_ai](design/related_notes_ai.md) |
| 2026-07-20 | #26 | アプリ起動時のブランドOPアニメーションを追加。システムスプラッシュ（`core-splashscreen`）＋Compose OP（`OpeningScreen.kt`）の2層構成。背景を着地画面と同色（`ReadingGradient`）で終端し継ぎ目を解消／`savedInstanceState==null` で新規起動時のみ再生／アダプティブランチャーアイコン新設／レビュー対応（TalkBack二重読み上げ回避・発光色を`AppColors`へ集約） | [opening_animation](design/opening_animation.md) |
| 2026-07-20 | #25 | AI補記の途切れ対策（finishReason検知・出力要求の絞り込み）／クイズを「フォーカスセクション周辺から2問」へ再設計し入口を吹き出しシートへ移動（AIタブのQ&Aボタン廃止・バッジ補記のみ）／クイズのセッション従属化／吹き出しタップ無反応の修正（pointerInputのstale closure） | [section_ai_chat](design/section_ai_chat.md)・[background_ai_ux](design/background_ai_ux.md)・[architecture](design/architecture.md) |
| 2026-07-20 | #24 | PR #23のレビュー対応3件（デッドコード削除・Snackbarイベントキーのテスト可能化・画面回転での通知再表示抑止）／Vault選択をオプションへ移動／全画面✕ボタンの視認性修正／当日分の閲覧履歴「今日読んだノート」追加 | [background_ai_ux](design/background_ai_ux.md)・[tab_navigation](design/tab_navigation.md)・[ai_picker](design/ai_picker.md) |
| 2026-07-20 | #23 | Q&A・AI補記のバックグラウンド生成UX。待機画面への遷移を廃止し、Snackbar通知＋AIタブバッジ＋未確認（isViewed）管理に変更。`QuizController` 新設、requestIdによる古い結果の混入防止 | [background_ai_ux](design/background_ai_ux.md) |
| 2026-07-19 | #22 | AI要約の生成中もノート閲覧を継続可能に（要約待ちのブロッキング解消） | [background_ai_ux](design/background_ai_ux.md) |
| 2026-07-19 | #21 | 壁打ちUI改善: 読む画面の低彩度グラデーション（ReadingGradient）・ボタン配色3役ルールの明文化・ノート出現アニメーション・検索結果への更新日表示 | — |
| 2026-07-19 | #16〜#20 | コード品質改善活動。静的分析で27項目（重要度高4・中8・効率5・リファクタ10）を指摘し全件解消。`NoteViewModel` の機能Controller分割（906行→348行）、SAF走査キャッシュ、AIジョブのキャンセル/タイムアウト制御、ユニットテスト整備 | [architecture](design/architecture.md) |
| 2026-07-18 | #15 | AIピッカー「さがす🔎」タブ。フォルダスコープを共通土台に、自然文キーワード（Nano選定3件）とランダム（AI不使用3件）の2モード | [ai_picker](design/ai_picker.md) |
| 2026-07-17 | #14 | セクション単位AIチャット。浮遊吹き出しがスクロールに追従し、今見ているセクションの要約・質問候補・Q&Aを生成 | [section_ai_chat](design/section_ai_chat.md) |
| 2026-07-16 | #13 | AI補記メモの削除機能（オプション画面を新設） | — |
| 2026-07-16 | #12 | UIをタブ・ナビゲーション構成に再設計（Plan C）。Foldのボタン切れとダブルタップ全画面不発を構造的に解消 | [tab_navigation](design/tab_navigation.md) |
| 2026-06-19 | #11 | AI補記プロンプト改善・UI名称変更・表示整理 | — |
| 2026-06-19 | #10 | Android 17対応・ステータスバー非表示・UI調整 | — |
| 2026-05-31 | #9 | AI補記メモ機能の追加・AI推薦の改善 | — |
| 2026-05-31 | #8 | Q&Aフラッシュカード（オンデバイス4択生成）・グラフビュー・関連ノート改善 | — |
| 2026-05-30 | #7 | リファクタリング: Activity分割・ノート走査のスタック安全化 | — |
| 2026-05-30 | #6 | Fold展開時に関連ノートTop-5を左ペイン表示・タップで本文切替 | — |
| 2026-05-30 | #5 | Gemini Nano 4によるオンデバイスノート要約（ML Kit GenAI Prompt API） | — |
| 2026-05-30 | #4 | Markdownレンダリング強化（見出し・リスト・コード・引用・テーブル等） | — |
| 2026-05-11 | #2, #3 | アプリ名を「Obsidian Mind」へ変更、旧View系リソース整理、ソースコード解析書の整備 | — |
| 2026-05-10 | #1 | Jetpack Compose移行 | — |
| 2026-04-30〜05-10 | — | プロジェクト開始。Java実装→Kotlin化、MVVM＋SAF＋Coroutinesの基礎構築 | [project_origin.md](../owner/project_origin.md) |
