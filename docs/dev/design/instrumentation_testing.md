# 設計思想 — 実端末を通すテスト（instrumentation）

**対象領域:** `androidTest` に何を置き、何を置かないか。実物SAFの作り方。実行の運用
**初版:** 2026-08-08
**状態:** 全段階（1〜4c）実装済み・**実機確認済み**（2026-08-08 / Pixel 10 Pro Fold で **37/37 成功・0 skipped**）。**CIでの実行は見送りで確定**（→ 判断4）

---

## 背景

課題台帳は長らく「実端末を通す統合テストの中身が無い（規模: 大）」を抱えていた。
着手前に実物を当てたところ、**「大」は前提が無かった頃の見積もりのまま**で、
その後の構造変更で条件が大きく変わっていることが分かった。

| 当時なかったもの | 現在 |
|---|---|
| instrumentation が動く保証 | 2026-08-01 に Android 16 で 2/2 成功 |
| SAF境界の差し替え口 | `VaultBrowser` が**インターフェース**（2026-08-01・段階7） |
| `androidTest` からの `internal` 可視性 | 既に利用中（`selectDistillCandidates` 等） |
| Activity/Compose のハーネス | `ActivityScenarioRule` と `ui-test-manifest` が稼働中 |

**規模感を「大」から「中」へ改める。**

## 判断1: 置くのは「実測が要るもの」だけ

判定は機能名ではなく**「JVMで書けないか」**で行う。純関数の値の伝播・分岐・境界値はJVM側が
覆っているので持ち込まない。`androidTest` に残すのは Compose のレイアウト実測、
可視判定、再コンポーズ、Activityのライフサイクル、実物のSAF/`BitmapFactory` に依存するもの。

**この基準は台帳（TEST-2）に元からあった**が、instrumentation を書き始めると
「ついでにここも」で崩れやすい。各テストのKDocに**なぜJVMでは書けないかを1行残す**ことで、
後から読んで判断できるようにする。

## 判断2: ViewModel を組み立てない（段階1）

`NoteReaderTab` / `FullscreenNoteScreen` は `NoteUiState` と `LazyListState` を
**素の引数として受け取る**。UIに業務ロジックを置かない規約の副産物として、
**ViewModelもFakeも無しに画面を描ける**。段階1が本番コードの変更ゼロで済むのはこのため。

**`MainActivity` 経由では差し替えられない。** `by viewModels()` でデフォルトファクトリを
直に使っているため、依存を注入する口が無い。段階1では画面コンポーザブルを直接 `setContent`
することで回避した。Activity全体を通したい要求が出たら、そのとき ViewModelFactory の
seam を本番へ入れるか判断する。**先回りで seam を作らない** — 使い道が確定していない
抽象を本番へ足すことになるため。

**`src/test` の Fake は `androidTest` から見えない**（別ソースセット）。段階2で共有が
必要になったら、共有ディレクトリを両ソースセットへ足す形を採る。段階1では不要だった。

## 判断3: ブロック番号を本文へ埋めて可視位置を特定する

`ReadingProgressReporter` は `visibleItemsInfo.last().index` を**そのままブロック番号として**
報告している（LazyColumn の index とブロック番号がずれない前提が本番側にある）。
したがってテスト本文を「段落0」「段落1」…と並べておくと、**可視位置を文言で特定できる**。
`LazyListState` の内部値ではなく画面に出ている文字で判定できるので、
レイアウトの細部が変わっても壊れにくい。

**全画面の引き継ぎ判定には先頭可視ブロックを使う。** 全画面はシステムバーを隠して表示域が
変わるため末尾可視ブロックは1つずれ得るが、引き継がれるのは
`firstVisibleItemIndex` / `firstVisibleItemScrollOffset` なのでそこを見る。

## 段階

| 段階 | 内容 | 本番変更 | 状態 |
|---|---|---|---|
| 1 | 読書画面（解析待ちの描画抑止・全画面への位置引き継ぎ・進捗報告の整合） | なし | **完了**（実機 4/4） |
| 2 | 実物SAF（`src/debug/` のテスト用 `DocumentsProvider`）＋走査・補記の保存/削除・読取失敗の注入 | debug ソースセット追加 | **完了**（実機 6/6） |
| 3 | `NoteImageGateway`（段階2の上に乗る） | なし | **完了**（実機 7/7） |
| 4a | 端末AIの横展開（**本番プロンプトで実際に生成させる**） | なし | **完了**（実機 4/4） |
| 4b | **同一プロセス内の**Activity再生成（回転・Fold開閉。`ActivityScenario.recreate()`） | なし※ | **完了**（実機 2/2）※※ |
| 4c | Compose Navigation の往復・**タブ履歴契約**（連続入力の競合は撤回 → 判断13） | なし | **完了**（実機 5/5。`popUpTo` 変異で落ちることを確認済み → 判断12） |

※ 着手前は「Activity を通すなら `MainActivity` の `by viewModels()` に差し替え口が
要るのでは」と見ていたが、**要らなかった** — 確かめたい振る舞い
（再生成でOPを再生し直さない）は **Vault 未選択のままで観測できる**ため、
実依存のまま起動してよい。判断2（先回りで seam を作らない）を維持している。

※※ **プロセス死亡後の復元は覆っていない。** `recreate()` は同一プロセス内で Activity を作り直すだけで、Application・静的状態・プロセス内キャッシュは生き残る。**34/34成功からプロセス死亡耐性は結論できない**（2026-08-08 の外部レビュー指摘。クラス名を `ActivityRecreationTest` へ改め、主張を狭める形で対応した）。

**4a・4b・4c は互いに独立**で、順序の制約が無い。3つとも段階1〜3の土台に乗る。

**段階2のプロバイダは `androidTest` ではなく `src/debug/` へ置く。**
`androidTest` 側に置くと別APK・別UIDになり、URI権限の付与が要る。
debug ソースセットならアプリ本体と同じUIDになるので、`DocumentsContract` の
tree URI をそのまま扱える。**release には入らない。**

## 判断4: CIでは実行せず、実行を運用で担保する（2026-08-08 に再確認し、見送りで確定）

**現在のCIは `assembleDebugAndroidTest`（コンパイルのみ）で、instrumentation を1件も実行していない。**
このまま増やすと「コンパイルは通るが誰も走らせないテスト」になり、
[lessons L29](../lessons.md) と同じ形（規則を書いただけで検査になっていない）になる。

それでも**CIにエミュレータジョブを足さない**と判断した。理由は2つ。

1. **端末AI依存のテストは、エミュレータでは `Assume` で skip される。**
   しかも**最も実機固有な部分がここ**なので、エミュレータが緑でも実機確認は残る。
2. CI時間の増加が大きく、現在の1人開発の回転数に見合わない。

代わりに**「PR前にAndroid Studioで実行する」を運用として明記**する。
**運用は検査に劣ることを承知の上での選択。**

### 2026-08-08 — 件数トリガーで再検討し、見送りで確定した

当初は「**instrumentation が20件を超えたら再検討**」を条件に置いた。
34件に達して条件が発火したので再検討し、**結論は見送り**である。

**見送る理由は「入れられない」ではなく「入れる根拠がまだ立っていない」。**

- **踏んだ2件は、実行忘れの流出ではなく基盤の初期設定ミスだった。**
  Manifest 不備（0件実行）はテスト基盤を初めて作ったときのもので、
  `@Test` の戻り値は**すでに `InstrumentationTestShapeTest` で再発防止済み**。
  どちらも「普段の開発で回し忘れて壊れたまま進んだ」実績ではない。
- **端末AI依存の9件はCIで守れない。** エミュレータが緑でも実機確認は残るので、
  **確認が二重になる。**
- **無料でも保守は要る。** エミュレータ由来の不安定な赤、PR待ち時間、
  API/system image・KVM・除外クラス・タイムアウトの面倒を見続けることになる。
  Linux での実行にはハードウェア仮想化の設定も要るので、
  **「ジョブを1つ足すだけ」は変更量としては正しくても運用としては軽すぎる。**

**再検討の条件を件数から実害へ変える。** 次のどれかが起きたら見直す。

1. instrumentation の**実行忘れで実害が一度出た**
2. 開発者が**複数人**になった
3. PR頻度が増え、毎回手で回すのが**明確に苦痛**になった
4. ストア配布前の**リリースゲート**が要るようになった

**件数はトリガーとして機能しなかった。** 20件という閾値は同じ日に発火し、
しかも「入れる価値が出た」という**誤った結論**を一度出させた。
数が増えたことは、手で回すのが苦痛かどうかを何も語らない
（→ [lessons L31](../lessons.md)。同じ誤りを `architecture.md` の
Controller共通化でも踏んでいる）。

## 判断5: 読取失敗は「例外」ではなく「null カーソル」で作る

`querySafChildren` は `contentResolver.query(...) ?: SafChildren.UNREADABLE` という形なので、
**実物の読取失敗は「query が null を返す」こと**である。したがって
[FakeVaultDocumentsProvider] の `queryChildDocuments` も、失敗させたいフォルダで
null を返す。例外を投げる形にすると本番が通っていない経路を試すことになる。

これで初めて、**「ノートが無い」と「そのフォルダを読めなかった」の区別**を
実物のSAF経路で確かめられる。痕跡の孤児判定は不在を根拠に**削除する**ため、
ここが崩れると生きた痕跡が消える。TRACE-1・TRACE-2 で純関数側は固めたが、
**`NoteRepository` から `VaultScan` までの実配線は手動確認しか担保が無かった。**

**必要だったのは `isChildDocument` の override。** tree URI 配下の入れ子を触ると
`DocumentsProvider.enforceTree()` がこれを呼ぶので、実装しないと
ルート直下しか読めない。

**マニフェストの3条件は必須で、外すと「テストが0件」になる。**
`DocumentsProvider.attachInfo()` は自分の宣言を検査し、
`exported="true"` ／ `grantUriPermissions="true"` ／
読み書きが `android.permission.MANAGE_DOCUMENTS` で保護されている、の
どれかが欠けると `SecurityException` を投げる。
**プロバイダはアプリ起動時に生成されるので、ここで落ちるとテストは1件も走らず、
Gradle は「テスト結果を読めない」としか言わない**（テストの失敗として現れない）。
最初に `exported="false"` で書いて実際にこれを踏んだ。

`exported="true"` でも外部から触れるわけではない。`MANAGE_DOCUMENTS` は
システム（DocumentsUI）だけが持つ署名権限で、通常のアプリは取得できない。
自アプリからは同一UIDなので権限検査を通らずに済む。
**`DOCUMENTS_PROVIDER` の intent-filter は置かない** — 宣言するとSAFのピッカーに
偽Vaultが並ぶ。直接URIを組む用途では要らない。

**`DocumentsProvider` の実体はシステムが生成する**ためテストから掴めない。
木の組み立てと失敗注入は `companion` 側の API（`reset` / `putFile` / `makeUnreadable`）に置く。

**教訓は「0 tests は失敗ではなく起動前の墜落」と読むこと。** 個々のテストを疑う前に
プロセスが立ち上がったかを見る。原因は `adb logcat` に例外として出る。

## 判断6: 上限の境界はBMPで通す（PNGでは通らない）

段階3は、IMG-3 のレビューで唯一満たせなかった受理条件
「実物の `NoteImageGateway` で `TooLarge` と `Broken` を分ける」を埋めるもの。

**最初に書いた「16MiBのゼロ列を食わせる」テストは無意味だった。**
`BitmapFactory` はヘッダを読んだ時点で画像でないと判断して読むのをやめるので、
**`BoundedInputStream` が上限に到達せず、境界を1バイトも通らない。**
`truncated` が立たないため結論は `Broken` になり、
**修正前の実装でも同じ結果**＝退行を検出できない。

そこで**非圧縮の24bit BMP**を組み立てる。BMPは寸法からファイル長が決まり、
復号器が画素データを最後まで読むので、上限の内外を狙って作れる。

| 寸法 | ファイル長 | 上限（16MiB）との差 | 期待 |
|---|---|---|---|
| 2400×2300 | 16,560,054 | −217,162 | 復号成功（大きさを理由に落とさない） |
| 2400×2400 | 17,280,054 | +502,838 | `TooLarge` |

**サイズを申告しないプロバイダとして置くのが要点。** メタデータ判定
（`size > MAX_INPUT_BYTES`）を素通りさせないと、`BoundedInputStream` 側の
判断まで届かない。`COLUMN_SIZE` に null を返すプロバイダは実在するので、
再現としても正しい。

**画素の中身は問わない（全部ゼロでよい）。** ここで見たいのは長さと
読み切れるかどうかだけで、絵が正しいかではない。

**「復号器がどこまで読むか」を意識しないと、境界のテストは空振りする。**
入力を大きくしただけでは上限に触れない。


## 判断7: 端末AIは「生成が返ること」までを assert し、書式は観測に留める

段階4a に着手して分かったのは、既存の `PromptTokenBudgetTest` が**計測しかしておらず、
`generate()` を本番プロンプトで通す経路が1つも無かった**こと。
`maxOutputTokens` の「1〜256」制限で全AI生成が落ちた前例は、ここが空いていたために
本番まで抜けた。**上限に収まることと、生成が返ることは別**である。

**assert するのは「空でない応答が返る」まで。** SDK制約・プロンプト長・API互換の破壊は
これで落ちる。**応答の書式は assert しない** — 端末AIの出力は非決定的なので、
一致を要求すると壊れていないのに赤くなる。パーサへ通した結果は logcat へ出して
人間が読む材料に留め、パーサについて assert するのは
**「実出力を渡しても例外を投げない」**ことだけにする。

**この線引きを動かしたくなったら、先に本番側を疑う。** 書式一致を assert したい＝
パース失敗時の見せ方が本番に足りていないサインかもしれない。
テストを厳しくするより、失敗したときのUXを確かめる方が効く。

## 判断8: 画面の目印は「タブのラベルと重ならない文言」から選ぶ

段階4c で、オプション画面の見出しがタブのラベルと同じ「オプション」であるため、
`onNodeWithText("オプション")` が**2件に当たって落ちる**ことが分かった。
遷移先の判定には**画面内の項目名**（「AI補記メモを削除」）を使う。

`ノート` / `さがす` / `関連` / `AI` は UI 上に1件ずつしか無いので、
現状ぶつかるのはオプションだけ。**画面へ文言を足すときは、
タブのラベルと同じ文字列にしないか確かめる**（テストが壊れるだけでなく、
読み上げでも同じ語が二度出る）。

## 判断10: 目印は「端末の実データに依存しないもの」から選ぶ

段階4b の目印に「Vaultを選択」ボタンを使っていたが、
**あれは `if (!vaultSelected)` の中にあり、Vault未選択のときしか描画されない。**

**instrumentation は実機に入っているアプリの `SharedPreferences` をそのまま使う。**
したがって**オーナーが端末でVaultを選んだ瞬間にテストが落ちる**。
2026-08-08 に実際に落ちた（`ActivityRecreationTest` が 2/2 から 0/2 になった）。

**切り分けの手がかりは「同じ画面の別の目印は通っていた」こと** —
`TabNavigationTest` は「Rediscover」で通っており、画面自体は出ていた。
消えたのはボタンだけなので、**アプリの状態依存だと分かる。**

目印は**アプリの設定状態に関わらず出るもの**（画面見出しなど）から選ぶ。
判断8（タブのラベルと重ならない）と合わせて、
**「一意」かつ「状態に依存しない」**の2条件で選ぶ。

## 判断11: 開始タブでの「戻る」は Activity を終了させる

段階4c の連続タップテストで、開始タブに着いた状態から `onBackPressed()` を送っていた。
**`NavController` に戻す先が無いので back は Activity の終了へ伝播し**、
直後の `assertIsDisplayed()` が落ちる。

バックスタックの主張は**開始タブ以外に居る状態で**確かめる。
連続入力の競合とバックスタック契約は**別のテストに分ける**
（同じテストで両方を見ようとしたのが原因）。

## 判断12: バックスタック契約は「組み合わせ」で守る（単体は隔離しない）

段階4c の変異検証（2026-08-08・実機）の結果。

| 外した設定 | 「さがす」から戻る | 開始タブ（ノート）から戻る |
|---|---|---|
| `popUpTo` | **落ちる** | — |
| `launchSingleTop` だけ | 緑のまま | **緑のまま** |
| `launchSingleTop` ＋ `restoreState` | — | **落ちる**（`RESUMED`） |

**`launchSingleTop` 単体は識別できない。** 開始タブへ移せば観測できると考えたが、
実機で試すと**`restoreState` が肩代わりして緑のまま**だった。
最初の想定（「開始タブなら落ちる」）は誤りで、**実測で否定された。**

**単体を隔離するテストは作らない。** `navigateToTab()` から設定を1つだけ外した配線は
本番に存在せず、それを固定しても**AndroidX のオプションの挙動を確かめるだけ**になる。
守りたいのは**「タブを何度選んでも戻る操作が設計どおりになる」という契約**なので、
テストの記述も3設定の組み合わせに対するものへ揃える。

**「変異させて落ちるか」を実機で確かめるまで、観測点が有効かは分からない。**
このテストは2度作り直している — 1度目は「さがす」を2回選ぶ形（緑のまま）、
2度目は開始タブへ移した形（やはり緑のまま）。
**机上で「これなら落ちるはず」と考えた2回とも外した。**

## 判断13: 入力の競合は Android 17 では作れない（主張を撤回した）

段階4c で「連打」を試すため、座標を1度だけ取って
`Instrumentation.sendPointerSync()` で生の `MotionEvent` を投げる形にした。
**Android 17 では instrumentation のUIDからの入力注入が拒否される**ため、実機で落ちた。

Compose のAPI（`performClick`）は毎回 `fetchSemanticsNode()` で同期するので、
そもそも遷移を重ねられない。UiAutomator を足せば通る余地はあるが、
**1つのテストのために依存を増やさない。**

**そこで競合そのものの主張を撤回した。** 連打で実際に困るのは
「履歴が積み上がって戻る回数が合わなくなる」ことなので、
**その契約のほうを直接観測する**（判断12）。
**試せないものを試したことにしない**のが、テストを増やすことより優先する。

## 判断9: `@Test` の戻り値は検査で縛る（規約にできない）

段階4a の初回実行で **`OnDeviceGenerationTest` が4件まるごと起動しなかった。**
原因は JUnit4 の `@Test` メソッドが `void` を要求すること — Kotlin の
`fun x() = runBlocking { ... }` は**ブロックの最後の式の型がそのまま戻り値になる**ため、
末尾が `Log.i()`（`Int` を返す）だと `void` でなくなる。

**性質が悪いのは3点。**

1. **コンパイルは通る。** `assembleDebugAndroidTest` は JUnit の契約を見ない。
2. **`InvalidTestClassError` はクラス単位。** 1メソッドの型ミスで**クラス全件が起動しない。**
3. **赤ではなく件数の減少として現れる。** 実行結果は
   「1 failed, 30 passed」で、失敗1件に見える。**消えた3件は数えられない。**

**「末尾に値を返す式を置かない」は規約にできない** — 末尾に1行足しただけで破れ、
破れたことに気づく手段が無い。そこで**書き方のほうを縛る**:
式本体に `runBlocking` を使うなら `runBlocking<Unit>` にする。
`InstrumentationTestShapeTest`（JVM）がソースを走査して固定する。

**Gradle の入力に `src/androidTest` を宣言するのを忘れない。** JVMテストの
コンパイル対象ではないので、宣言しないと**androidTest だけを直したときに
UP-TO-DATE で飛び、検査が一度も走らない**。受付簿の検査で `docs` に対して
踏んだのと同じ穴（→ [lessons L29](../lessons.md)）。

**この検査が見ていないもの:** 式本体が `runBlocking` 以外（`runTest` 等）の場合。
使い始めたら足す。ブロック本体（`fun x() { ... }`）は戻り値が `Unit` に決まるので元から安全。

## ソースセットが4つになった

`src/debug/` を足したので、Android Studio のプロジェクトツリーに
`com.example.newproject.testing (debug)` が並ぶようになった。役割は次のとおり。

| ソースセット | 入るビルド | 中身 |
|---|---|---|
| `main` | すべて | 本番コード |
| `debug` | debug のみ | **テスト用の偽Vault（`DocumentsProvider`）だけ** |
| `test` | ビルドに入らない | JVMユニットテスト |
| `androidTest` | 別APK（テスト用） | instrumentation テスト |

**`debug` はアプリ本体側**なので、`androidTest` と違ってアプリと同じUIDで動く。
段階2の偽プロバイダをここへ置いたのはそのため（→ 判断5の直前）。
**release ビルドには入らない** — `processReleaseMainManifest` の出力に
プロバイダ宣言が現れないことを確認済み。

## 実機確認の結果（2026-08-08）

Pixel 10 Pro Fold（Android 17）で **34/34 成功・0 skipped**。
全段階が緑になり、当初リストは消化した。

| 段階 | 件数 | 初回で緑だったか |
|---|--:|---|
| 1 読書画面 | 4 | ✓ |
| 2 実物SAF | 6 | マニフェスト宣言の修正が要った（→ 判断5） |
| 3 画像Gateway | 7 | ✓ |
| 4a 端末AI生成 | 4 | クラスが起動しなかった（→ 判断9） |
| 4b 再生成 | 2 | ✓ |
| 4c タブ遷移 | 4 | ✓ |
| 既存 | 7 | ✓ |

**3回の実行で、2回は「テストの失敗」ではない形で止まった。**
段階2は**0 tests**（プロバイダ宣言の不備でアプリが起動時に落ちた）、
段階4a は**件数の減少**（1メソッドの戻り値型でクラス全件が起動しなかった）。
どちらも赤いテストとしては現れないので、
**まず「期待した件数が出ているか」を見る**のが最初の一手になる（→ [lessons L30](../lessons.md)）。

**副産物が2つ。**

1. **端末AIが `Assume` で飛ばされず実行された**（`FeatureStatus.AVAILABLE`）。
   計測5件に加えて**生成4件も実際に走っている**。
   ただし「CIのエミュレータでは skip される」という判断4の前提は変わらない。
2. **判定を本文の文言で行う設計（判断3）が実測でも安定した。**
   全画面はシステムバーを隠して表示域が変わるが、先頭可視ブロックで見ているため影響が無い。

**Claudeは instrumentation を実行できない。** 以後も
「実装＋`assembleDebugAndroidTest` 通過」までがこちらの担保で、
**緑であることの主張はオーナーの実行結果を待つ**。
