# Gradle
.gradle/
.kotlin/
/build/
/app/build/

# Local environment
/local.properties

# IDE
/.idea/
*.iml

# Build outputs
*.apk
*.aab
/app/release/
captures/

# macOS
.DS_Store

# Signing keys
*.keystore
*.jks

# Claude Code
.claude/

# レビュー本文（日付つき）は作業ツリー限りで履歴へ残さない。
# 端末の識別子や検証中のローカルパスが入るため。処遇は docs/review/findings.md が持つ。
docs/review/[0-9][0-9][0-9][0-9]-*.md
docs/review/device_validation/
# 上の手順書だけを見る検査なので、手順書と寿命を合わせて作業ツリー限りにする。
app/src/test/java/com/example/newproject/architecture/DeviceValidationDocsTest.kt
# 実機検証の依頼書は使い捨て。依頼が済めば消えるので履歴へ残さない。
docs/review/pending_device_validation.md
