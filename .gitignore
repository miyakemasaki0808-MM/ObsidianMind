# Gradle
.gradle/
.kotlin/
/build/
/app/build/

# Local environment
/local.properties

# IDE
/.idea/
*.iml

# Build outputs
*.apk
*.aab
/app/release/
captures/

# macOS
.DS_Store

# Signing keys
*.keystore
*.jks

# Claude Code
.claude/

# 実機検証の記録のうち、端末を特定するものと使い捨てのものだけを履歴へ残さない。
# 再利用できる手順とケースは追跡する（docs/review/device_validation/ の残り）。
#
# 日付つきレビュー本文（端末モデル・実測値・一時領域のパスが入る）。処遇は findings.md が持つ。
docs/review/[0-9][0-9][0-9][0-9]-*.md
# 実シリアル・Vault URI・端末内パスを書く手元の設定。
docs/review/device_validation/local_config.md
# スクリーンショット・UIダンプ・pullした検証ファイル。
docs/review/device_validation/evidence/
# 実機検証の依頼書は使い捨て。依頼が済めば消える。
docs/review/pending_device_validation.md

# Fable 5.1 の評価報告書。**特別枠として履歴に残さない**（オーナー判断・2026-09-12）。
# 課題候補29件の処遇は docs/_wip/fable51_triage.md が持つので、本文が消えても追跡は切れない。
docs/owner/Fable5.1_report/

# オーナーの実Vault。分析のため一時的に置くことがあるが、**絶対にコミットしない**
MyVault/
MyVault_Fable5.1Version/
